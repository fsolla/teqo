/**
 * S13 — shared card-studio copy. The privacy notes are literals of the intent
 * and must stay identical between the `/cards` page and the composer.
 */

export const CARD_PRIVACY_NOTE =
  'Seu nome e sua foto são processados apenas no seu aparelho e não são enviados para nós.'
export const CARD_PHOTO_PRIVACY_NOTE = 'Sua foto fica neste aparelho e não é enviada para nós.'
/**
 * S31 — the colinha has no typed name and no photo: the slip is composed from
 * the official art plus the deputy choice, all on the device (literal of the
 * design gate, scene 3).
 */
export const CARD_COLINHA_PRIVACY_NOTE =
  'A escolha é processada neste aparelho. Nenhum nome, foto ou cadastro é solicitado.'

/**
 * S17 — the tone-harmony control of the team model. Literals of the intent and
 * the design gate; keep both identical between the plan and the composer.
 */
export const CARD_HARMONY_LABEL = 'Harmonizar cores'
export const CARD_HARMONY_HELP =
  'Ajusta o brilho e as cores da sua foto para combinar com as fotos do card.'

/**
 * S14 — example name of the name-model tile. Drawn by the real composer
 * pipeline over the base image (same fit and left border as the generated card).
 */
export const CARD_NAME_TILE_PLACEHOLDER = 'SEU NOME'
