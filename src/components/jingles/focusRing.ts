/**
 * S21/S22 — the shared focus-visible ring of the jingles surfaces (play,
 * download and the "Ver todos" handoff): yellow outline over the dark red
 * shadow, the same policy the campaign sections apply via `safe-focus` in the
 * design artefact.
 */
export const JINGLE_FOCUS_RING =
  'focus-visible:shadow-[0_0_0_2px_var(--pt-red-dark)] focus-visible:outline-[3px] focus-visible:outline-offset-[3px] focus-visible:outline-(--pt-yellow)'
