**D13 — drift C6: `supporter_import_batch.actor_id` nullable (#643):** a migration `20260824_010000_make_supporter_import_batch_actor_nullable` alinha o schema — a coluna `actor_id` deixava de ser `NOT NULL` para concordar com a FK `ON DELETE set null` (a contradição original fazia o delete de um `campaignUser` dono de um lote não consumido falhar com 23502). O campo `actor` em `SupporterImportBatch` passa a `required: false` (config reflete o schema, sem drift futuro em `migrate:create`). O hook `deleteCampaignUserImportBatches` (CampaignUser.ts) e os hand-deletes em `personDelete`/`personCapacityExit` permanecem como cleanup explícito (evitam lotes órfãos até o sweep), seguindo o precedente de passkeys/notificações — sem `CASCADE` de schema. Cobertura: int em `personDelete.int.spec.ts` (lote com `actor` nulo é criável + delete de conta com lote órfão remove ambos sem violação).
