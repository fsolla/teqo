**OPS120 — relatório de cidade: página 1 sob pesquisa cheia (2026-09-16):** corrige o defeito em que o builder abortava a página 1 (`A página 1 estourou (…px > 1001px úteis)`) com o checklist de pesquisa cheio (9 itens) + notícias ≤90 dias — o `itacaré` do lote OPS118 estourou em 1110px. A causa não eram as notícias (na página 1 há só a contagem no KPI), e sim os textos livres dos `items[].answer` reusados na página 1 por `researchAnswerItem` ("Quem é quem", par anunciar×não, card de oposição e callout de emendas). O corte agora é por **teto/orçamento de conteúdo** em `scripts/lib/cityReportBlocks.mjs` — helpers `excerpt` (com `…` explícito) e `joinLimited` (`e mais N`), constantes `PAGE_ONE_ANSWER_MAX=90`, `PAGE_ONE_WHO_MAX=30`, `PAGE_ONE_RISK_MAX=70`, `PAGE_ONE_LIST_LIMIT=2` e `PAGE_ONE_NAMES_MAX=20`, no espírito dos caps do E16 — sem mexer em layout/CSS nem no `PAGE_ONE_BUDGET_PX` da guarda fail-closed. Para o corte nunca perder fato com fonte, a seção `fontes` ganha a tabela **"Pesquisa — respostas integrais (texto completo)"** (Item | Resposta integral | Data, com link inline), emitida só quando há itens de pesquisa. Prova local (Chromium, mesma viewport/print do builder): fixture que reproduz o itacaré (pré-fix 1142px) e fixture adversarial (respostas de 400 chars, 5 assessores, 6 dobradinhas, 6 riscos, 30 notícias) passam a guarda, e as 9 cidades reais do lote OPS118 seguem gerando PDF+MD com margem ≥ ~23px. Testes unit atualizados/criados em `tests/unit/cityReportBlocks.unit.spec.ts` (cap da página 1 + preservação integral no aprofundamento, texto curto intacto, `e mais N` sobrevive ao cap). Skill `.agents/skills/relatorio-cidade/SKILL.md` atualizada (página 1 capada, tabela em `14. Fontes e limites`, troubleshooting). Sem schema/migração/access/Consent/PII. Issue #1069.
