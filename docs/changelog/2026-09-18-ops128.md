**OPS128 — a verificação pós-deploy em staging sai do fechamento do `work-issue` (2026-09-18):** tira a obrigação de esperar/pollar o run do `deploy.yml` e testar a feature no navegador — o merge/flip `done` volta a ser o fim do fluxo. A mecânica era dona do `execution-pipeline.md` (OPS121) e é removida de lá (§"Verificação pós-deploy (staging)" inteira, incluindo a fronteira dura) junto com a coluna `Pós-deploy (staging)` da tabela `## Deltas por ator`; `work-issue/SKILL.md` perde o item 8 do checklist e o Passo 8 inteiro (o resumo final volta a `Issue · impl plan · simplify + débitos · PR + merge`); `agent-work-issue/SKILL.md` perde o defer "Cloud ⇒ §Verificação pós-deploy diferida" (checklist + Passo 6) — nenhum dos três cita mais o passo. A infra de staging do OPS125 fica intocada e reutilizável (`scripts/bootstrap-staging-test-account.mjs`, `scripts/lib/staging-test-account.mjs`, var `STAGING_TEST_ACCOUNT_PASSWORD`, §Staging do runbook): só a justificativa nos comentários/runbook deixa de prometer o fluxo removido. `bug-fix` (confirmação manual em produção) e `.github/workflows/deploy.yml` intocados; testar pós-merge segue desejável, mas será desenhado em item futuro próprio. Sem migration, sem UI, sem código de runtime. Issue #1188.
