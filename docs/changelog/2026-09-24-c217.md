**C217 — Cortes e links compartilháveis na fonte "Falas na internet" (2026-09-24):** o corte do acervo deixa de ser só da Câmara e passa a valer para as falas da fonte web do C215/C216, sem mecanismo novo e **sem migration**: o `SpeechCut.speech` já era relação genérica. A entrada ganhou um predicado puro no dono (`speechCutSourceKind` em `lib/speechCut` — `web` + `mirroredMedia` → web, senão VOD da Câmara; web sem espelho → `null`, fail-closed, nunca cai no VOD) com o vocabulário de copy/prompt separado (`speechCutOriginKind`), usado pela action e pelo job; o `runSpeechCutJob` ganha o branch web — lê o espelho privado com `downloadPrivateMediaToFile`/`resolvePrivateMediaStaticDir` (linha sumida via `disableErrors` e objeto ausente viram `SPEECH_CUT_FAILURE_MIRROR_MISSING`, o literal honesto da tela), corta a MESMA janela com ffmpeg na variante de áudio (`buildSpeechCutAudioFfmpegArgs`: `0:a:0` + AAC + faststart num MP4 só-áudio, escolhida pelo mime via o dono `webSpeechMediaKind`) e publica na `media`/transação/retry/reaper de sempre — o caminho Câmara fica byte a byte (args de vídeo, labels default, `originOf`, crédito). Copy source-aware com default Câmara: fallback de título/descrição web sem "na Câmara dos Deputados", passo `resolving` ("Localizando o arquivo da fala"), mensagens de falha e prompt de IA em tail único (Câmara idêntico); o `SpeechCutDialog` ganha `speechSource` (renomeado de `source` para não colidir com a sugestão) e `speechDurationSeconds`, portando a anatomia da janela (trilho, alças e campos Início/Fim). No detalhe web (`WebSpeechDetailPlayer`) a transcrição vira o picker do corte — clique posiciona (C216) e marca/estende a frase com a máquina pura `speechExcerptSelection` (mínimo 5 s, sem teto), com `segmentInExcerpt` extraído para o dono (os dois players não podem divergir) e o segmento **em reprodução** separado do **selecionado** (neutro × borda vermelha, `aria-pressed`); "Cortar trecho" (CTA primário, "Baixar" outline, "Abrir na origem" ghost) abre a janela existente com `selection ?? initialExcerptRange` e o corte publicado mostra o result card in-session. A biblioteca ganha a origem visível no card (`SpeechCutOriginViewModel` com `source`/`platform`/`label`/`href`: pill "Origem: YouTube/Rádio/…" e link para o detalhe web), o kit produtivo do artefato (Copiar link primário + Baixar/WhatsApp com rótulos compactos no mobile) e o `SpeechCutOriginCard` source-aware ("Fala de origem"/"Ver fala na internet" + plataforma); a página pública `/corte/<id>` mantém o contrato (unlisted, `noindex`) e o crédito segue a origem real — web usa `Fonte: <plataforma> · <canal>` (`webSpeechCreditLabel`), fala sem plataforma/ausente omite a linha em vez de afirmar a Câmara. Verificação: unit (`speechCut` com predicado/args de áudio/fallback web/failure/origin VM, `webSpeech` mime/título/crédito, VM web com `durationSeconds`, diálogo web-sem-Câmara, `webSpeechDetailPlayer` novo provando gesto/`data-selected`/diálogo), int `speechCut` estendido (corte web publica do espelho sem tocar na Câmara, web sem espelho falha fechado na action e no job, variante de áudio logada pelo fake ffmpeg, proveniência na lista; fixture de bytes extraída para `tests/helpers/webSpeechMediaFixture.ts`, fechando o trigger D2 do C216), e2e (crédito web na página pública, recusa sem espelho na rota, card com origem/detalhe web, detalhe web com o gesto; pins da Câmara verdes). Crítica final do `designer` (trigger c) **certificada em tier primário** (`openai/gpt-5.6-sol`) após três rodadas — aplicados CTA, ativo×selecionado, anatomia da janela, kit do card e a copy do estado sem espelho; o header público global (`SiteHeader`) e o FAB/nav do shell ficam registrados como desvios pré-existentes fora do diff.
