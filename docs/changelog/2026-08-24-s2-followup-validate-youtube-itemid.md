**S2-FOLLOWUP (#775) — validate do itemId (excludedItems do YouTube) deixa de ser código morto:** o `validate` do campo `itemId` em `social-feed-settings` (`src/globals/SocialFeedSettings.ts`) lia `data.platform` — o documento inteiro, sempre `undefined` — então nunca validava ID de vídeo do YouTube em produção (código morto desde a S2). Agora lê `siblingData.platform` (a row do array) via helper puro `validateExcludedItemId` (`src/utilities/socialFeed/excludedItems.ts`), que rejeita IDs fora do padrão `[A-Za-z0-9_-]{11}` com a mensagem pt-BR já existente. Em dupla, os ids do stub YouTube (`tests/e2e/youtube-stub.mjs`) foram trocados de 20 chars para 11 chars exatos (`e2evideo001..004`) e o spec (`tests/e2e/frontend.e2e.spec.ts`) acompanhou (`excludedItems`, hrefs e mensagem de compartilhamento do WhatsApp), destravando o e2e do describe "Campaign home content section" que dependia da mortandade. Sem migration (validate é validação de runtime, não schema). Instagram não é afetado (`platform !== 'youtube'` → `true`). Débito: verificação read-only pré-deploy de `excludedItems` YouTube inválidos já salvos em prod (gatilho = resultado do pré-check no deploy manual; se houver ruído, fallback B licenciado pela intenção é remover o validate) — deferred_trigger, não criado agora.
