**Deploy staging destravado — pnpm assado na imagem do migrator, sem fetch no registry em runtime (2026-09-13):** o primeiro run do job `deploy staging (teqo-staging)` desde a OPS103 (`b03b7bd9`, mergeada 2026-09-12 22:48 -03 / 2026-09-13 01:48 UTC) falhou no deploy manual run 34765554681 (main `67a40a6c`, PR #973 mergeado às 15:24:11 UTC): o `verify` aprovou a suíte completa, mas o staging morreu com `[deploy] FAILED: migrations failed — restoring previous compose and image` (exit 1, 15:52:09 UTC), o `deploy production (teqo-1313)` ficou skipped (`needs: [deploy-staging]`) e o script restaurou o compose anterior — nada publicado, produção intocada, sem outage. No log, o corepack anunciou `! Corepack is about to download https://registry.npmjs.org/pnpm/-/pnpm-10.11.0.tgz` (15:52:05) e falhou (15:52:08) com `Error when performing the request to https://registry.npmjs.org/pnpm/-/pnpm-10.11.0.tgz`, `AggregateError [ETIMEDOUT]` para 104.16.x.x:443 (Cloudflare) e `ENETUNREACH` em IPv6. Causa (provada): a imagem do migrator (`Dockerfile`, stage `AS migrator`, `CMD ["pnpm", "migrate"]`) roda o pnpm via corepack no START do container, mas o cache do corepack populado no build da stage `deps` (`RUN pnpm install --frozen-lockfile`) não é copiado — o `COPY --from=deps` traz só `/app/node_modules` — então o runtime tenta baixar o pnpm do registry; o MESMO download funcionou minutos antes no build (`deps`, 15:46:24) e no container de manutenção de produção às 01:09:36 UTC (`teqo-1313-migrate`, run 34728680411), o que manteve a dependência de egress latente até o staging não ter esse egress (motivo exato não apurado — o compose vive no homeserver, `~/stack/docker-compose.yml`, fora do repo). Fix (sem migration, sem código de app): a stage `AS migrator` ganha `RUN corepack install` (`Dockerfile:28`) após o `COPY . .` — o pnpm 10.11.0 (do `packageManager` no `package.json`) é baixado em BUILD time e o cache do corepack viaja na layer; prova offline local: `docker run --network none <imagem> pnpm --version` imprime `10.11.0` com o bake e, sem o bake, o mesmo run offline falha com o erro exato do staging. Regressão pinada por teste unitário novo em `tests/unit/deployScript.unit.spec.ts:142` ("Dockerfile: the migrator bakes pnpm at build time (no runtime registry fetch)"), RED sem a linha; `pnpm gate:fast` verde; re-dispatch do deploy a registrar. Prevenção: bake + pin implementados agora (PR # a preencher); probe/healthcheck de egress pré-migration ou registry mirror interno ficam documentados como estratégia cara, não implementada. Lição: imagem de deploy que roda package manager no start carrega dependência de rede oculta — o toolchain se assa no build; a produção mascarou porque tinha egress, e o primeiro run real do staging expôs.
