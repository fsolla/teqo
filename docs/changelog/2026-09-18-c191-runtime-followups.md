**C191-runtime — cobertura de runtime da skill `graficos-dados`: entry testável sem Chromium, PNG real nos 3 tamanhos e parse pt-BR explícito (2026-09-18):** fecha os três follow-ups registrados no C191 (#1151), sem mudar o comportamento entregue. (1) **Parse ambíguo pt-BR:** a regra do separador único vira contrato documentado — `1.234`/`1,234` leem 1.234 e `1.500` lê 1,5 (separador único é sempre decimal; para milhar use `1.234,56` ou `1234`) e separador repetido (`1.234.567`) segue recusado; docstring de `parseNumber` corrigida, `SKILL.md` de troubleshooting atualizado e os casos pinados em `chartData.unit.spec.ts` (inclusive `parseInput` sem issue — a regra é intencional, não ambígua para o fluxo). Fail-closed de `1.234` fica com gatilho de revisitação se o uso real mostrar milhar por ponto. (2) **Entry:** `scripts/build-chart-from-data.mjs` ganhou o seam `main({ argv, launchBrowser, screenshot, die, repoRoot })` com guard `isMain` (o `main()` deixava de executar no import e o `die` matava o worker), permitindo `buildChartFromData.unit.spec.ts` cobrir `--inspect`, `needsQuestion`, `--source`/`--in` faltando, `--spec` inválido, replay, montagem do spec e a linha de log por tamanho com fakes — sem Chromium e sem `process.exit`; a CLI real fica idêntica (defaults = imports atuais). (3) **PNG real:** novo smoke `campaignChartPng.e2e.spec.ts` com Chromium real spawna o entry por tamanho (feed `1080×1350`, quadrado `1080×1080`, stories `1080×1920`), valida as dimensões exatas via `sharp.metadata()` e o `<8 MB` via `stat`, e exercita a guarda de `screenshotHtmlPng` com `maxBytes` baixo (só havia guard com browser fake). CI: `scripts/build-chart-from-data.mjs` entra em `SCRIPTS_SPEC_PINNED` (diff do entry vira high-risk) e o smoke entra na `E2E_CURATED_SPECS` + pin congelado, pois o manifest de e2e só mapeia `src/**`. Sem schema/migration/DB/PII; nada commitado em `data/`/`docs/research/`. Verificação: `pnpm test:unit` (3827+), smoke e2e local 4/4 (15,6s) e `pnpm gate:fast`.
