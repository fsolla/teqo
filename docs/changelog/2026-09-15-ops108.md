**OPS108 — Modo autônomo por flag nas skills plan-issue e work-issue (2026-09-15):** as duas skills ganham opt-out da pausa por invocação via flag única `--auto` (dentro do prompt, ex. `/work-issue --issue 1018 --auto`); sem a flag, os GATEs atuais valem idênticos. Com a flag, `plan-issue` registra sem confirmação (divergência material → item fora do lote) e `work-issue` marca o impl plan como aprovado e executa até o PR (divergência → `blocked`, nunca claima). A flag nunca atravessa os invariantes duros (Consent/LGPD fail-closed, migração de schema, URL público, shapes públicos, produção, CI green). Os dois comandos viram a flag numa linha de uso sem ganhar lógica (guard OPS25 intacto); o texto desambigua do `--auto` do CLI opencode. Testes: novo `skillsAutoFlag.unit.spec.ts` (8 testes pinando flag, fail-safe, lista dura e contrato thin-wrapper); suite unit 3075 verde, lint/typecheck/format limpos.
