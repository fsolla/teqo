**C177 — "Abrir fonte" do acervo só quando há fonte oficial (2026-09-16):** o botão de fonte do acervo de falas (`/campanha/comunicacao/acervo`) derivava `sourceUrl: officialTextUrl ?? youtubeUrl` e, sem o Diário oficial, abria o vídeo cru no YouTube sem o ponto — atalho pior e duplicado do "Abrir no YouTube" timestampado (C154/C178). O fallback morre: o `SpeechListItemViewModel` expõe `officialTextUrl` (nunca `youtubeUrl`) e o `SpeechDetailPlayer` só renderiza o botão quando `officialTextUrl` existe, agora rotulado **"Abrir Diário Oficial"** — lista e detalhe com o mesmo critério e o mesmo rótulo; o `page.tsx` deixa de derivar. O aviso "sem YouTube" passa a prometer só as saídas realmente renderizadas (MP4 e/ou Diário). C160 preservado (PDF direto quando há Diário); "Abrir no YouTube" intocado; sem schema/migration/Consent/VOD/URL pública. Testes: unit do view model (caso negativo YouTube-only → `officialTextUrl` nulo) e do player (rótulo, ausência e a frase do aviso nos quatro casos) + e2e de fala sem fonte na lista e no detalhe. Issue #1087.
