**C154 — Vertical Comunicação: papel communicator + Acervo pesquisável (2026-09-13):** o assessor de comunicação ganhou a vertical própria. `communicator` vê e navega só `/campanha/comunicacao` (nav própria; sem áreas de staff, sem Sollinha — o débito do C153 dos chips de líder foi resolvido escondendo as superfícies do assistente para esse papel via `canUseCampaignAssistant`); `coordinator`/`candidate` também veem; `advisor`/`leader` são redirecionados pelo gate novo `speechCatalog` do `requireCampaignPageActor`. O Acervo (`/campanha/comunicacao/acervo`) busca por termo com e sem acento sobre os segmentos ASR (destaque `<mark>` no trecho) e keywords oficiais como termo exato, com filtros de ano, tema, alcance, fase, município citado e duração em URL canônica (`resolveListUrl`/`CampaignListOmnibox`/`CampaignHeaderFilterPopover` com trigger em chip opt-in) e paginação **por discurso**: `speech.searchText` (concatenação normalizada dos segmentos, mantida pelo `upsertSpeechBundle`, coluna opcional + índice GIN `pg_trgm` hand-written com backfill em `20260913_150000_backfill_speech_search_text_trgm_index`) resolve o "N falas encontradas" sem dedupe entre páginas. O detalhe (`/campanha/comunicacao/acervo/[id]`) tem player `<video>` nativo que abre no trecho (`?t=`), transcrição ASR clicável com destaque, sumário/contexto da sessão, baixar VOD e abrir fonte, com fallback "VOD indisponível" e transcrição oficial em `<details>`. Highlight puro em `src/lib/speechHighlight.ts` (mapa offset normalizado→original), loader `speechPageData`, view models, omnibox e módulos de URL/filtros em `src/utilities/speech/`; parser `parseOfficialKeywords` do C153 corrigido no dono (a API separa keywords por vírgula dentro de grupos por linha — o card exibia um chip gigante). Provas: 20 unit novos + int `speechAcervo` (busca sem acento, keyword, facetas, paginação por discurso, RBAC) + extensão do int do import (`searchText` espelhado e preservado em `segments: undefined`); e2e `campaignSpeechAcervo` (6 cenários HTTP: nav/redirects, highlight, filtros, RBAC, VOD ausente) com entry no manifest; prova viva local com import real (3 discursos, 43 segmentos ASR, busca "saude" e detalhe no browser). Migration `20260913_145820_add_speech_search_text` + backfill/índice.
