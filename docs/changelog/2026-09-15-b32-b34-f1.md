**B32-B34-F1 — UI pós-verify: clamp podia esconder TODOS os chips e warning de key no head (2026-09-15):** dois débitos que ficaram fora do escopo da #1036, ambos no dono compartilhado. **F1 — `RelationChipCell`:** a medição de overflow (`useLayoutEffect`, `RelationChipCell.tsx`) começava com `fitting ≥ 1` e o loop `while (fitting > 0 && fitting < chipElements.length)` podia descer até **0** quando os chips largos preenchem todas as linhas numa coluna estreita e o espaço reservado do toggle + input de busca (128px do `min-w-32`) não cabe ao lado de nenhum chip de cauda — a célula renderizava **sem chip nenhum**, só "Ver mais…" (snapshot de falha do e2e B34; o `expandCollapsedChips()` era contorno). Fix: piso de um no próprio guard (`fitting > 1`) — o toggle e o input passam a quebrar para a linha seguinte (a linha é `flex-wrap`, pós-medição não há clamp), nunca zero chips. Pin unitário novo em `relationChipCellCollapse.unit.spec.tsx` ativando o ramo de `min-width` que o stub antigo pulava (INPUT width 0): com o loop antigo o stub estreito dá 0 chips (RED verificado), com o piso dá 1 e expande/recolhe; os 4 pins do contrato de 3 linhas seguem verdes. **F2 — `CampaignSortableHead`:** o `filter` criado por componente **server** (`LeadershipSortableHead`, sem `'use client'` desde 7a450bc8) era filho keyless do array estático `{labeledSort}{filter}`, e o React dev acusava `unique key` a cada render de `/campanha/liderancas` — o guard de `console.error` do e2e derrubava todo run em dev local (por isso a #1036 precisou de `E2E_PROD=1`). Fix no dono compartilhado: Fragments com `key` nos dois slots (cobre também o mesmo padrão latente em `StateDeputySortableHead`), sem nó novo no DOM. Prova: `campaignLeaderships` em **dev local sem `E2E_PROD=1`** 2/2 (RED comprovado revertendo o head: o warning exato derruba o guard) e mesma superfície `campaignPeople`+`campaignMunicipalities` 34/35 (a única falha, C131 `Todas as zonas`, reproduz na árvore limpa — pré-existente, dona #882); `gate:fast` verde (lint + typecheck + 3350 unit). Defers com gatilho no plano (exemption de `campaign/shared` no manifesto e2e; corrida de DDL do allocator sob workers paralelos). Plano: `docs/plans/b32-b34-f1-ui-pos-verify-impl.md`.
