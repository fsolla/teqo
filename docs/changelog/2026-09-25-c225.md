**C225 — operação de ingestão das falas da internet no acervo de produção (2026-09-25):** a escrita não-local de `pnpm falas-web:import` passou a recusar, antes de abrir o Payload, a ausência de mídia espelhada: o guard reutiliza a decisão de alvo de `scripts/lib/cli.mjs` e exige as quatro variáveis `S3_*` quando o alvo exige confirmação; `--dry-run` e o caminho local seguem inalterados. O runbook `docs/ops/teqo-1313-deploy.md` agora documenta a rodada manual no homeserver: workspace separado, estado durável fora do clone, cookies revogáveis e wrapper de `yt-dlp`, reescritas de Postgres/Garage, preflight, smoke limitado, lote, idempotência, inventário, honestidade de falhas e rollback pelo Payload. A curadoria C218 passou a exigir `externalId` para YouTube/Instagram e conferência do `sourceKey` no dry-run. Esta entrega não executou escrita em produção, não adicionou migration/UI/agendamento e o resultado da primeira rodada permanece para o operador registrar.
