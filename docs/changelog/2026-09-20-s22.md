**S22 — Jingles e rádio na home: a seção de som (2026-09-20):** o som do mandato chega à vitrine — a home pública ganha, entre "Nossa história" e "Faça parte", a seção "A trilha da nossa caminhada" (portada classe-a-classe do artefato aprovado `docs/plans/jingles-radio-homepage-ui-design.html`, Cenas 01–07) com a **Rádio Jorge Solla 1313** e até **3 jingles publicados** do S21, sem rota nova e sem tocar `/jingles` nem o rodapé. A rádio é um `RadioFacade` click-to-load: no estado `facade` não existe iframe, script ou request de terceiro — só o botão "Ouvir a rádio" e o link "Abrir no Zeno" —; o clique monta o widget oficial do zeno.fm e o card passa por `loading` (`role="status"` + `aria-busy` + spinner, com o foco do teclado movido para a região) até `loaded` no `onLoad`, com o **mesmo footprint reservado nos três estados** (330px mobile / 312px desktop, medido 522=522 e 314=314 no app real) — zero CLS, e o iframe nunca remonta (sem refetch). Os jingles reusam o core do S21, **extraído behavior-preserving** de `JinglePlayer` para `JingleCards` (exclusividade, `preload="none"`, download pelo slug e contrato `article[data-jingle]`/`data-state` intactos, com `headingLevel` para o outline da home e o anel de foco único em `focusRing.ts`): grade `md:grid-cols-3`, até 3 exibidos e "Ver todos os jingles" → `/jingles` **somente** quando há mais de 3; com zero publicados a grade e o link somem e a rádio permanece (fail-closed, mesmo kill switch do S21) na variante compacta da Cena 07 (`max-w-[880px]`, coluna de 240px, copy própria). A home segue estática: uma leitura `getPublishedJingleItems()` (cache tag `jingles`) alimenta a seção e o flag do rodapé (`slice(0,3)` em memória; nenhum `payload.find` novo). e2e: `frontendJingles` ganhou a seção da home (facade inerte com zero request `zeno.fm` **e** zero request de MP3 antes do gesto, widget de teste via `page.route`, `data-radio-state` até `loaded`, 4 publicados → 3 cards + handoff, kill switch → só rádio) e o `frontend` pinou a ordem (`sound = story+1`, `cards = sound+1`) e o overflow 390; unit: `radioFacade` (facade sem iframe → clique monta → `onLoad` não remonta) e `jingleHomeSection` (0/1/3/>3, "Ver todos", contrato da grade). Sem migration (nenhum schema; leitura pública sem PII/Consent). Crítica final do `designer` **CERTIFICADO** (tier `openai/gpt-5.6-sol`) contra o app renderizado em 390/1280 nos estados facade, carregando, carregado e zero — após uma primeira passada com 6 ajustes (escala do título, facade mobile da Cena 02, arte/wave, CLS, variante zero, header mobile), todos aplicados. `pnpm gate:fast` verde (4224 unit), e2e local selecionado verde (`frontendJingles` 4/4, `frontend` 2/2), simplify com 2 revisores (11 achados: 9 resolvidos no lugar, 2 descartados com respaldo; nenhum débito novo).
