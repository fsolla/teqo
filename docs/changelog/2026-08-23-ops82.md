**OPS82 — default de launch do worktree opencode → DeepSeek V4 Flash 0731 (mais barato) + variantes expostas + limpeza duplicatas Cheapest Inference (2026-08-23):** continuação da OPS78: o launch de `pnpm worktree next/plan/new` passou do preset `vercel/deepseek/deepseek-v4-flash` (input $0.13/M) para **`vercel/deepseek/deepseek-v4-flash-0731`** (input $0.076/M — mesma família, razão forte, ~41% mais barato). Mudança config-only (mecanismo OPS26 "trocar modelo = editar a constante"): `OPENCODE_PRESET_MODEL` em `scripts/lib/worktree.mjs` → `deepseek-v4-flash-0731`; o repo `opencode.json` ganhou o `provider.vercel.models["deepseek/deepseek-v4-flash-0731"].variants` com low/high/max (espelha o sibling OPS78) — o catálogo do opencode traz esse model key com `reasoning_options` vazio, então o override é o que torna as variantes selecionáveis no TUI. No mesmo repo `opencode.json`, **removido o bloco `provider.cheapest-inference` (com hífen) inteiro** — duplicata do global `~/.config/opencode/opencode.jsonc`, que já define `cheapestinception` (sem hífen) com os mesmos `deepseek-v4-flash` + `mimo-v2.5` **e** com variantes low/high/max; agora o TUI mostra **um único** DeepSeek e **um único** MiMo no Cheapest Inference. 8 literais + pin da constante em `tests/unit/worktree.unit.spec.ts` atualizados; textos em `scripts/worktree.mjs` (docblock/help — o help agora interpola `OPENCODE_PRESET_MODEL`, importado), `.agents/shell/worktree.sh` e `worktree-next-issue/SKILL.md` sincronizados; o help deixou de duplicar o literal. Anti-goals preservados: cardápio de execução de Issues intocado (`work-issue.md`/`plan-issue.md` + pin `opencodeCommands.unit.spec.ts` seguem com o provider direto), config global do humano intocada, docs históricos congelados, usos diretos `deepSeek('deepseek-v4-flash')` no app (Sollinha/`campaignDemandTitle.ts`) intactos. Verificação: `pnpm gate:fast` verde; `opencode debug config` confirma variantes do 0731 ativas e `cheapest-inference` do repo ausente; PR em main com auto-merge.
