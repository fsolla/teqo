feat(OPS109): painel de issues e planos no terminal (via SSH)

TUI read-only (`pnpm issues:tui`) que lista todas as issues rastreáveis com estado, prioridade, kind e ação pendente; filtra por estado (em andamento / aguardando / concluídas) sem sair do painel; abre o detalhe de uma issue com o plano de intenção e o de implementação — cada um com status legível (`aprovado` vs `aguardando aprovação`) e link; renderiza o markdown do plano no terminal (títulos, listas, tabelas, código, negrito, citação) com rolagem; abre o rascunho UI no browser da máquina com fallback para impressão do caminho em SSH sem display; e anexa à sessão do agente via `agent:session attach` (OPS110) — sem nunca iniciar um run novo. Modo não-interativo (`pnpm issues:tui --json`) expõe o view model versionado para teste/script. Sem migration, sem deps novas, sem alterações em collections. `knip.json` limpo — zero exports mortos.

Related #1020
