**C174 — Acervo: encontrar cortes na navegação, na fala e na busca (2026-09-16):** a assessoria passa a reencontrar um corte pelos três caminhos, sem recortar de novo. (1) **Navegação:** `nav.ts` ganha `subItems` aditivo no item "Comunicação" — "Acervo de falas" e "Biblioteca de cortes" — renderizado na sidebar (`SidebarMenuSub*`, filho ativo com cartão branco + ring) e no drawer "Mais" do mobile; era a causa do "não encontrei a edição" (o C168 só tinha o CTA dentro do acervo). (2) **Na fala:** nova seção **"Cortes desta fala"** no detalhe (`loadSpeechCutsForSpeech`, `speechCut` por `speech` index, depth 0, mais recentes primeiro), com tabela no desktop e cards no mobile, badges de status do C168 e vazio honesto ("Nenhum corte desta fala ainda" + "Selecione um trecho no player para criar o primeiro corte." + CTA "Selecionar trecho"). (3) **Na busca:** `loadSpeechCutsForSpeeches` pré-carrega os cortes dos ids da página e o `SpeechResultCard` aninha "N corte(s) desta fala"; a opção B assunida também entra — `loadSpeechCutOriginSpeechIds` OR-eia no `where` textual as falas de origem de cortes que casam por título/descrição, então a fala aparece como "Fala de origem" com a nota honesta "A fala não contém o termo exato…" e "Correspondência no corte". A ponte CTA↔player é um evento nomeado (`SPEECH_EXCERPT_REQUEST_EVENT`): o CTA só anuncia a intenção, o `SpeechDetailPlayer` (dono do estado) liga o picker sem duplicar a seleção. Sem collection, Consent ou migration; gate `speechCatalog` e lockdown advisor/leader intocados (o gate de página segue fail-closed; a leitura nova usa `user` + `overrideAccess:false`); `/corte/<id>` público intocado. Testes: units de nav/chrome/`buildSpeechListWhereIncludingCutOrigins`/CTA; int do loader por fala, do batch, do acesso por papel e da origem opção B; e2e `campaignSpeechCut` cobre a seção, o aninhamento e a origem.
