**C186/C187 — Dossiê corrente (sem caps) + redação/síntese/gráficos, com paridade cidade × instituição (2026-09-18):** revisão de produto dos dois dossiês Solla a partir do uso real (UFBA). (1) **Fim dos caps**: eras, abrangência/região, lacunas, notícias e acervo saem completos — cada seção flui por quantas folhas precisar, com o builder medindo a altura real de cada linha (`scripts/lib/dossiePack.mjs`: probe → pack → **grow/shrink medido** por folha até estabilizar; `scripts/lib/buildPdf.mjs` ganhou `measureDocumentPackProbe`/`measureDocumentSheets`), quebra só entre linhas e guarda de A4 no emit; as folhas de Era C foram de ~41–63% para 82–90% de ocupação e nenhuma página fica pela metade. (2) **Redação de abertura** (nova página "A contribuição"): sub-agente redator (`.opencode/agent/dossie-solla-{cidade,instituicao}-redacao.md`) escreve `<slug>.narrative.json` (título + 3 parágrafos + 1 por era) **somente** dos itens com fonte, com o orquestrador auditando citação por citação (um nome inventado foi pego e removido no primeiro uso); sem o arquivo, o builder cai na consolidação determinística. (3) **Parágrafo de consolidação por era** ("O que esta era entrega") do `narrative.json` ou determinístico. (4) **Síntese e gráficos consolidados** (novas páginas): leitura dos números com lastro (pontos por era/esfera, concentração, fases) e SVG print-safe de **recursos com execução por ano** (empilhado por fase), propostas/articulações sem fase, abrangência, trajetória, áreas, lacunas e painel do acervo — valores em R$ sempre com a fase e recortes nunca somados; o acervo é **amostra declarada** (N de M do recorte) com painel do total. (5) **Paridade**: o dono (`dossieBlocks`/`dossieRender`/`dossieUnit`) foi generalizado (síntese, páginas carta/síntese/gráficos, engine de eras com honrarias, folhas de lacunas/notícias/acervo e copy por unidade) e o ramo cidade adotou as mesmas decisões (23 páginas no smoke de Ilhéus); a cidade mantém arrays e a instituição os objetos capados, ambos normalizados por `listOf`. (6) Skills/comandos/agentes e specs atualizados (`dossieBatchSkill`/`dossieInstitutionBatchSkill` pinam "não usa caps", grow/shrink, contrato do `narrative.json`, recibos e os agentes redatores), designs hi-fi re-sincronizados pelo `designer` (14 cenas instituição, 16 cidade). Sem schema/migration/DB write; artefatos gitignored. `pnpm gate:fast` verde (354 arquivos / 3687 testes) + prettier/knip limpos.
