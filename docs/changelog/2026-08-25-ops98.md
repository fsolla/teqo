**OPS98 (#903) — skill `engineering-audit` v2 (autônoma, sub-agentes, PR único sem auto-merge):** a skill virou o padrão do repo (autônomo-first: 105 linhas, era 182; método intacto — smells/triagem P0–P3/passo 4b movidos verbatim para `reference/` com `canon|smells|consolidation|guards.md`, carga do canon antes de julgar continua obrigatória). Contrato de entrega novo: Pass inteiro na branch `audit/pass-<N>` → melhorias P0/P1+SM planejadas por escritores `-impl` e executadas por implementadores em série (teto duro de 6; migrações/URL pública/Consent-LGPD fora por construção) → **UM PR Ready base `main` SEM auto-merge** com relatório completo na descrição e loop até CI green + mergeable; modo solitário/precheck do agent-pool reduzido a nota histórica (OPS65). Safety-net: `decideAutomergeAction` ganha veto dominante `audit/*` (`audit-veto`, espelha `cursor/*`) com pins unitários novos — nenhum PR não-`audit/*` muda de veredicto; `.agents/rules/agent-pr-workflow.mdc` documenta o único opt-out restrito por prefixo.
