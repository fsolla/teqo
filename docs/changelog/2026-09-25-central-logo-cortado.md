**Central de Conteúdos pública — logo do header sem corte (2026-09-25):** o S36 (`772a5cf0`) trocou o `Image` direto `h-7 sm:h-9` do header da Central por uma janela `overflow-hidden` de recorte, mas o artefato de design trazia os números errados (188×50 desktop / 142×39 mobile, img 246px/186px, offsets −29/−38 e −22/−29) e a implementação os portou fielmente: a janela tinha razão 188/50 ≈ 3,76 (mobile 3,64) contra a razão da tinta real do PNG oficial — canvas 1037×595, alpha bbox 790×285 em (123, 162), razão ≈ 2,77 — então a tinta renderizada (187,4×67,6 no desktop; 141,7×51,1 no mobile) ficava mais alta que a janela e o `overflow-hidden` decapitava a base de "SOLLA" (18px/12,2px cortados; reproduzido read-only em produção nos dois breakpoints, nas três superfícies do header, catálogo/peça/não-encontrado). O dono único `src/components/conteudos/ContentPiecePageHeader.tsx` passa a derivar a janela da caixa da tinta no peso da referência `/jingles` — mobile `h-[46px] w-[128px]` (img 167px, offsets −19,8/−26,1) e desktop `sm:h-[52px] sm:w-[144px]` (img 189px, offsets −22,4/−29,5) — com o comentário reescrito (invariante + receita de recálculo via `sharp trim`); `CampaignPageHeader` (`/jingles`) intocado, sem migration/access/Consent. Regressão e2e nova em `frontendConteudos` ("keeps the whole brand ink inside the header crop, both breakpoints (S36)") mede o alpha bbox real do PNG com sharp e asserta a tinta renderizada dentro da janela em 1280 e 390 — RED provado sem o fix (`ink bottom clipped @1280px`, esperado ≤57,5, recebido 75,04) e verde com; e2e `frontendConteudos` 12/12, `frontendJingles` 5/5, `pnpm gate:fast` verde (unit 4685 em 423 arquivos), `format:check`/`knip`/`check:cycles` verdes; nota de prevenção no `public/campaign-kit/README.md` §"Lockups do nome"; CI e prod pendentes (nunca declarar corrigido antes do approve humano). Post-mortem: `docs/postmortems/2026-09-25-central-logo-cortado.md`.
