**OPS116 — Painel/TUI: aceitar `-ui-design.html` com retrocompat `-ui-draft.html` (2026-09-16):** o painel de issues (`pnpm issues:tui`) volta a enxergar o artefato de design dos planos depois do rename do OPS114. `siblingPlanPaths` (lib pura) passa a derivar `-ui-design.html` (nome vigente) e `-ui-draft.html` (legado), e o novo `resolveUiDraft(planPath, exists)` — puro, existência injetada pelo CLI — é dono da precedência "design vence, draft é fallback, nenhum → design com `exists:false`". O `readPlans` do CLI sonda design→draft no disco e devolve o **caminho real**, que flui por `plansByNumber`/`buildIssueRow` até o view model; o contrato `--json` segue `version: 1` com o shape `{ path, exists }` e a chave histórica `uiDraft` (nenhum campo novo). As superfícies do painel trocam "rascunho UI" por "design UI" (ajuda, linha de ação, rodapé e mensagem da tecla `w`). Retrocompat é só de leitura — o acervo legado (30 `*-ui-draft.html` + PNGs) não é tocado; HTML-only, sem render de imagem. Unit dos dois specs do painel cobre design-only, draft-only, ambos (design vence), nenhum, o caminho real vindo do contexto e o shape do JSON. Sem migration, sem deps novas, sem tocar `src/`.
