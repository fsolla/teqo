**Agenda do link de anúncio (S41, Issue #1335):** os eventos gerados pelo Google Agenda e pelo `.ics` agora carregam a URL canônica do anúncio na descrição, acrescentam uma única assinatura ` - Jorge Solla 1313` e usam `America/Bahia` (UTC−3) nos dois caminhos; o `.ics` também emite `TZID`, `VTIMEZONE` e `URL`. A resolução da URL canônica é server-only e a serialização rejeita URLs não absolutas/controles para preservar o fail-closed. Página, menu e cadastro permanecem inalterados; testes unit, int e o e2e de share links cobrem o novo conteúdo e os guardrails existentes.
