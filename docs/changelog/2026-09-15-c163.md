**C163 — Relatório de cidade pré-viagem (agente + skill → PDF A4) (2026-09-15):** documento por município para o candidato/CG antes da viagem — **PDF A4 datado + companion `.md`** gerados sob demanda, cruzando a base Teqo lida **direto da produção, read-only** (snapshot do momento) com pesquisa web datada (URL + data por item) e emendas lidas da fonte oficial **em tempo de geração, sem persistir nada**. Página 1 = resumo de uma olhada (identificação/prioridade/classe/nível, conta eleitoral 2022 relativa — votos, % do próprio voto, rank/435, meta e cobertura de pledges —, quem é quem, o que Solla entregou com fase das emendas, anunciar × não anunciar com o defeso, riscos); páginas 2+ = aprofundamento em 10 seções (série 2014/2018/2022, rede/lideranças com frescor, conjuntura, sinais, demandas/visitas, demografia, acervo de falas, notícias, panorama regional da TI, fontes e limites). Guardrails: "sem fonte, não publica" (item sem URL/data vira lacuna explícita), empenho ≠ pagamento, leitura relativa, PII de contato fora, PDF completo para o candidato (estimativas e nível N0–N4 entram). Entrega: `scripts/extract-city-report-snapshot.mjs` (Payload Local API no homeserver com `default_transaction_read_only=on` na connection string + guard `CITY_REPORT_CONFIRM=1`; mesmos loaders do E16 — `loadMunicipalityDossierData`, detail VM, rank do artefato TSE, acervo por `buildSpeechListWhere`, demandas por município), `scripts/build-city-report.mjs` (blocos puros → HTML+MD do mesmo conteúdo, `page.pdf` do Chromium já instalado, guarda de página 1), composer de integração em `scripts/cityReportSnapshot.mjs` e libs puras em `scripts/lib/cityReport{Format,Research,Territory,Blocks,Render,Database}.mjs` + `portalTransparenciaEmendas.mjs` (Portal da Transparência com fallback lacuna), skill `.agents/skills/relatorio-cidade/` + subagente `.opencode/agent/relatorio-cidade.md`, runbook em `docs/ops/teqo-1313-deploy.md` §C163. Artefatos (PDF/MD/snapshot/HTML) **gitignored** — o repo é público e o relatório carrega dado interno de campanha. Testes: 39 unit (formatters, checklist de pesquisa, normalização de emendas, blocos, renderers, URL read-only) + 1 int (composição do snapshot com fixtures, provando PII fora e totais). Issue #1007.
