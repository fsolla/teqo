**Deploy destravado — override sharp@0.32.6 → 0.34.2: o pnpm install deixa de depender de github releases (2026-08-27):** os 4 deploys falhados desde 10:59 UTC morriam na camada `deps` do Dockerfile: o `postinstall` do `sharp@0.32.6` (dependência do `@imgly/background-removal-node`, usado só pelo script local `resize-images.mjs`) baixa o libvips de `objects.githubusercontent.com` e estoura timeout no homeserver (`sharp: Installation error: Request timed out`) — o egress não conclui downloads de github releases embora o `git fetch` funcione (hostname distinto da CDN); a cache do BuildKit não esconde a classe porque o `builder prune` do pós-deploy limpa a camada a cada deploy bem-sucedido. Fix estrutural (PR #929, reverted em outro eixo o #927 que tentou a rota da cache): override no `pnpm-workspace.yaml` força o sharp transitivo para 0.34.2 (versão direta do app) — a partir do 0.33 os binários vêm de pacotes do **npm registry** (`@img/sharp-libvips-*`), alcançável pelo homeserver; lockfile regenerado remove o sharp@0.32.6 e toda a subárvore do prebuild-install (−296 linhas, tar-fs/simple-get/bare-\*/etc.). Verificação: `pnpm why sharp` (imgly → 0.34.2 via override; next/payload → 0.34.5), smoke do `resize-images` (processamento webp OK), format/lint/tsc verdes, suíte int completa verde (78/712), gate:ci verde, PR CI verde. **Deploy do main (`4d6533c0`, run 33074305879) verde: verify 19m + deploy 16m — e o admin voltou a renderizar em produção** (login do Payload com formulário, 0 erros de console; verificação read-only via browser, confirmação final do humano pendente). Candidato a registro no runbook: a regra "nenhum binário de github releases no `pnpm install` de produção" — qualquer dep futura que baixe de github.com/objects.githubusercontent.com na instalação volta a travar o deploy.
