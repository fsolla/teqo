**B203 — Sollinha começa fechado no desktop (2026-09-16):** sessão nova em `/campanha` no desktop abre com o painel fechado e o conteúdo em largura cheia; o efeito de settle que forçava `setOpen(true)` foi removido do `CampaignAISidebarProvider` (a prop `panelRef`, morta com ele, saiu da assinatura) e o `open` só sai de `false` via restore de sessão ou ação do usuário (FAB/botão do header, que seguem abrindo como hoje com `openBy: 'user'`). Largura B166/B167 (`25%`/cap 360px/resize lembrado), persistência B199 e mobile (drawer fechado por padrão) intocados; `sollinhaChatSession.ts` sem mudança (sessões `openBy: 'settle'` legadas continuam lidas). E2Es que pinavam o auto-open ganharam abertura explícita pelo header (incl. rewrite do teste OPS22 para o default-fechado e assertion de sessão não-vácua); `restoredSessionRef` write-only removido no simplify. Sem migration. Issue #1107.
