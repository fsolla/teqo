**C197 — narração rascunho (TTS pt-BR) + transcrição sempre no pacote do reel (2026-09-19):** o motor de reels do C196 passou a fechar o contrato da biblioteca privada: **todo** pacote sai com `narracao.srt` (um bloco por beat, timecodes das cenas) + `roteiro.md` (fala por cena, para a assessoria regravar) e o `metadata.json` ganhou `coverAlt` (obrigatório no ingest C195; fallback do `title`), `durationSeconds`, `voice` e a lista `artifacts` — antes o pacote não era ingerível (faltavam transcrição e alt). O gate da skill pergunta **com ou sem áudio** (padrão sem) e `--audio` gera o rascunho narrado: `edge-tts` (TTS neural pt-BR, grátis e sem key) em venv dedicado (`pnpm reels:tts:setup`, `EDGE_TTS_PATH` → `scripts/reels/.venv` → PATH, fail-closed antes da captura), síntese por beat com o texto passado por arquivo (nunca argv), medição exata por PCM decodificado (sem `ffprobe`), `atempo` ≤1.5× + padding de silêncio para a fala caber na janela da cena (estouro falha com a cena e a duração) e mux `-c:v copy` → `narracao.mp3` + `reel-audio.mp4` com a trilha de vídeo idêntica. A narração é o roteiro aprovado no shot list (campo `narration`, fallback na `caption` com espaços autorais preservados); sem clonagem de voz, sem música, sem publicação. Hash do `cards` muda (nova versão C197). Unit novos: `reelScript` (SRT/roteiro/fit/PCM) e `reelTts` (resolução/setup/síntese/trilha), mais probe de áudio (libmp3lame + atempo) e args de decode/encode/mux em `reelFfmpeg`; verificação real com `pnpm reels:build cards [--audio]` + `pnpm reels:ingest` (dry) aceitando o pacote. Sem schema/migration; TTS fora do deploy npm (`--frozen-lockfile` intacto).
