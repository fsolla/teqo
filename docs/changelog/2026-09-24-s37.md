**S37 — Central de Conteúdos: quem aparece na peça e a faceta pública "Lideranças" (2026-09-24):** a ficha C211 ganha, entre Temas e Cidade, o bloco "Quem aparece na peça" com os dois recortes: `leaders` (relationship hasMany → `leadership`) com snapshot readOnly `leaderNames` (só o nome de exibição, derivado no `beforeValidate`) e `publicFigures` (text hasMany, texto curado canonicalizado contra o catálogo novo `src/lib/publicFigureCatalog.ts`, derivado do roster S30 das 53 dobradinhas + personalidades por texto livre). A busca de liderança do picker é um server action gated (`canReadCommunicationCatalog`) com projeção fixa `{id, label}` — único bypass intencional documentado (o RBAC atual nega o `communicator`, que é quem edita a ficha) e os int tests asseguram o shape. O catálogo público (S27) ganha a faceta única `Lideranças` unindo os dois recortes numa opção por nome de exibição (`slugify`), derivada SÓ de peças publicadas (fail-closed: despublicar remove o nome na hora); o nome entra no `searchText` (busca por termo) e lidera o `metaLabel` do card, com título contextual e chip ativo ao lado no desktop. Migration aditiva `20260924_123940_add_content_piece_people` (tabelas `content_piece_texts`/`content_piece_rels`, sem backfill, sem `ALTER TYPE`; `content_piece_curated_fields` intocado — o job nunca escreve os campos novos). Zero Consent/collection novos; o read público nunca seleciona `leaders` (depth 1 vazaria Contact). Verificação: `pnpm gate:fast` verde (lint + typecheck + unit) e `pnpm test:int` full verde (941 passed, 2 skipped); e2e local `frontendConteudos` 8/8 e `campaignSpeechAcervo` (bloco C211) 31/31; design certificado PARIDADE pelo `designer` (crítica final contra o app renderizado, cenas 01/01A/02/03).
