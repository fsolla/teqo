**C88 — deliberação na atualização (responsável, fio, resolvido) (2026-08-23):** a coordenação agora delibera na própria atualização de município, sem abrir demanda/atividade/Zap paralelo para cada fato (depende do modelo unificado do C87). O card da atualização ganhou três camadas: (1) **responsável** — coordinator/candidate atribuem um `campaignUser` staff do município (assessores da carteira + coordinator/candidate; nunca liderança; reatribuir/limpar livres), salvo server-side com validação de elegibilidade e lock por doc; (2) **fio de comentários** — qualquer staff que pode criar atualização naquele município comenta (array `comments` no doc, precedente `Activity.updates`, hook carimba autor/data), ordem cronológica, composer some quando resolvido; (3) **Resolvido/Reabrir** — coordinator/candidate marcam (audita `resolvedBy`+`resolvedAt`) e reabrem; atualização resolvida permanece legível com badge "Resolvido" + corpo atenuado, e "Em deliberação" aparece quando há fio/responsável. Segurança: canal de escrita novo via `context.mutationKind` (`assignResponsible|appendComment|resolve|reopen`) com split `canUpdateMunicipalityUpdate`/`canDeleteMunicipalityUpdate` (delete segue admin-only), allowlist de campos no `beforeChange` por diff contra o doc original (403 fora dela), zero `overrideAccess: true` novo, leader barrado na raiz (read escopado + ações com `overrideAccess: false`). Superfícies: tab Atualizações do município, feed global `/campanha/atualizacoes` e dossiê compartilham a mesma ilha client com capabilities server-side. Migration `20260823_161905_add_municipality_update_deliberation` (colunas `responsible_id`/`resolved_by_id`/`resolved_at` + tabela `municipality_update_comments`, serializada após `20260820_003426`; o snapshot .json gerado saneia o snapshot stale da cadeia). Corrigido também o alias `canMutateAllocationDecision` que herdava o gate de update do MunicipalityUpdate (agora explícito admin-only — o gate deliberativo jamais alcança `allocationDecision`). Verificação: unit (2423) + int (826, incl. spec novo da deliberação com 11 casos) + build + e2e novos (`campaignMunicipalityDeliberation`, 2 fluxos) e afetados verdes; um e2e pré-existente (B27, gate `writable` do C142) falha também no main limpo — não é regressão desta entrega.
