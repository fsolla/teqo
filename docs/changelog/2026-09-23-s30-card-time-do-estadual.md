**S30 — Card "Time do estadual": quinto modelo do funil público, com as 53 dobradinhas (2026-09-23):** o estúdio de cards (`#cards` da home + `/cards` + `?model=time-do-estadual`) ganha o quinto modelo no MESMO composer: o visitante escolhe um dos 53 estaduais, digita como é chamado e envia a foto de busto; o recorte roda no aparelho (esteira do S15 inteira: envio, progresso/retry, harmonização, pan/zoom) e o card final é `TIME DE <nome>` com a arte oficial do escolhido, na ordem medida FOTOS do estadual → recorte na janela `{286,439,592,577}` → BASE do estadual → banners (`TEAM_CARD_LABEL` + banner azul dinâmico do S16). Catálogo estático derivado dos lockups da arte (`src/lib/stateDeputyCatalog.ts`: slug, nome de exibição, número de 5 dígitos e os dois caminhos de asset; 53 entradas imutáveis com snapshot `tests/fixtures/state-deputy-catalog.snapshot.json` + sha256 e busca por nome sem acento ou pela pasta entregue), com os 106 derivativos WebP otimizados (~6,8MB, q82) commitados em `public/cards/estaduais/<slug>-{fotos,base}.webp` — re-deriváveis por `pnpm build:state-deputy-card-assets -- --from <origem>` (sharp, falha fechado em par ausente/duplicado/dimensão fora de 1080×1440) — e o jpeg exato aprovado pelo humano copiado byte-a-byte para o tile/placeholder (`/cards/modelo-time-de-voce-com-estadual.jpeg`). Seletor inline portado do gate (trigger com nome+número+"Trocar", painel aberto por padrão sem escolha, busca, lista rolável `role="listbox"`, ↑↓/Enter, seleção por linha inteira com check); pré-escolha mostra a arte de exemplo, pós-escolha sem foto desenha a silhueta do visitante (`#001a42`, geometria do design normalizada na janela) e trocar de estadual não perde o nome nem o recorte (par de arte em estado próprio, sem flicker; harmonia congelada na referência do recorte). Galeria: 5 tiles numa fileira no desktop (`lg:grid-cols-5`), 5 dots no mobile seguindo o scroll e dica "Deslize para ver os cinco modelos"; o badge `NOVO` passa para o modelo novo. Shell do dialog endurecido (`flex flex-col` sobre o `grid` do kit) para o corpo rolar e o rodapé ficar pinado com o seletor aberto. Sem migration, sem collection/CMS, sem Consent, sem segundo cadastro: roster e artes são estáticos, 100% no aparelho e nenhum byte da foto sai. Verificação: unit (catálogo 53/snapshot/sha256/dimensões dos 106 arquivos via sharp, silhueta/`subject` no recorder falso, pins do 5º modelo), `pnpm gate:fast` e e2e do `frontend` (35 verdes: seleção por busca/teclado, fluxo completo com download real do PNG, troca preservando nome/recorte, dots mobile).
