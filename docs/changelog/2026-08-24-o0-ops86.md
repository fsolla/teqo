**O0+ (OPS86+): curated não engole unmapped-risk sem rastro (2026-08-24):** `selectE2eSpecs` (scripts/lib/test-affected-core.mjs) retornava `unmapped: []` no early-return de high-risk antes de computar os unmapped de área de risco — um diff com migração + arquivo de risco sem manifesto perdia o rastro sem diagnóstico. Fix: hoist `collectUnmappedSrc` (fonte única de unmapped, semântica espelho da walk) antes do check high-risk; `unmappedRisk` (filtro `E2E_RISK_PREFIXES`) é exposto no `unmapped` do curated e no `reason` (`; curated + risk files without mapping: ...`). Mode continua `curated` (a curadoria cobre as superfícies de risco). Pin novo em `testAffected.unit.spec.ts`: "high-risk + risco não-mapeado → curated com unmapped no reason". Manifesto production intocado (todo risk prefix tem entry — o caso real só ocorre em drift). Consumidores (`ci-scope`/`e2e-affected`/`run-e2e-affected`/`gate-ci`/`ci-pr.yml`) intocados: `unmapped` populado em curated só soma diagnóstico (stderr + reason), não altera gating. Verificação: `test:unit` 2456 verdes, `gate:fast` verde, classificador exercitado à mão (produção: unmapped vazio; drift: unmapped + risk note).
