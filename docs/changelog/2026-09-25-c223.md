**C223 — Falas longas entram no acervo sem estourar o limite do texto normalizado (2026-09-25):** a ingestão web passa a aceitar transcrições completas de qualquer duração nos campos brutos e derivados de `Speech`, `Recording` e `ContentPiece`, com `TRANSCRIPT_TEXT_MAX_LENGTH` explícito e o default global de 40 000 preservado para os demais textos. O upsert transacional, a idempotência por `sourceKey`, o relatório e a busca `LIKE`/GIN continuam intactos; regressões cobrem fala longa, marcador no fim da transcrição, retry sem duplicação e persistência dos campos irmãos. Sem migration, mudança de URL, busca/facetas ou UI.
