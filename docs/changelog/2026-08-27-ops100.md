# OPS100 — Worktree model flags `--glm` and `--free`

**Date:** 2026-08-27
**Issue:** — (direto do humano, config-only)
**Type:** ops, config

## Summary

Two new model flags in the `pnpm worktree` per-invocation menu (`next`/`plan`/`new`/`fix`): `--glm` → `opencode-go/glm-5.3-flash` (GLM-5.3-Flash via provider Opencode Go) and `--free` → `openrouter/openrouter/free` (Free Models Router via OpenRouter). Menu goes from 5 to 7 named flags; same mechanism (edit the constant/map, OPS26/OPS93), no behavior change without flags.

## Changes

### Scripts

- `scripts/lib/worktree.mjs` — `WORKTREE_MODEL_MAP` gains `glm` and `free` entries; conflict error message in `resolveWorktreeModel` lists the two new flags; docblocks updated
- `scripts/worktree.mjs` — docblock + `--help` flag lists synchronized (pipe and slash forms) with the two new model IDs in the map prose

### Tests

- `tests/unit/worktree.unit.spec.ts` — map pin updated (7-flag map + `WORKTREE_MODEL_FLAGS` pin + single-flag resolution for `glm`/`free`)

### Documentation

- `.agents/shell/worktree.sh`, `.agents/skills/worktree-next-issue/SKILL.md`, `.agents/skills/local-database/SKILL.md`, `.opencode/commands/worktree.md` — flag lists and per-flag model values synchronized

## Verification

- `opencode models` — confirms `opencode-go/glm-5.3-flash` and `openrouter/openrouter/free` exist (models.dev display names: "GLM-5.3-Flash (2x usage)" / "Free Models Router")
- `pnpm gate:fast` — green (lint, format, typecheck, unit)
