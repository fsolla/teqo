**Admin em branco — importMap órfão do handler S3, 4ª recorrência (2026-08-26):** o admin do Payload em produção (jorgesolla1313.com.br/admin) abria em TELA BRANCA (login e dashboard) — zero erros de console no browser, SSR 200, assets 200; detecção por relato humano. Causa: o `src/app/(payload)/admin/importMap.js` commitado perdeu a entrada `@payloadcms/storage-s3/client#S3ClientUploadHandler` no commit 1c865bd8 (2026-08-23, refactor "E1 simplify"), que regenerou o importMap sem as envs `S3_*`; com as envs setadas em prod (plugin s3Storage condicional em src/payload.config.ts, resolvedor src/utilities/mediaStorage.ts), `getFromImportMap` retorna undefined e a raiz do Payload não monta — o erro sai como console.error no log do SERVIDOR, invisível no browser; dev/test/CI sem `S3_*` usam storage local e o importMap órfão é invisível no loop local (e2e do admin passa), e o smoke do deploy só checa HTTP 200. Classe recorrente: OPS69/OPS72/OPS73 (4ª vez); o guard de CI planejado nas lições do OPS69 (depends #77/#87) nunca foi implementado. Fix causa: regeneração do importMap com `S3_*` dummy → diff de exatamente +2 linhas (import + entrada); sem migration/access/UI. Prevenção implementada no mesmo PR: pin unit `tests/unit/importMapS3UploadHandler.unit.spec.ts` (vermelho sem o fix, verde com); guard `scripts/check-importmap-s3.mjs` (regenera com envs dummy, exit 1 com drift + restore se divergir) ligado no ci-pr.yml (content guards) e no job verify do deploy.yml; wrapper `scripts/generate-importmap.mjs` no `pnpm generate:importmap` (injeta `S3_*` dummy quando ausentes — regeneração local nunca mais orfa o handler); contrato compartilhado `scripts/lib/importMapContract.mjs`; AGENTS.md atualizado. Verificação (prova do verificador 6/6): regressão e guard vermelho/verde provados, unit completa 284 arquivos/2682 testes verdes, e2e original com S3\_\* dummy (PORT 4093) monta login+dashboard com 0 `getFromImportMap` e 0 erros de console, tsc/lint exit 0, gate:fast verde. Prevenção documentada (não implementada): gerar o importMap no build (BuildKit secrets) em vez de commitá-lo, s3Storage incondicional no config, smoke de deploy headless que asserta o mount da raiz Payload, e fail-fast estruturado quando getFromImportMap retorna undefined. Deploy PENDENTE (manual, workflow_dispatch do deploy.yml) + confirmação do humano em prod. Post-mortem: `docs/postmortems/2026-08-26-admin-branco-importmap-s3.md`.
