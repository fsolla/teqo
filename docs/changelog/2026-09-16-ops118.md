**OPS118 — relatório de cidade: lote de municípios + sub-agentes por etapa (2026-09-16):** a skill `/relatorio-cidade` deixa de ser single-city: a invocação aceita um município (caso de sempre, N=1 sem regressão) ou vários separados por vírgula (`/relatorio-cidade Ilheus, Itacare, Una`), cada token resolvido a slug canônico pelo catálogo existente (`isMunicipalitySlug` ou fold `resolveMunicipalityName` → `municipalityCatalogEntriesForCity`; `Salvador` é ambíguo — 19 zonas `salvador-ze-N` — e vira falha explícita, nunca expansão silenciosa). O pipeline vira etapas: o **orquestrador** (agente principal) parseia/deduplica a lista e coordena; um **sub-agente researcher por cidade** (`.opencode/agent/relatorio-cidade.md`, que perde os papéis de ssh/build) faz a pesquisa web — o único passo pesado de contexto — e devolve só um **recibo curto** (`slug`, `status`, `itemCount`, `gapCount`, `gaps`, `newsCount90d`, `weakSourceCount`, `failureReason`), nunca o corpo do `research.json`; a extração read-only no homeserver é **serializada** (checkout `~/teqo-report` compartilhado) e o build local é por cidade, ambos com log em `data/relatorios-cidade/logs/<slug>.log` fora do contexto do orquestrador. **Falha isolada:** cidade inválida/ambígua, mismatch snapshot×research (guard fail-closed inalterado) ou erro de build não cancela as demais — o summary final diz o que saiu e o que falhou. Sem PDF/índice agregado, sem mudar conteúdo/layout do relatório, sem tocar nos dois CLIs por cidade, sem command/`opencode.json`/`src/`, artefatos seguem gitignored. Entrega documental: `.agents/skills/relatorio-cidade/SKILL.md` (seções "Lote", "Pipeline (etapas)" e "Recibo do researcher") + subagente researcher + docs-guard `tests/unit/cityReportBatchSkill.unit.spec.ts`; prova local: 3 cidades → 3 pares PDF+MD, resolução (`Ilheus`/`Itacare`/`Una` ok, `salvador` ambíguo, token inválido isolado) e mismatch fail-closed, além de uma corrida real do researcher devolvendo só o recibo. Issue #1065.
