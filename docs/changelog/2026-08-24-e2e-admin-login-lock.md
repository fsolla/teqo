**E2E-ADMIN-LOGIN-LOCK — unificar logins do usuário de teste no helper com advisory lock (2026-08-24):** fechamento do débito restante do S10 (#767, orig. do simplify do S10 #94). O e2e full mantinha logins do usuário compartilhado `dev@payloadcms.com` fora do ponto único de serialização: o shadow local `adminHeaders` sem lock em `frontend.e2e.spec.ts` (11 call sites, 10 `seedTestUser` por teste) e o `cleanupTestUser` que ainda **deletava** o usuário no `afterAll` do `admin.e2e.spec.ts` no meio da suíte paralela — janela de usuário ausente contra seeds concorrentes e sessão read-modify-write perdida (token perdedor → 403). A entrega removeu o shadow e roteou os 11 logins pelo helper `tests/helpers/adminApi.ts` (`adminHeaders(request, baseURL)`) com `pg_advisory_lock(727001)` — o mesmo lock do `login()` UI — e removeu `cleanupTestUser` (helper morto + afterAll), convergindo o registro do OPS73-FOLLOWUP que já o declarava removido. Verificação local: admin.e2e (5/5) e frontend.e2e (20/20) verdes; corrida exercitada em run com cadeia completa (falhas colaterais da família campaign isoladas como pré-existentes via `git stash` em main limpo — 5 reprodutíveis + 5 irmãos 1×). Dívida capturada: **#882** (`E2E-FLAKE-CAST-ROTATIVA-POS-OPS83`, depends #767) — cast rotativa de flakes da família campaign.
