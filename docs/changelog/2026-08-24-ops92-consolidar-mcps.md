**Recently resolved (2026-08-24):** **OPS92** — consolidar MCPs do opencode: global fica só com `jina` + `playwright`; `penpot` e `postgres` viram subagentes no repo Teqo (`opencode.json` + `.opencode/agent/penpot.md`/`postgres.md` com `permission` gated — `penpot*`/`postgres*`/`query` negados no `build` e liberados só nos subagentes, validado via `opencode debug config`/`agent`); GitHub MCP morto (`github` removido do global, já `enabled:false` desde 17/08) — `gh` CLI 2.98 autenticado cobre o canal; `forgejo`/`jobspy`/`office-word`/`stalwart` re-homed para os repos donos (decisões do gate 2026-08-24): `forgejo` → `lei-mercados-digitais` + `infra-solla` + `iara-pwa`, `jobspy` → `jobs`, `office-word` → `lei-mercados-digitais`, `stalwart` → `infra-solla`; `AGENTS-infra.md:13` e `docs/campanha/prompt-montagem-home.md` coerentes com subagente, `project-status`/`engineering-audit` sem `MCP/pnpm issue`. Global é manual (fora do git, como OPS89); Teqo fica com 4 MCPs (2 globais + 2 locais), siblings com o dono + 2 globais (validado `opencode debug config` nos 5 repos). Sem migration, sem Longa, `pnpm gate:fast` verde.
