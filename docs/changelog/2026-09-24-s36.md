**S36 — Central de Conteúdos: logo do header no tamanho da marca (2026-09-24):** a marca no header da Central pública (`/conteudos`) lia ~3× menor que a referência (`/jingles`) porque a caixa media a moldura transparente do PNG oficial (`jorge-solla-negativo.png`, canvas 1037×595, tinta 790×285), não o desenho. A correção é só no dono único `ContentPiecePageHeader` (page-local, sem tocar `CampaignPageHeader`): o `Image` direto (`h-7 sm:h-9`) vira a janela de recorte da opção A do design aprovado — `overflow-hidden` 142×39 no mobile (tinta 186px, left −22 / top −29) e 188×50 no desktop (tinta 246px, left −29 / top −38) — com o mesmo PNG oficial, sem binário novo no kit, sem migration/access/Consent e com header (`h-14 sm:h-16`), badge "Central de Conteúdos" e "Voltar à Central" intactos nas três superfícies (catálogo, peça, não-encontrado). Verificação: `pnpm gate:fast` verde (4489 unit), e2e `frontendConteudos` 7/7 verde, lado a lado com `/jingles` em 390/1280 e crítica final do `designer` **certificada** (`Design tier: openai/gpt-5.6-sol`).
