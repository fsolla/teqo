**FIX — 401 `Bad credentials` do `GITHUB_TOKEN` local (PAT expirado) + C201 `main` RED (2026-09-18):** `pnpm issue next`, `agent:*`, `github-pr.mjs` e o próprio `gh` falhavam com `GitHub API GET /repos/fsolla/teqo/issues → 401 Bad credentials`. Causa-raiz: o `GITHUB_TOKEN` exportado no `~/.bashrc` desde o OPS71 era um PAT fine-grained, expirou (~30d) e, inválido, passou a sombrear a credencial válida do keyring do `gh` (que honra o env var acima do keyring) — e o bash dos agentes herda o env do `opencode serve`, não do `.bashrc`, então worktrees quebravam também. Fix: (1) **fora do repo** o `~/.bashrc` deriva `GITHUB_TOKEN` de `gh auth token` (sobrescreve valor herdado/expirado, auto-renova enquanto o login do keyring existir); (2) **repo** `scripts/lib/github-api.mjs` anexa dica acionável a todo 401 (REST e GraphQL: rode `gh auth status` e rotacione o PAT), pinada por 2 testes unitários em `tests/unit/githubApi.unit.spec.ts`, e `docs/AGENT-OPS.md` documenta a expiração/recuperação; (3) **C201/#1169** — o rename do C193 (`canReadSpeechCatalog` → `canReadCommunicationCatalog`) deixou o import órfão em `src/utilities/speech/speechPageData.ts:5`, e o `typecheck` da `main` estava RED desde `bdaf3c7c` (job `verify`/Typecheck do deploy run 35394524888, bloqueando staging/produção) — import/call site corrigidos. Sem migration/schema/Consent; post-mortem em `docs/postmortems/2026-09-18-github-token-401.md`.
