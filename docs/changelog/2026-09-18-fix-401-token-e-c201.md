**FIX — 401 `Bad credentials` do `GITHUB_TOKEN` local (PAT expirado) + `AUTOMERGE_PAT` expirado + tracker C201/#1169 (2026-09-18):** `pnpm issue next`, `agent:*`, `github-pr.mjs` e o próprio `gh` falhavam com `GitHub API GET /repos/fsolla/teqo/issues → 401 Bad credentials`. Causa-raiz: o `GITHUB_TOKEN` exportado no `~/.bashrc` desde o OPS71 era um PAT fine-grained, expirou (~30d) e, inválido, passou a sombrear a credencial válida do keyring do `gh` (que honra o env var acima do keyring) — e o bash dos agentes herda o env do `opencode serve`, não do `.bashrc`, então worktrees quebravam também. Fix: (1) **fora do repo** o `~/.bashrc` deriva `GITHUB_TOKEN` de `gh auth token` **sobrescrevendo** valor herdado/expirado (auto-renova enquanto o login do keyring existir; `opencode serve`/`web` e `zed` precisam de restart para perder o env velho); (2) **repo** `scripts/lib/github-api.mjs` anexa dica acionável a todo 401 (REST e GraphQL: rode `gh auth status` e rotacione o PAT), pinada por 2 testes unitários em `tests/unit/githubApi.unit.spec.ts`, e `docs/AGENT-OPS.md` documenta a expiração/recuperação; (3) **C201/#1169** — o rename do C193 deixou import órfão em `src/utilities/speech/speechPageData.ts:5` (typecheck da `main` RED desde `bdaf3c7c`, bloqueando staging/produção); corrigido de forma independente pelo PR #1171 (`c7d1fd29`), e este PR fecha o tracker — o drive-by equivalente foi dispensado no rebase; (4) **achado do mesmo lote**: o secret `AUTOMERGE_PAT` (criado no OPS71) também expirou e o safety net `agent-pr-ready-automerge` falha fail-closed desde 2026-09-18T21:39Z — **rotação é ação humana pendente**; até lá, merge manual com o required check verde. Sem migration/schema/Consent; post-mortem em `docs/postmortems/2026-09-18-github-token-401.md`.
