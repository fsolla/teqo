**C151 — Atividade: marcar evento como público (2026-09-12):** o staff agora marca/desmarca "Evento público" no modal de atividade (criar e editar), uma flag durável e consultável que registra a INTENÇÃO de expor o compromisso na futura agenda pública do site — a exposição em si é item PUB posterior. Campo `publicEvent` (checkbox, default `false`, indexado) na collection `activity` existente, com o mesmo padrão de `deputyPresent`/`allDay`; migration `20260912_224719_add_activity_public_event`. O parser de FormData, o zod de entrada, o `ActivityFormViewModel`/`activityFormSelect` e o `ActivityOverlay` (checkbox ao lado de "Todo o dia", default desmarcado, volta correto ao reabrir) acompanham o campo; o snapshot do guard de liderança em `Activity.ts` inclui a flag (defesa em profundidade). Nada no site público, na agenda, na lista, no detalhe ou no espelho do Google (C114) muda: `publicEvent` fica fora de `SYNC_RELEVANT_ACTIVITY_FIELDS` e dos selects de agenda/lista/detalhe, sem filtro/badge novo. Testes: unit do overlay (render, submit e prefill), int de parse e persistência/reabertura, e o e2e `campaignActivity` cobre marcar→salvar→reabrir. Plano: `docs/plans/c151-atividade-evento-publico-impl.md`.
