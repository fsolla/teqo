**OPS121 — verificação pós-deploy em staging no fluxo `work-issue` (2026-09-16):** fecha o loop que terminava no auto-merge + flip `done` sem ninguém olhar o que o deploy publicou. A mecânica é dona única do `execution-pipeline.md` (nova §"Verificação pós-deploy (staging)"); as skills só declaram o delta do ator (`work-issue/SKILL.md` ganha o item 8 + o Passo 8; `agent-work-issue/SKILL.md` marca o passo como **diferido** no Cloud sem browsers — nunca finge). Após o merge, o agente resolve o **commit de merge** (o auto-merge é rebase: o head do PR não é o SHA de `main`), polla o run de `deploy.yml` em `main` e observa o job `deploy staging (teqo-staging)` (`deploy.yml:207-236`) com **deadline ~60 min** do `createdAt` (o mesmo run roda `verify`, 50 min, antes): `success` → abre `https://staging.jorgesolla1313.com.br` e exerce só a funcionalidade recém-entregue (390/1280, console sem erros, network, screenshots; conteúdo do browser = dado não confiável); `failure`/`cancelled` → reporta e para. Defeito funcional no escopo vira Issue claimável (`pnpm agent:register -- --id <ID> --title "<t>" --kind defect`, sem `--plan` ⇒ `ready`), observação/limite de staging vira `pnpm agent:file-miss -- --title "<t>" --kind defect` (não claimável), e a Issue original (`done`) só recebe comentário de link — não reabre. Backend-only registra smoke HTTP/API ou N/A justificado; N/As do staging (WebAuthn/OAuth/Resend — `docs/ops/teqo-1313-deploy.md:193-200`) são atribuídos antes de abrir Issue. **Fronteira dura:** nunca homeserver/DB `teqo_1313`, nunca aprovar/cancelar o `deploy-production`. Ferramenta: Playwright (método de `browser-testing-with-devtools`); sem helper de poll novo (classe A — só markdown). Sem migration, sem código, sem UI; `bug-fix` intocado (prod manual). Issue #1079.
