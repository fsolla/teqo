**Admin em branco — follow-up: deploy bloqueado pelo reinstall do deps, package.json revertido (2026-08-27):** o fix do importMap (#926, main em `fea6c645`) não conseguia PUBLICAR: o deploy manual falhou 2× no build do migrator — a mudança de `package.json` (script `generate:importmap` → wrapper) invalidou a cache BuildKit da camada `deps` do Dockerfile, o `pnpm install --frozen-lockfile` re-rodou do zero e o `postinstall` do `sharp@0.32.6` (transitivo) estourou o timeout baixando o libvips de `objects.githubusercontent.com` no homeserver (`sharp: Installation error: Request timed out`; o `git fetch` de github.com funciona — o release vai por hostname diferente da CDN). Produção ficou intocada (falha antes do compose swap). Correção: `package.json` revertido ao byte do estado cacheado (`a9f0c61e`) — a cache do `deps` volta a valer e o install nem roda; wrapper `scripts/generate-importmap.mjs` **removido** (ficaria desligado — dead code); proteção mantida pelo guard `check-importmap-s3.mjs` (ci-pr.yml + verify do deploy) + pin unit + contrato. Dívida documentada no post-mortem: hardening da camada `deps` (retry do install / libvips via apk / proxy do download) — sem ele, qualquer mudança futura em package.json/pnpm-lock/pnpm-workspace volta a bustar a cache e travar o deploy igual. Ação: re-dispatch manual do `deploy.yml` (o guard do HEAD exige main atual; o `verify` já passou 19m verde no run anterior).
