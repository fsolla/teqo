**C163 — relatório de cidade: pauta das lideranças, acervo por região/tema e fonte inline (2026-09-15):** o relatório de cidade pré-viagem ganhou duas frentes de pesquisa e uma convenção de fonte inline. (1) **`leaderAgenda`** — novo campo opcional do `research.json` e bloco **"Pauta das lideranças (pesquisa)"** na seção 3 (`scripts/lib/cityReportResearch.mjs` valida `name` + `topics`/`hook` + URL/data; sem fonte vira `agenda_lideranca_sem_fonte`; `scripts/lib/cityReportBlocks.mjs` renderiza `Líder · Campo · Pauta provável · Gancho recente · Fonte`), com a ressalva de que a pauta é **hipótese ancorada em fonte a validar na conversa**, priorizando rede da campanha e mesmo campo; liderança de rede sem rastro público é lacuna de ativação, não pauta inventada. (2) **Acervo por região e tema** — o extrator (`scripts/cityReportSnapshot.mjs`) passou a devolver três recortes não sobrepostos de `speeches`: município, **região** (demais municípios do mesmo Território de Identidade, `speeches.region`) e **temas regionais** (`speeches.topics`, temas-chave do interior via enum `SPEECH_TOPICS`); a seção 10 mostra as três tabelas com o rótulo honesto de cada recorte — "Menção a \<cidade\>", "Município da região citado" e **"Tema"** (falas selecionadas por tema **não** são marcadas com o município e não podem ser rotuladas como tal); região/polo nunca é somado como fala da cidade e snapshots antigos degradam para a busca por município sem quebrar. (3) **Fonte inline** — os textos de pesquisa aceitam os tokens `{{fonte}}` (fonte principal) e `{{fonte:2}}`/`{{fonte:3}}`… (`extraSources`), resolvidos pelo renderer em `(fonte)` / `(fonte N)` **no ponto exato da citação**, no PDF e no companion `.md` (`[(fonte)](url)`); a coluna Fonte fica só com a data e a lista completa de URLs segue abaixo da tabela (`> Fontes:`). `extraSources` agora é preservado também em `opposition`/`alliances`/`investments`/`approach`/`leaderAgenda`. Skill `.agents/skills/relatorio-cidade/SKILL.md` atualizada (contrato do `leaderAgenda`, recortes do acervo, tokens inline). Unit C163 verdes (37/37); sem migration/UI/access/Consent/e2e (artefatos PDF/MD e `research.json`/`snapshot.json` seguem gitignored).
