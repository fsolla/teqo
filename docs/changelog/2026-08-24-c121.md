**C121 — chave de lock da redenção de convite unificada com o lock de ficha `contact-ficha:<id>` (2026-08-24):** débito colhido no fechamento do C120 (#663): a redenção de convite serializava com `invite-redemption-contact:<id>` enquanto o find-or-create (C120) usa `contact-ficha:<id>` — fluxos que resolviam a MESMA ficha por primários diferentes não se serializavam e um telefone digitado sumia. **Renomeada** a chave para `contact-ficha:<id>` via `acquireContactFichaLock` (já existia em `contactPhoneLocks.ts`); **removido** o helper `acquireCampaignInviteRedemptionContactLock` (`campaignInviteRepository.ts:127-132`, único caller). **Realinhada a ordem ao C120** (telefones/account em batch PRIMEIRO, ficha POR ÚLTIMO e sozinha) ao mover a aquisição para DEPOIS dos phone/account-locks e ANTES do RMW do contact — cobre a mesma janela (pg_advisory_xact_lock é transaction-scoped) e **evita o deadlock** que o simples renome (mesma ordem) introduziria sob a corrida (redenção ficha→phone vs create phone→ficha na mesma chave). **Re-leitura da ficha DENTRO do lock** em ambos os fluxos (login `campaignInviteRedemption.ts` + autofill), preservando a semântica de reorder do primário mas com phones frescos — senão o append concorrente do create seria dropado pelo full-replace stale. **Incluído o path de autofill** (antes sem lock de ficha) para fechar 100% a corrida. Sem migration (schema intocado). **Spec int `tests/int/campaignInvite.int.spec.ts`** revalidado: `invite-redemption-contact:`→`contact-ficha:`, teste de ordem reescrito para ficha por último, e novo teste de serialização do autofill no `contact-ficha`. /simplify (2 revisores): MAJOR (RMW stale pré-lock) e minor (teste de autofill) ambos resolvidos inline; nenhum débito aberto. Closes #885.
