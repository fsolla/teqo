**S9+ (#771) — integridade de consentimento e alinhamento das ações públicas:** `Consent.beforeDelete` bloqueia a exclusão de um consentimento ainda referenciado por consumidores (subscription, signature, pushSubscription, petition `form.consent`, leadership, supporter `consent`/`voteIntentionConsent`) com `APIError` 409 e mensagem composta por coleção — fim do `23502` cru no admin. `submitWhatsapp`/`submitPetitionSignature` delegam o ciclo de transação ao dono `withPayloadTransaction` (rollback também no commit-falho, `AggregateError`, after-commit flush), preservando os contratos `{ ok: true }`/`{ ok: true, signatureNumber }`. `contactStateSchema` passa a ser exportado e `contactCitySchema`/`contactCityFieldSchema` são consolidados; as 4 fichas (`contactSchema`, `contactCreateSchema`, `contactFieldUpdateSchema`, `contactFullUpdateSchema`) usam os exports únicos em vez de cópias inline do predicate `value in CitiesByState` (DRY de conhecimento no dono). Simplify: 0 high/medium; 3 low (type-safety do `where` do hook descartado, `beginFailureMessage` por action intencional, helper de collapse `''` deferido) — triage no impl plan.
