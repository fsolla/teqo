**C190 — Dossiê Solla por tema/área + Boletim modelo (2026-09-18):** terceira variante da família de dossiês — o recorte passa a ser a **área de política** do acervo (os 18 `SPEECH_TOPICS`), ao lado de município (C186) e instituição (C187), sem geminar o pipeline. (1) **Seam generalizado**: `scripts/lib/dossieUnit.mjs` ganha `THEME_UNIT` (`area|segmento|rede`, default `area`) e a forma `subject` compartilhada com a instituição; `dossieBlocks`/`dossieRender`/`dossieBulletinRender` passam a ler identidade, esferas, listas de abrangência, método por era, limites e copy do descritor — município e instituição seguem byte-idênticos (specs C186/C187 verdes; `isSubjectUnit` substitui `isInstitutionUnit`). (2) **Resolução canônica no dono**: `resolveSpeechTopic` exportado de `src/lib/speechFacets.ts` resolve valor/label (acento/caixa-insensível) e **falha fechado** — token fora dos 18 nunca vira slug inventado. (3) **Recorte nativo do acervo**: `scripts/themeSnapshot.mjs` + `scripts/extract-theme-snapshot.mjs` (`THEME_REPORT_CONFIRM=1`, sessão read-only) filtram `Speech.topics` direto, sem ponte nominal nem `mentionExcerpt`; `scripts/build-dossie-solla-tema.mjs` pagina por medição (`dossiePack`) e emite PDF A4 + `.md` + boletim de 1 página. (4) **Skill/comando/2 subagentes** (`dossie-solla-tema` researcher por era + redator), checklist próprio por era, lote por vírgula com dedup e falha isolada (`sucesso parcial`), sem escape one-off. (5) **Guardrails**: abrangência nunca somada (`área × segmento × rede`), empenho ≠ pagamento, atribuição de emenda/proposição só com fonte, acervo 2011+, PII mínima, artefatos gitignored, sem schema/migration/DB write. Specs novas: `dossieTheme`, `dossieThemeResearch`, `dossieThemeBatchSkill` (unit) + `themeSnapshot` (int) e `resolveSpeechTopic` no spec do dono; `SCRIPTS_SPEC_PINNED` e `opencodeCommands` atualizados. (6) **Design certificado** pela crítica final do `designer` (`openai/gpt-5.6-sol`) após adaptação theme-only via descritor: pills/identidade canônica no boletim, cards sem métrica com ano ou variante textual, cena do acervo com universo/amostra + alerta de pertinência, tabelas temáticas de abrangência/lacunas/hierarquia de fontes, cobertura das eras pelo ledger, fases separadas e eixo anual completo; smoke com continuidade real (lacunas 15+16+10, notícias 16+16+9, 20 páginas).
