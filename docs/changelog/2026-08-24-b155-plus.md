**Recently resolved (2026-08-24):** **B155+** (Issue #368) — padrão de escrita de célula: migra os quick-edits restantes de form action para rota JSON. **F1 (sinal de municípios) morreu antes do lote** — C87 (2026-08-07) trocou o quick-edit por form multi-campo (`MunicipalityListUpdateControl`), que per a regra B155 corretamente permanece em server action; registrado como "já resolvido" no plano de intenção. **F2+F3 entregues:** `POST /campanha/liderancas/state-deputies` e `/campanha/liderancas/municipalities` via `campaignJsonMutationRoute` (schemas reusados `leadershipStateDeputyMembershipSchema`/`leadershipMunicipalitiesMembershipSchema`; allowlists floor/cap/scope/cap-dobradinhas; revalidate herdado das actions record — contrato B34/B31 preservado, a lista onde o chip está não é revalidada). Call sites trocados por wrappers `'use client'` de mesma assinatura `(state, formData) → postCampaignJson` (`LeadershipStateDeputiesColumnCell`, `LeadershipMunicipalitiesColumnCell` em `src/components/campaign/leadership/`), shared cells intocadas (multi-consumidor: dobradinhas/assessores/pessoas). **B36 incluído de graça** (coluna "Lideranças" de `/campanha/dobradinhas` usa a mesma action record da rota F2 — 3 form actions removidas no total). **Deferido com gatilho:** chips B37/B156 de `/campanha/dobradinhas` (domínio stateDeputy) e inline-edits por campo B153/B163/C129 (contrato de célula próprio) — rotas F2/F3 já nascem prontas para os dois lados. Guards atualizados: whitelist do `sharedSheetHostConventions` (+2 wrappers, padrão `PeopleMunicipalityCell`), prewarm e2e (+2 rotas), e2e B34 aponta para `/campanha/liderancas/municipalities`. Sem migration/access/Consent/UI/schema (Impeccable A).
