**Instagram por link na Central volta a baixar e catalogar — early-stop da janela de 500 (2026-09-24):** o C220 prometia "janela de 500 mídias com early-stop; a chamada típica é 1", mas a implementação pedia as até 10 páginas de cursor ANTES de casar o shortcode: qualquer erro em página ≥2 (a doc oficial da edge `/{ig-user-id}/media` documenta paginação time-based) lançava e o resolver devolvia `indisponivel`, mesmo com o post recém-publicado já na página 1 — a peça-link voltava sem mídia/transcrição. O dono da paginação (`src/utilities/socialFeed/instagramFeed.ts`) ganha o opcional `shouldStopAt?: (post) => boolean` (checado após cada página; board/sync não passam — contrato de 1 página e tudo-ou-nada intocados) e o resolver (`src/utilities/content/contentPieceLink.ts`) passa `matchesInstagramShortcode(link.shortcode)`, reusado no match final: a colagem típica volta a custar UMA chamada de mídia e falha profunda nunca mais derruba um match de página 1. Sem migration/access/UI. Verificação: int de regressão com o `loadInstagramFeed` REAL e `fetchImpl` fake (`after=` → 400) — RED provado sem o fix (`indisponivel`) e verde com; unit do dono (para no match, continua quando o alvo está mais fundo); int contentPiece 38/38; `pnpm gate:fast` verde (unit full 4669). Post-mortem: `docs/postmortems/2026-09-24-instagram-link-sem-early-stop.md`.
