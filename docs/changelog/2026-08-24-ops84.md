**OPS84 — verificação de valores (não só entidades) da migração — foco nas estimativas de votos (2026-08-24):** extensão do reconciliador OPS79 (`reconcile-campaign-vertical.mjs`) com modo `--values` que compara todas as colunas de conteúdo das 13 tabelas da vertical campanha (Neon vs `teqo_1313`). Colunas classificadas em 3 buckets: content (falha o run), derived (hook-derivadas, informativo), sensitive (PII, nunca impresso). Resultado: 0 divergências de conteúdo em todas as 13 tabelas — a migração de valores está 100% íntegra. Runbook em `docs/ops/teqo-1313-deploy.md` §OPS84.
