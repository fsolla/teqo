**B195-F1 — gate S: (stream settle) no último spec exposto à duplicação do wizard (2026-08-24):** `campaignRegisterDemand.e2e.spec.ts` ('creates a demand from the final step') recebe `waitForStreamSettled(page)` após o `goto` do passo final — o HTML de prod-build sob carga contém a árvore do shell + form duas vezes (cópia hidden `div[id^="S:*"]`, artefato OPS83/C106 #517; diag DOM em #659: ~15KB, action placeholder pré-hydration vs `$ACTION_REF_1`) e o spec ficou fora dos 21 call sites migrados pelo E2E-DEBT-S-GATE. O `.first()` legado (cd469857) permanece com comentário corrigido; kind/body/fill/submit seguem SEM escopo de propósito — rede de deteção que mantém falhando um form genuinamente duplicado em produção. Raiz no Next 15.4.11 (streaming RSC/form-actions sob concorrência) registrada como débito com gatilhos na Issue #879. Zero product code; smoke local E2E_PROD=1 3/3 verde.
