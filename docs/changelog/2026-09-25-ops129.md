**OPS129 — Modelos das flags do worktree: `--zen` → Space Bunny Free e `--free` → Space Bunny Alpha (2026-09-25):** o mapa fixo `WORKTREE_MODEL_MAP` agora resolve `zen` para `opencode/space-bunny-free` e `free` para `openrouter/stealth/space-bunny-alpha`. As referências nos scripts, shell, comando e skill foram sincronizadas, com o pin unitário atualizado para preservar preset, demais flags, fail-high e variante `max`. Sem alias, fallback, migration ou mudança de schema; verificação: teste unitário direcionado (102/102), smoke da ajuda e formatação.
