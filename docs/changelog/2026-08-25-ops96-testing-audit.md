# OPS96 — skill /testing-audit: auditoria autônoma da suíte com melhorias aplicadas

**Recently resolved (2026-08-25):** **OPS96** Skill `/testing-audit` — auditoria noturna autônoma da suíte de testes com implementação das melhorias seguras. Novo comando `.opencode/commands/testing-audit.md` + `.agents/skills/testing-audit/SKILL.md` (autossuficiente: baseline → inventário paralelo read-only por camada → rubrica de disposições com prova forte para remoção → loop ≤6 melhorias com prova verde antes/depois e revert fail-closed → fecho PR ready SEM auto-merge armado — desarme `--disable-auto` como último passo, com fallback draft), pinado no guard `opencodeCommands.unit.spec.ts`. Tooling: `scripts/testing-audit-metrics.mjs` (+ core puro em `scripts/lib/`, spec próprio) gera o retrato por camada dos reporters nativos; cobertura first-party `@vitest/coverage-v8` instalada atrás de flag com whitelist knip e ignores do artefato `coverage/` (eslint/prettier). Primeira execução (relatório: `docs/testing-audits/2026-08-25.md`): **16 specs puros movidos int→unit** com conservação exata de totais (pirâmide OPS90 mais honesta — int ficou fronteira Payload/DB real), 1 assertion reforçada (`api.int.spec.ts`), referência morta corrigida (`measure-e2e-family.mjs`); flake da família Google Calendar documentado com evidência de recidiva (4 falhas distintas em 7 runs full, verde isolado) e registrado em #906; propostas não executadas (e2e HTTP-sem-browser, moves condicionais, gaps) vivem no relatório. Intocáveis preservados: zero toques em LGPD/RBAC/liderança, ci-scope/e2e-manifest/workflows/husky e specs e2e.
