**OPS119+ — launch do worktree: risk map das libs + pin do wiring do `plan` (2026-09-16):** fecha os dois achados de score 3 do `/simplify` da OPS119 (#1066) na superfície do launch. **F1:** `HIGH_RISK_EXACT` (`scripts/lib/test-affected-core.mjs`) só marcava `scripts/worktree.mjs`; um diff que tocasse **apenas** `scripts/agent-session.mjs`, `scripts/lib/agent-session.mjs` ou `scripts/lib/worktree.mjs` classificava `classifyTestScope` como `none` e a suíte unit não rodava — as libs puras eram pinadas por specs que o próprio diff que as quebrava nunca exercitava. Os três entram no piso de blast radius (fail-closed), com pin de membership/classificação em `tests/unit/testAffected.unit.spec.ts` e um caso novo em `ciSkipInvariants.unit.spec.ts` que também confere a existência em disco (entrada high-risk morta por rename falha alto). **F2:** o repasse `argument: bag` de `cmdPlan` → `cmdNamespaceBranch` não tinha pin unitário — um `argument` dropado no caller passava silencioso. Extraído `namespaceLaunchDescriptor({ purpose, bag })` para `scripts/lib/worktree.mjs` (mapa puro purpose→`{ noun, sessionLabel, argument }`, purpose desconhecido lança) e `cmdNamespaceBranch` passou a **derivar** o descriptor internamente de `{ purpose, bag }`: o parâmetro `argument` deixou de existir, então a classe do bug some estruturalmente (não há o que dropar). `cmdPlan`/`cmdNew`/`cmdFix` passam só `purpose`+`bag`; labels (`lote "…"`/`bag "…"`/`bug "…"`/`sequencial`) e `noun` preservados byte a byte. Sem migration, sem Consent/access, sem UI (Impeccable A). Testes: 7 casos unit novos no `describe` do descriptor (plan/new/fix com e sem bag, bag em branco, bag com aspas/backslash cru — sanitização segue só nos donos, purpose `'constructor'`/desconhecido lança) + os pins de high-risk; `pnpm gate:fast` verde (lint + typecheck + suíte unit full). Débito residual (risk map das ~40 `scripts/lib/*.mjs` unit-pinadas + static scope de `scripts/`) registrado como OPS119++ (#1077). Impl plan: `docs/plans/escala-dry-pos-ops119-impl.md`.
