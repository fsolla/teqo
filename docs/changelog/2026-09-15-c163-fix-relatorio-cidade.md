**FIX — C163 relatório de cidade: emendas por município, página 1 e datas (2026-09-15, Related #1025):** quatro defeitos encontrados na primeira geração real (Itamaraju), todos no caminho de leitura/render do relatório, nenhum toca a base. **(1) Emendas oficiais mentiam:** o endpoint `/api-de-dados/emendas` não tem filtro por município (swagger: `codigoEmenda, numeroEmenda, nomeAutor, tipoEmenda, ano, codigoFuncao, codigoSubfuncao, pagina`; `codigoMunicipio` é ignorado) e devolve valores como string pt-BR (`"81.000,00"`), que o `Number()` do módulo zerava. A extração real trouxe as **41 emendas 2023–2026 do autor** (localidade UF/Nacional/Múltiplo) com totais 0 — zero silencioso proibido. Fix: parse pt-BR, filtro por `localidadeDoGasto` casando com o `municipalityName` do snapshot (novo parâmetro do builder) e fail-closed → `status: 'gap'` com URL + motivo quando nenhuma linha é atribuível ao município. **(2) Página 1 estourava com pesquisa real:** a fonte por linha imprimia a URL completa em coluna de ~44mm e a guarda de overflow abortava até com respostas mínimas. Fix: fonte compacta na página 1 (`pesquisa web · data`; URL completa preservada em "Fontes e limites", seção 10) e a dica do KPI de emendas em gap deixou de repetir o motivo longo. **(3) Gap duplicado:** item do checklist declarado como gap pelo agente também virava "Não pesquisado."; agora o gap explícito marca o item como coberto. **(4) Data com um dia a menos:** `sourceDate` "AAAA-MM-DD" era formatado em America/Bahia a partir da meia-noite UTC (06/10 → 05/10); `formatDateBr` mantém a data de calendário para strings date-only. Regressões pino em `tests/unit/{portalTransparenciaEmendas,cityReportBlocks,cityReportResearch,cityReportFormat}.unit.spec.ts`. Verificação: unit 3108/3108, `tsc --noEmit` e eslint verdes. Issue #1025 registra a limitação da fonte e a decisão em aberto (fonte oficial por município × lacuna documentada); runbook/skill ainda prometem o filtro `codigoMunicipio` — atualizar quando a Issue fechar.
