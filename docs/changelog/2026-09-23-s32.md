**S32 — Analytics dos cards personalizados: quantos e quais (2026-09-23):** o download de qualquer card personalizado passa a contar **um** evento anônimo "Download do card" no **mesmo mecanismo do C213** — nenhuma collection, rota ou serviço paralelo: o card é `subjectType: 'card'` da `contentEvent` e o evento carrega o model id (`subjectId`) e, nos modelos com escolha estadual (S30/S31), o slug do estadual escolhido numa coluna nova `variant` (migration aditiva `20260923_190414_add_content_event_variant`, nullable, sem índice). O `POST /api/content-events` ganhou uma variante discriminada **estrita** (`{ type: 'download', subjectType: 'card', cardModelId, stateDeputySlug? }`): o cliente manda as chaves públicas e o servidor valida contra os catálogos commitados (`CARD_MODELS`/`stateDeputyCatalog`) — id de modelo inventado, slug em modelo sem picker, picker sem slug e slug desconhecido respondem 400 **sem gravar**; o contrato de peça (`{ type, pieceSlug }`) fica intacto (objetos estritos: exatamente um assunto por body). O beacon `sendCardDownloadEvent` sai do ponto único de download do `CardComposer.handleDownload` logo após `downloadBlob` (fire-and-forget, nunca lança/atrasa; o transporte `sendBeacon` → `fetch keepalive` virou um envio compartilhado no dono `src/lib/contentEvents.ts`). A home staff `/campanha` ganhou o bloco compacto **"Cards"** no resumo (`CampaignHomeCardDownloads`, porte classe-a-classe do artefato aprovado): um contador absoluto por modelo, na ordem natural do catálogo, 6 colunas no desktop e uma linha por modelo no mobile, com a caption de honestidade "Os números contam downloads, não pessoas ou visitantes únicos." e um vazio honesto antes do primeiro download; a agregação reusa `loadContentEventCountsBySubject` (uma query, 6 ids) e o mapper puro `resolveCardDownloadCounts` (`data | empty | unavailable`), e a falha da leitura **omite a seção** — nunca renderiza "Nenhum download ainda" (o estado indisponível fica distinto do vazio). Sem PII/cookie/IP/user-agent/rastreio, sem `Consent` novo, sem total/%/série/ranking/dashboard/rota nova; o bloco herda o gate de staff da home (leader nunca vê). Verificação: unit (`cardDownloadCounts` com ordem/estados/literais/normalização, `contentEvents` com o pin do campo `variant` e o beacon de card, `campaignHomeCardDownloads` com dados/vazio/indisponível), int (`contentEvents`: grava com `variant`, recusas de catálogo sem gravar, agregação `card` sem vazar `peca` e vice-versa, loader data/empty/unavailable), e2e (`frontend`: o beacon observado no download real de `eu-sou-solla`, `time-do-estadual` e `minha-colinha` com o slug; `campaignHomeActions`: o bloco com os seis rótulos na ordem e o leader sem o bloco), `pnpm gate:fast`, `pnpm test:int` e os e2e afetados locais. A crítica final do `designer` (trigger c) reprovou a primeira passada e os ajustes entraram antes do push: o divisor saiu de baixo do título "Cards" para o fim do resumo (o `h2` do bloco neutraliza a regra global `h2 { border-b pb-2 }` e o `CampaignHomeSummary` ganhou `border-b`), o bloco ganhou respiro inferior (`pb-4 md:pb-6`) e o `summarySlot` passou a rolar no mobile (`max-md:min-h-0 max-md:overflow-y-auto` no `CampaignHomeLayout`) — as seis linhas e a caption são alcançáveis acima do dock de ações; re-capturado e **certificado em tier primário** (`Design tier: openai/gpt-5.6-sol`).
