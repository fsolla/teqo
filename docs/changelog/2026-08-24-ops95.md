**OPS95 — Corrigir mapa de modelos e launch do worktree CLI, follow-up OPS93 (2026-08-24):** `opencodeLaunchDirective` deixa de emitir `--variant max` — o yargs do TUI do opencode rejeita o flag desconhecido e saía no helper em vez de abrir (quebra incondicional do OPS93 em next/plan/new); `WORKTREE_VARIANT` eliminada e o conceito "effort max na diretiva" sai junto (variantes ficam na config global da máquina via Ctrl+T; `opencode run --variant max` segue a via headless). Mapa `WORKTREE_MODEL_MAP` corrigido no GATE para os IDs pedidos: pro=deepseek/deepseek-v4-pro, go=opencode-go/hy3, alibaba=alibaba-token-plan/deepseek-v4-flash (cheap/zen mantidos); chaves, preset e guardrail at-most-one intactos. As 7 superfícies que citam o mapa/diretiva sincronizadas na mesma entrega (`scripts/lib/worktree.mjs`, `scripts/worktree.mjs` docblock/help incl. resquício "`--go` é no-op" do kill, `.agents/shell/worktree.sh`, `.opencode/commands/worktree.md`, skills `worktree-next-issue` e `local-database`) + pins de `tests/unit/worktree.unit.spec.ts`.
