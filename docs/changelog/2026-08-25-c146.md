**C146 (#913) — guarda CSRF de mesma-origem de `/campanha` proxy-aware (fix do "Requisição inválida." atrás do túnel):** `isSameOriginRequest` (`src/utilities/sameOriginRequest.ts`) passou a comparar o header `Origin` contra duas bases: a literal de `request.url` (legado, verbatim — dev/portas de worktree/TLS direto) **ou** a origem pública resolvida pela política estabelecida de `getCampaignInviteBaseURL` (`campaignInviteOrigin.ts`, intocado: `x-forwarded-*` para autoridades locais, `NEXT_PUBLIC_SITE_URL` HTTPS-DNS-público em produção, fail-closed). Atrás do cloudflared, `request.url` chega `http://localhost:3000` enquanto o navegador envia `Origin: https://jorgesolla1313.com.br` — antes, mismatch → 403 "Requisição inválida." no autosave de estimativas (`POST /campanha/municipios/pledge-estimated-votes` e as 14 rotas do wrapper). Sem migration, sem UI, sem mudança de call site: superconjunto estrito da base legada (cross-origin continua 403; ausência de `Origin` continua aprovando). Spec novo `tests/unit/sameOriginRequest.unit.spec.ts` (matriz: túnel prod, fail-closed sem env, cross-origin, header encadeado, dev porta-diferente, TLS local); spec do wrapper intacto como pin. `NEXT_PUBLIC_SITE_URL` confirmado presente no stack de produção; smoke e2e do autosave de estimativas verde.
