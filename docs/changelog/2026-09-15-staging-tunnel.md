**Staging no túnel Cloudflare (2026-09-15):** `https://staging.jorgesolla1313.com.br` passou a responder pela edge Cloudflare (HTTP 200 em `/`, `/campanha/login` e `/admin`) apontando para o staging container do homeserver. O túnel `home` é remotely-managed (`cloudflared tunnel run --token`), então o ingress vive na API: inserido `staging.jorgesolla1313.com.br → http://localhost:1314` no config (versão 7, antes do catch-all `http_status:404`) e criado o CNAME `staging` → `991f49e4-…-cfargotunnel.com` (proxied) na zona `jorgesolla1313.com.br` — ambos via `CLOUDFLARE_API_TOKEN` de `~/Code/infra-solla/.env`; `docker restart cloudflared` aplicou a rota. Acesso direto por IP Tailscale (`100.119.220.31:1314`) continua não funcionando por design: os containers bindam só em `127.0.0.1` no homeserver. Runbook atualizado em `docs/ops/teqo-1313-deploy.md` §Staging (bootstrap step 5).
