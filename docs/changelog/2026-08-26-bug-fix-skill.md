**bug-fix (2026-08-26) — nova skill `/bug-fix` + subcomando `pnpm worktree fix` para correção de bugs de ponta a ponta com post-mortem:** a skill (`.agents/skills/bug-fix/SKILL.md` + `postmortem-template.md`, comando `.opencode/commands/bug-fix.md`) cobre o ciclo completo — reprodução → investigação de causa-raiz → fix (nunca sintoma) com teste de regressão → verificação (regressão + suíte + CI, e para bug de prod a confirmação do humano após o deploy manual) → análise de prevenção classificada em barata (implementar no mesmo PR) vs cara (documentada no post-mortem, vira Issue futura) → **post-mortem** em `docs/postmortems/<data>-<slug>.md` + entrada de changelog. Token-optimized: 4 sub-agentes (investigador, verificador, estrategista de prevenção, escritor do post-mortem) fazem as fases pesadas com contexto mínimo — o agente principal só orquestra e implementa o fix. `pnpm worktree fix [bug] [--stay] [--no-migrate] [--cheap|--pro|--zen|--go|--alibaba]` cria worktrees `fix/<bag>` ou `fix/<n>` sequenciais (namespace minúsculo novo, nunca colide com `next`/`plans/`/`work/`) com o mesmo provisionamento isolado dos demais; no terminal, o launch envia `--prompt "/bug-fix <bag>"` — a descrição do bug chega com a skill (aspas/barra-invertida removidas do bag: o xargs da camada shell não honra escapes; `opencodeLaunchDirective` ganhou `argument`, exclusivo do purpose `fix`); nenhuma Issue é claimada nem criada — o registro do bug é o post-mortem. Sem migration/dados/access/UI; specs novos no `worktree.unit.spec.ts` (namespace `fix/`, launch com/sem bag, sanitização, isolamento do `argument`).
