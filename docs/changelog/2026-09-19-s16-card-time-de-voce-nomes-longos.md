**S16 — Card "Time de você": nomes longos cabendo no banner (2026-09-19):** o banner azul do nome deixa de ter largura fixa — mantém a referência 509 do S15 quando o nome cabe e alarga só o necessário até o teto 1000 (≈40px de margem de cada lado; pode ultrapassar o banner vermelho 693), com o canto mais baixo em ≈436,5 sem cruzar o topo da janela da foto (y=439). A tinta máxima sobe de 470 para 961 e o cap mínimo legível desce de 56 para 40 (decisão do humano no gate, 2026-09-19): MARIA DA CONCEIÇÃO passa a caber e só os absurdos abaixo do piso (BARTOLOMEUCOSTAJUNIOR cap 38, ANTICONSTITUCIONALISSIMAMENTE cap 29, medidos com a Brexter real) seguem `too-long` com a mesma mensagem e CTA desabilitado — nunca corte silencioso. Núcleo puro ganhou `CardNameFit.ok.inkWidth` (medido no fontSize final), `CardBanner.maxWidth` (azul 1000; vermelho fixo 693) e `resolveCardBannerWidth(slot, inkWidth) = max(width, min(maxWidth, inkWidth + (maxWidth − maxInkWidth)))` (padding 39 = 1000−961 = 509−470); `drawCardBanner` recebe a largura resolvida e só o `fillRect` do banner azul muda — centro/y/altura/rotação, o banner vermelho, o modelo de nome e os outros 3 modelos ficam intocados. Unit dos 3 specs puros reescrito (fake 0,84em espelha a Brexter; 6 pares do design pinados no resolver; guard do canto/margens) e e2e do `frontend` com nome composto (`Maria Eduarda`) + sonda que mede o alargamento real (>700px sólidos de `#0061A5` numa linha) e o download de PNG. Sem migration/Consent/access; 100% client-side. Design gate DEGRADED (artefato do tier 3) com crítica final do `designer` no tier 1 (paridade certificada, sem divergências) e sign-off humano registrado no PR. Verificação: `pnpm gate:fast` (4049 unit verdes), e2e da superfície 9/9 e spec `frontend` completo executado; fronteira real-font não pinada no e2e por flakiness pré-existente da prontidão da fonte (evidência no impl plan, fora do escopo).
