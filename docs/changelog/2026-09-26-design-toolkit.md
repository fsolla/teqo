**DESIGN1 — Toolkit de design dos agentes e regras visuais vivas (2026-09-26):** o design do Teqo ganhou um toolkit vendorado em `.agents/skills/` (origem e licenças em `.agents/skills/UPSTREAM.md`): a suíte anti-slop do **Taste Skill** (`design-taste-frontend` v2, `gpt-taste`, `redesign-existing-projects`, `high-end-visual-design`, `minimalist-ui`, `industrial-brutalist-ui`, `stitch-design-taste` + `full-output-enforcement`), o review de acessibilidade/UX/perf **`web-design-guidelines`** (checklist da Vercel vendorado em `guidelines.md`, findings `file:line`) e a biblioteca de referências **`awesome-design-md`**. O **Playwright CLI** (`0.1.21`, instalado global via `npm i -g @playwright/cli`) entra como automação de browser token-efficient para captura/crítica de UI (`playwright-cli install --skills=agents` → `.agents/skills/playwright-cli/` + config `.playwright/cli.config.json`; artefatos de sessão em `.playwright-cli/`, adicionado ao `.gitignore`), com os MCPs Playwright/Chrome DevTools como fallback (cross-ref em `browser-testing-with-devtools`). Ficaram de fora apenas `image-to-code-skill` (o artefato hi-fi HTML do gate o supera), `taste-skill-v1` (legado) e as skills de `imagegen`/`brandkit` (exigem geração de imagem indisponível no stack). A doutrina `ui-design-html.md` ganhou a seção **Skills de design** (papel de cada skill, precedência `PRODUCT.md` → `DESIGN.md` → contrato do gate → skills de execução, e escopo por superfície: taste/estilo em persuade/read, desk `/campanha` em operate) e **o guardrail de marca do kit 1313 foi retirado**: o kit passa a ser acervo de ativos disponível — quirks técnicos de uso mantidos —, não camisa de força. `DESIGN.md` ganhou a §7 **Evolution**: é o dono vivo das regras visuais e deve ser atualizado na mesma mudança em que um padrão melhor for decidido (com o porquê); mudança material segue voltando ao gate humano. O `designer` (permission de edit agora inclui `DESIGN.md`; usa `gpt-taste` no frontier e `playwright-cli` + `web-design-guidelines` na crítica) e o `designer-campanha-solla` foram alinhados, assim como o trigger (e) do `execution-pipeline.md` e a linha de posse visual do `engineering-standards.mdc`. Nada muda no runtime do site.
