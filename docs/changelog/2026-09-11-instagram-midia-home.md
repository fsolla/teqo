**Capas do Instagram voltam a carregar na home e legendas longas não esticam mais o card (2026-09-11):** os cards do Instagram da seção "Acompanhe de perto" apareciam sem as capas e as legendas longas esticavam o card — a Graph API passou a servir a mídia de `*.fbcdn.net` (`instagram.<pop>.fna.fbcdn.net`), host fora do `images.remotePatterns` (que só cobria `*.cdninstagram.com` desde a entrega S3 de 2026-08-18, commit `a638a8ac`), e o otimizador respondia `400 "url" parameter is not allowed`; além disso, os patterns declarados como `new URL()` pinavam `search: ''` e o matcher do Next 15.4 exige igualdade estrita de `search`, rejeitando as URLs assinadas da Meta mesmo com o host certo. O fix (PR #940) troca os patterns do Instagram por objetos planos `{ protocol: 'https', hostname: '*.cdninstagram.com' | '**.fbcdn.net', pathname: '/**' }` e aplica `line-clamp-3` no título e `line-clamp-2` no subtítulo do `CampaignContentCard`; a regressão fica coberta por um guard unit que casa URLs assinadas reais contra o allowlist com o matcher do próprio Next (+ seam parser→allowlist) e por um e2e com legenda longa no stub do Instagram. Pré-requisito descoberto no gate: o `activityOverlay.unit.spec.tsx` era dependente da data (fixtures de agosto/2026) e quebrava a suíte unit full desde 01/09/2026 — sem o relógio fixo (commit `2dead731`) nenhum PR passaria, já que o wrapper de `changed` cai no full por um `--` extra repassado pelo pnpm. Verificação: unit full 2687 passed, int full 712 passed, e2e "Campaign home content section" 10/10 local, proxy real `200 image/jpeg` (antes 400), typecheck/lint/format/knip/cycles/build verdes no `pnpm push`; CI do PR a preencher e prod pendente (deploy manual + confirmação do humano).
