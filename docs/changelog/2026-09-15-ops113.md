**OPS113 — Agentes `designer` e `designer-degraded`: design hi-fi como fonte de verdade (2026-09-15):** a doutrina do gate virou `.agents/skills/plan-issue/ui-design-html.md` (rename via `git mv`) e o artefato `docs/plans/<slug>-ui-design.html` (+ `-ui-design-assets/*.svg`) passa a ser **fonte de verdade do port** — tokens/brand/shadcn esperados e o implementador porta classe-a-classe; teto mantido (JS mínimo de apresentação — modal/tabs/toggle —, sem lógica de negócio; claims só os aprovados no plano; `NEEDS ASSET`; LGPD/TSE; um CTA primário). Novos agentes `.opencode/agent/designer.md` (`mode: all`, `temperature: 0.7`, `model: openai/gpt-5.6-sol`) e `designer-degraded.md` (`mode: subagent`, `model: opencode-go/deepseek-v4.1-flash`), ambos com modos Criar/Criticar, visão nativa fail-closed e `permission.edit` fail-closed (`*` deny → `docs/plans/*-ui-design*` allow — nada em `src/`), provado por smoke em subprocesso. Ladder documentada (`sol → astra → deepseek-v4.1-flash DEGRADED → deepseek-flash DEGRADED inline`) com tier registrado no PR; tier degradado nunca certifica (sign-off humano). Parágrafo operacional em `docs/AGENT-OPS.md`. Consumidores do nome antigo ficam para OPS114; sufixo do painel, OPS116. Sem migration; nenhuma mudança em `src/`.
