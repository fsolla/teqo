**C159 — Sollinha na assessoria de comunicação (chat escopado ao acervo) (2026-09-14):** o papel `communicator` passou a ver e abrir o Sollinha na vertical `/campanha/comunicacao` (FAB, drawer e botão do header), com o chat **escopado ao acervo de falas** do C158 — pergunta de acervo devolve trechos com link para o Acervo, pergunta fora do escopo recebe negativa clara e educada, nunca erro técnico nem dado de campanha/eleitoral/município. O gate de superfície `canUseCampaignAssistant` (que escondia as três superfícies desde o C154) foi removido no dono `src/lib/campaignRoles.ts`; o escopo passa a ser por construção no registry: `buildAITools` entrega ao communicator (e a um papel desconhecido, fail-closed) apenas `calculate`, `buildCampaignLinks` e `findSpeechExcerpts`, e mantém as 15 tools para staff/leader — o modelo não vê o que não pode chamar, o que elimina a classe de `Forbidden`/vazio enganoso que tools sem gate produziriam (opção B de gatear 15 donos foi rejeitada). O prompt virou `buildAISystemPrompt(role)` no dono: a composição da assessoria derruba as afirmações de acesso a dados da campanha, as seções de tools de campanha e o `## Contexto eleitoral` (que materializava o quociente de 210.000), e ganha o bloco `## Escopo do seu acesso` + follow-ups só de acervo; staff/leader mantêm o texto byte a byte. Ajuste pontual no dono dos links (`assertDestinationAccess`): `leaderContacts` passou a ser negado para não-líderes com mensagem própria (antes vazava para o communicator um link que redireciona). Chips de abertura próprios de acervo (3, todos respondíveis por `findSpeechExcerpts`) e boas-vindas por papel no `CampaignAIChat` (prefixo "Olá! Eu sou o Sollinha" preservado). A sessão do chat em `sessionStorage` ganhou o papel que a escreveu e mismatch/legado sem papel viram conversa nova (fail-closed): um tab compartilhado não restaura mais a conversa de staff para a assessoria — risco direto ao guardrail. Sem migration, collection, Consent ou mudança de access; gate do dado segue `canReadSpeechCatalog` (C153/C158); leader lockdown, advisor, nav, rate limit 50/15min, `maxDuration 60` e limites de conversa intocados. Verificação: `pnpm gate:fast` (lint/format/typecheck + 305 arquivos/2956 unit), `knip`/`check:cycles`/`format:check` verdes; e2e `campaignSpeechAcervo` 6/6 (teste do communicator invertido para pinar shell + chips de acervo + ausência dos chips de staff) e superfície do chat (`campaignSollinhaContext`, `campaignAiChatOpeningChips`, `campaignAiLinks`, `campaignSollinhaWidth`, `campaignAiChatResize`) 19/19 local; manifest ganhou a entry `src/lib/sollinhaOpeningQuestions` → specs que pinam os chips.
