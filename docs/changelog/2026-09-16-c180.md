**C180 — Acervo cortes: dívida pós-C174 — select enxuto, teto do `id in` e paridade busca↔VM (2026-09-16):** três achados do `/simplify` do C174 atacados em `src/utilities/speech/`, sem mudança visual. (1) **Select enxuto + DTO estreito:** `loadSpeechCutsForSpeech`/`loadSpeechCutsForSpeeches`/`loadSpeechCutOriginSpeechIds` deixam de trazer a linha inteira do corte; novo `SpeechCutSummaryViewModel` (`lib/speechCut.ts`) entrega só `id`/`status`/`title`/`description`/`durationSeconds` — o que "Cortes desta fala" e o aninhamento da busca renderizam — reusando o fallback de duração e a normalização `status` desconhecido→`failed`, e o `select` espelha os campos do mapper. (2) **Predicado único busca↔VM (bug corrigido):** `speechMatchesSearchQuery` puro em `lib/speechSearch.ts` passa a ser o espelho do ramo textual do `where`; o `keywords` do VM vira case-insensitive como o ILIKE `%q%` do Postgres, então uma fala cujo keyword casa em caixa diferente deixa de ser marcada como "Fala de origem" indevidamente. (3) **Teto do `id in` da origem:** `SPEECH_CUT_ORIGIN_SPEECH_ID_LIMIT = 200` + `originSpeechIdsOfCuts` (dedupe + cap puro) limitam a query de origem e o `id in` do acervo, com degradação honesta (menos resultados origin-only, nunca resultado errado); gatilho de revisão ~5–10 mil cortes (C168). Sem collection, Consent ou migration; loaders seguem `user` + `overrideAccess:false`; `/corte/<id>`, o acervo depth-1 e o dialog/player intocados. Testes: unit de `toSpeechCutSummaryViewModel`, `originSpeechIdsOfCuts` (dedupe/cap/`limit: 0`) e `speechMatchesSearchQuery` (acento/caixa); int do loader com chaves exatas, do keyword case-insensitive e da origem opção B; e2e curado `campaignSpeechAcervo` + `campaignSpeechCut` verde.
