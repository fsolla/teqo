**C170 — Acervo: cortar sem teto — do mínimo de 5 s até o fim da fala (2026-09-16):** o teto de produto de 3 min some do código: `MAX_EXCERPT_SECONDS` removido da geometria (`speechExcerptSelection`), do `resolveExcerptRange` da action, da barra, do diálogo, do schema e do prompt da IA — o único limite passa a ser a `durationSeconds` da fala (fala inteira `[00:00, duração]` permitida, mínimo de 5 s mantido). A barra mostra `Mínimo 5s · sem limite — até o fim da fala` com `aria-valuemin/max` em `0–duração`, o diálogo mostra `<duração> · mínimo 5 s · até o fim da fala`, o erro do servidor diz `Selecione um trecho de 5 segundos até o fim da fala.` e o prompt da IA fala em `trecho da fala com 5 segundos ou mais`. Sem migration, sem Consent/collection/URL nova; gate do acervo, kill switch/publicação (C167/C168) e pipeline/ffmpeg (C169) intocados — corte longo que bater em timeout operacional falha honesto como hoje. Testes: units (`speechExcerptSelection`, `speechDetailPlayer` com `valuemax=duração`) + int (`speechCut`: mensagem nova, longo 0–240 aceito, <5 s rejeitado) atualizados; e2e `campaignSpeechAcervo`/`campaignSpeechCut` verdes sem spec novo.
