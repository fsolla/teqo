**Docs fora do scan do Tailwind (2026-09-26):** o `styles.css` ganha `@source not "../../../docs/**"` — a detecção automática do Tailwind v4 varria os designs HTML de `docs/plans/` e a classe `bg-[url('../../public/…')]` do design do álbum de fotos (C231–C234) virava um módulo relativo no CSS de produção, quebrando `next build` na main desde 4bf0c0c0 (o `verify` do deploy falhou e o preflight pulou os deploys seguintes). Artefatos de design não alimentam mais o CSS publicado; o build local e o do CI voltam a passar com a classe root-absolute que os componentes reais já usam.
