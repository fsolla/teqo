**C163 — vídeo ancorado no trecho da Câmara, com folga (2026-09-15):** o `speechAt` da API (14:08) é o horário do _slot_, não o início real da fala — os segmentos do trecho mostram o orador anterior nesse momento, e o 1º segmento do clipe é o presidente concedendo a palavra (o trecho gerado a partir do `excerptTMs` começa às 14:10:45). O relatório volta a ancorar no trecho oficial (`youtubeExcerptStartSeconds` = `excerptTMs` − início da sessão) e aplica **folga de 20s** no link (`YOUTUBE_LEAD_IN_SECONDS`) para não abrir já dentro da fala; na fala 641: `t=625s`. A função `speechStartOffsetSeconds` (hipótese errada de que `speechAt` fosse o início) foi removida do dono `src/lib/speechVod.ts`. Unit 3118/3118, `tsc`/eslint verdes; snapshot reextraído e PDF regerado.
