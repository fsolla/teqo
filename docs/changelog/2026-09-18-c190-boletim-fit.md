**C190-fit — boletim de uma página cabe por medição (2026-09-18):** o primeiro run real do `/dossie-solla-tema` (Educação) entregou o dossiê mas **não** o boletim: com o acervo real (15 falas) o one-pager media 3502px contra 1133px úteis. Três correções no dono, sem geminar pipeline: (1) **acervo é painel de fontes, não achado** — os fatos de fala entram no `bulletinFacts` marcados `sourcePanel: true` (`dossieBlocks`) e o `buildBulletin` os mantém **contados** no "e mais N fatos com fonte" mas **fora das vagas impressas** (destaques e "E mais"), que passam a ser só findings com fonte; (2) **fit por medição** — `emitHtmlPairPdf` ganha `onBulletinOverflow` (espelho do `onDossierOverflow` do C188), e o builder do tema re-renderiza com `printLimit` dois fatos menor até a folha caber, sempre declarando o resto (fail-closed se o callback não encolher); (3) **contador C188 no boletim `subject`** — tema/instituição não exibiam o "e mais N" que o C188 exige (o renderer de município já tinha); o rodapé ganha `copy.remaining` por descritor ("no dossiê da área/da instituição"). O contrato do tema (skill + subagente) passa a documentar `brief` (title ≤80 / note ≤120, obrigatório no item publicado) e `summary` (C188) — a copy impressa que faltava deixava o `answer`/`details` integrais irem para as páginas. Specs novos em `dossieTheme.unit.spec.ts` (painel contado fora dos destaques; `printLimit` encolhe e declara o resto); specs existentes de município/instituição seguem verdes (testes 3772/3772). Sem schema, migration, URL pública, Consent ou LGPD — scripts/skill/testes apenas.
