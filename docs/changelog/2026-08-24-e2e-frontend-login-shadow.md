**E2E-FRONTEND-LOGIN-SHADOW (#811) — fechada como satisfeita por #767:** o "shadow local de `adminHeaders`" citado em `tests/e2e/frontend.e2e.spec.ts` (login REST do usuário compartilhado sem advisory lock) não existe mais no HEAD. O commit `d0161f6d` (_test(e2e): unificar logins do usuário de teste no helper adminHeaders com advisory lock_, Closes #767), mergeado 2026-08-24 (após a abertura da #811 em 2026-08-23), removeu o login inline e o substituiu pelo helper `adminHeaders` de `tests/helpers/adminApi.ts` (que carrega `withAdvisoryLock(ADMIN_LOGIN_LOCK_KEY, …)`). Verificação por grep no estado atual: `users/login` aparece só em `tests/helpers/adminApi.ts`; nenhum construtor custom de cookie `payload-token` fora do helper (exceto `campaignHomePixel`, intocável por contrato do #760). Todas as 14 chamadas em `frontend.e2e.spec.ts` usam o helper travado. Nenhuma mudança de código — apenas registro.
