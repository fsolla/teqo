**E2E-DEBT-S-GATE — consolidar o gate S: (stream settle) num helper compartilhado de fixtures (2026-08-24):** extração de `waitForStreamSettled` em `tests/e2e/fixtures/campaignE2EFixtures.ts` (default 15 s, pass-through de `options`) e migração de 21 call sites em 5 specs — campaignMunicipalities (11, incluindo o helper local `settleMunicipalityStream` removido), campaignPeople (4), campaignPermissionProfile (2), campaignMunicipalityDeliberation (1, helper local `settleStream` removido, timeout explícito preservado) e campaignUpdatesMobile (3, helper local `settleStream` removido, timeout explícito preservado). Polls complementares (leadershipLink, footer "1 município encontrado" 30 s, chips `toHaveCount(0)`) e outros gates (`waitForRouterSettled`, `meta-pixel`, `networkidle`) intocados — padrão OPS83 "settle + poll específico" preservado. Sem mudança de product code.
