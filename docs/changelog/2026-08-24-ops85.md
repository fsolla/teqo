**Recently resolved (2026-08-24):** **OPS85** (Issue #830) — eliminar a classe de conflito de merge no changelog: o agregado commitado `docs/CHANGELOG-AGENTS.md` (171 blocos reescritos no mesmo anchor a cada entrega paralela) morre — `docs/changelog/<data>-<id>.md` vira o **único registro commitado** e a leitura "Recently resolved" é gerada sob demanda: `pnpm changelog:build` grava o artefato **gitignored** no mesmo path, `pnpm changelog:read` imprime no stdout, ambos com seed do snapshot congelado **`docs/CHANGELOG-AGENTS-HISTORY.md`** (cópia integral do agregado na data da migração, incluindo as 106 entradas pré-OPS44 que só existiam lá; nunca é editado). Guard de CI `docs-guards` reescrito: entradas additions-only (imutável), HISTORY congelado (só a criação é permitida) e agregado proibido no diff (a remoção única desta entrega foi a exceção que criou o HISTORY); o multiset do agregado (perda de linha) morre — a garantia de não-perda vira additions-only + pós-condição do gerador ("toda entrada parseada aparece no agregado"). O step "Aggregate is up to date" sai do `ci-pr.yml` e do `gate-ci.mjs` (o `--check` vira sanidade local opcional); escape `changelog-rewrite:` permanece CI-only (agora também cobre correção do HISTORY). Contratos atualizados: AGENTS.md, AGENT-OPS.md (item 4 + "Registries compartilhados" + seção nova **"Decisões arquivadas"**), execution-pipeline (entrada sem build), PR template, roadmap, agent-pool-prompt, engineering-standards, agent-pr-workflow, AGENTS-infra. **Docs agent-facing NÃO migram para fora do repo — arquivado com evidência:** o conflito era do agregado, não das entradas por-arquivo; alvo registrado se um dia houver custo novo: Wiki.js. Sem migration/access/Consent/UI/schema. Esta entrega é o tracer bullet do fluxo novo (entrada commitada, agregado não).
