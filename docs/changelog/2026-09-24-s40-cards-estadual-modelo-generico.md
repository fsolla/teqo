**S40 — Time do estadual: a arte de modelo genérica entregue pelo humano (2026-09-24):** o tile e o placeholder de pré-escolha do modelo `time-do-estadual` deixam de ilustrar o JULIO PINHEIRO 13999 do S30 e passam a ser a arte genérica entregue pela campanha (`SEU ESTADUAL` + duas silhuetas), **byte-a-byte no MESMO caminho público** `/cards/modelo-time-de-voce-com-estadual.jpeg` (1080×1440 jpeg sRGB, sha256 `1f5dc8212662697149f0acac5701e8de58c53a86f5af316535736dccb7a53c7b`): a cópia é feita pelo dono (`pnpm build:state-deputy-card-assets` copia o asset aprovado de `docs/plans/cards-estadual-dobradinha-ui-design-assets/` e o skip por sha deixa as 106 webps intactas) e o unit do catálogo passa a pinar o sha literal novo dos dois lados (fonte aprovada × arquivo público). Na pós-escolha sem foto, a silhueta desenhada sai — decisão do gate de 2026-09-24 («deixa vazio até adicionar a foto do usuário»): `CARD_VISITOR_SILHOUETTE_FILL`, `drawCardVisitorSilhouette` e o subject `{kind:'silhouette'}` foram **removidos** do `src/lib/cardRender.ts`; o `renderTeamCard` aceita `subject: TeamCardSubject | null` (tipo achatado, sem o discriminante morto) e compõe base → overlay → banner `TIME DE` → banner do nome, então a janela do visitante fica vazia (a arte do estadual escolhido como está) até a foto entrar, como o `Time de você` — a pré-escolha segue com as duas silhuetas da própria arte entregue (não são desenho). Pins acompanham a remoção: unit `cardRender` com o novo teste "sem foto" (base + overlay + banners, `transform: null`, zero ops de `beginPath/arc/ellipse/rect/fill`) e e2e S30 com o probe `(582,540)` = `[239,223,196,255]` (a cor da arte do JULIO na janela; antes `[0,26,66,255]`). Sem migration, sem superfície/copy/estado/analytics novo; fluxo, seletor, catálogo das 53, as demais artes e o evento S32 intocados. Crítica final do `designer` (trigger c) **certificada** em tier primário (`Design tier: openai/gpt-5.6-sol`), zero ajustes.
