**B194 — E2E do chat: extrair helper compartilhado do mock SSE (formato v7) e migrar specs legados (2026-08-24):** helper `sollinhaChatStreamBody(delta)` + `mockSollinhaChat(page, delta)` em `tests/e2e/fixtures/campaignE2EFixtures.ts` (ao lado de `checkRadixWhenHydrated`/`expectPostResponse`) centraliza o corpo SSE v7 canônico (start/text-start/text-delta/text-end/finish, id `t1`, finishReason `stop`); os 6 specs de chat (`campaignSollinhaContext`, `campaignAiChatResize`, `campaignAiLinks`, `campaignAiChatFollowUps` — este com mock dinâmico renomeado para `mockSollinhaChatDynamic` que reusa o body, `campaignAiTranscribe`, `campaignAiChatOpeningChips`) passam a usar o helper, migrando os 2 specs legados do formato `{"type":"text"}` (que falhava o schema v7 do cliente) para v7; eliminada a duplicação do corpo SSE entre specs, sem mudar comportamento observado nem timing.
