**Deploy destravado — envs do standalone no `next build` e importmap canônico no build da imagem (2026-09-12):** nenhum build de imagem completava desde o OPS99 (commit `493f4ba9`, 27/08 19:05 UTC; prod ficou no build anterior, `4d6533c04`) e o "sucesso" das 05:22 foi stale run saindo 0 em falso verde (main avançou durante o verify). O dispatch das 13:34 (run 34696817841) falhou no `pnpm exec payload generate:importmap` com `Error: This module cannot be imported from a Client Component module` (`server-only`): o OPS99 encadeou o gerador inline sem o `--conditions=react-server` dos scripts donos (`package.json` → `generate:importmap`/`migrate`/`build`, `scripts/predev-importmap.mjs:41`) — o `server-only` de `src/utilities/access/campaignStaffAdvisors.ts:5` (via `src/payload.config.ts:49` → `utilities/campaignAccess.ts`) resolve para o módulo que lança. O #953 (commit `52a2ef6d`) trocou o gerador por `pnpm generate:importmap` e tornou o stale run `fatal`; o deploy seguinte (run 34701266297, 15:07 UTC) passou o importmap mas expôs a segunda falha latente do mesmo RUN: prefixos de shell (`VAR=x cmd1 && cmd2`) valem só para o primeiro comando, então `NEXT_OUTPUT_STANDALONE=1` (`next.config.mjs:8`) e `NODE_OPTIONS` ficaram presos no gerador, o `next build` completou (399s) sem `.next/standalone` e o stage `runner` morreu em `COPY --from=builder /app/.next/standalone ./` → `"/app/.next/standalone": not found` (Dockerfile:73). Este PR (#952, sobre o #953) move `NEXT_OUTPUT_STANDALONE=1`/`NODE_OPTIONS` para o comando do `next build` e estende o guard de invariantes em `tests/unit/deployScript.unit.spec.ts` (script canônico + flags no lado do `next build`, RED com o Dockerfile do OPS99 e com o do #953). Verificação: produção-equivalente local gera o importMap com `S3ClientUploadHandler` e `.next/standalone/server.js`; `pnpm gate:fast` verde (lint + typecheck + unit) e gate:ci local verde (int 78 files / 712 tests); post-mortem completo em `docs/postmortems/2026-09-12-deploy-build-importmap.md`. Sem migration, sem access/Consent. Deploy de prod segue manual — dispatch do humano após o merge, confirmação pendente.
