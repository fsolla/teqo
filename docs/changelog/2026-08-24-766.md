**#766 — PetitionForm: Lead dentro do try não transforma mais assinatura commitada em erro (2026-08-24):** guarda interna try/catch em volta de `trackMetaLead` no `PetitionForm.onSubmit` (src/components/PetitionForm.tsx), espelhando o padrão S10 do `CampaignNewsletterForm` — falha de tracking (ex.: `crypto.randomUUID` em contexto não-secure) não vira mais "Falha ao enviar assinatura" com assinatura já commitada, eliminando retry duplicado e re-disparo de `Lead`; spec novo `tests/unit/petitionLead.unit.spec.tsx` (4 testes: Lead único em captura ok com pixel, sucesso preservado quando tracking lança, nenhum Lead sem pixel, nenhum Lead em falha de submit); plano `docs/plans/petition-form-lead-guard-impl.md`.
