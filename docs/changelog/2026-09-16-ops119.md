**OPS119 — worktree plan abre sem auto-enviar o `/plan-issue` (mensagem opcional na linha) (2026-09-16):** `worktree plan` sem argumento agora cria a sessão persistente **sem driver** (mesmo contrato observável do `new`) — o humano digita o `/plan-issue` com o contexto que quiser; `worktree plan "<mensagem>"` segue auto-submetendo uma única `/plan-issue <mensagem>` e usa a mesma mensagem como bag do branch (`plans/plan-issue-<slug>`), espelhando o `fix <bag>`. A regra vive no dono do ciclo (`purposeInvocation` em `scripts/lib/agent-session.mjs`): o ramo `plan` reusa a sanitização do `fix` (`["\\]` + `trim`) e devolve `null` quando o bag fica vazio (divergência deliberada do `fix`, cujo bag vazio ainda submete o comando bare). `opencodeLaunchDirective` (`scripts/lib/worktree.mjs`) passa a emitir `--argument` para `fix || plan`, `cmdPlan` (`scripts/worktree.mjs`) repassa `argument: bag` e o aviso "sem comando/issue válida" do `cmdStart` isenta `plan` (driverless por desenho). `next`/`new`/`fix`, `--stay` e o comando `/worktree` intactos; supersede o auto-envio incondicional do OPS31 (#593). Sem migration, sem schema, sem UI. Testes: `agentSession.unit.spec.ts` e `worktree.unit.spec.ts` repinados (plan sem bag → `null`; plan com bag → `--argument` sanitizado; `new` segue ignorando o argumento); `pnpm gate:fast` verde e walkthrough manual do launch registrado no PR. Documentação viva atualizada em `scripts/worktree.mjs`, `.agents/shell/worktree.sh`, `.opencode/commands/worktree.md`, `.agents/skills/worktree-next-issue/SKILL.md`, `AGENTS.md` e `docs/AGENT-OPS.md`.
