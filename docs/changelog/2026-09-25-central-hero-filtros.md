**Central de Conteúdos pública — revisão visual do hero e correção dos filtros (2026-09-25):** a rota `/conteudos` ganhou o polimento aprovado no artefato `docs/plans/central-conteudos-hero-polimento-ui-design.html` (cenas 01–04, designer). O hero passa a ter textura do `pattern-shapes.png` do kit, placa azul translúcida com o `numero-negativo.png` em maior escala no desktop, faixa amarela inferior de fechamento e o 1313 também no mobile (antes o ativo desaparecia no celular), mantendo a copy contratual e o único objetivo de voto; o separator `border-t` separa a barra de busca do cabeçalho dos resultados. Os filtros deixam de usar o glifo textual `⌄` (tinta no rodapé da caixa de linha, lida como subscrito) e passam a usar o `ChevronDown` do lucide centralizado por `inline-flex + items-center`, girando no menu aberto; os `<details>` das facetas vazias agora compartilham `name="content-facet"` (accordion exclusivo nativo, sem JS) — abrir um filtro fecha o outro — e o dropdown ganhou alvo de 40px e largura mínima de 192px. O board de peças ficou fora do redesenho. Cobertura: e2e `frontendConteudos` 12/12 (a spec S37 mede o centro do chevron contra o centro do chip com tolerância de 1px e asserta um único `details[open]` após abrir o segundo menu — o comportamento antigo foi reproduzido em produção antes do fix, `open after second click: 2`), `pnpm gate:fast`, `format:check`, `knip` e `check:cycles` verdes; sem migration/access/Consent.
