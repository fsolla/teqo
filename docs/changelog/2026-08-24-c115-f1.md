**C115-F1 (#694) — corrida do snapshot de eventos com compare-and-swap + hermeticidade da suíte int documentada:** o snapshot `lastSeenEventIds` do motor Google Calendar era gravado last-writer-wins (`recordSyncState` recarrega e sobrescreve) — duas passadas sobrepostas (webhook vs hook de atividade em instâncias distintas) podiam fazer a visão antiga gravar por último, o id do evento sair do snapshot e a remoção permanente feita pelo usuário no Google virar recriação sem convergência. A gravação agora passa por `recordLastSeenSnapshot` (`googleCalendarSync.ts`), update condicional `where: { id, updatedAt = token }` capturado num reload NO TOPO de `runSyncPass` (depois do channel-ensure — cujas escritas fazem parte do baseline da própria passada — e antes das leituras que o snapshot resume): o MESMO doc alimenta as decisões D2–D4 (fecha também a janela basis-vs-token apontada no /simplify) e serve de guard CAS; em miss (`docs.length === 0`) a visão contaminada é descartada silenciosa (debug log, never-throw) e a passada seguinte cura sem recriar. Sem migration (o JSON não muda de forma); stamps `lastSuccessAt`/canal permanecem LWW (comutativos). Teste int novo coreografa a corrida de forma determinística (promise gates, zero sleeps): passada B estacionada dentro de `listEvents` com view pré-X, X nasce no meio, A completa e grava `{x}`, remoção permanente, B termina → CAS de B falha → terceira passada cancela (`created === 0`, `reverseEdits === 1`); RED confirmado contra o código anterior (recriava). Header de `googleCalendarSync.int.spec.ts` documenta a convenção F2 de hermeticidade (escopo via `activityWhere` com prefixo próprio; cleanup por teste nos irmãos já correto); helper compartilhado segue fora até a 2ª spec sensível à janela (DRY < 3). Débito único do triage: flip de linha de config entre specs paralelas (loader `limit:1` sem sort) — pré-existente, defer com gatilho no impl plan.
