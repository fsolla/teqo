**C163 — Relatório de cidade: coluna Título da seção 11 com largura fixa e maior fatia (2026-09-15):** a tabela "Notícias internas e imprensa local" era renderizada sem larguras, então o layout automático do navegador dava à coluna **Link** a maior parte da largura — a URL crua, sem espaços, virava um token gigante e a tabela estourava a página (medido: tabela de **2.261 px** e coluna Link de **2.052 px**, com o **Título** espremido a 64 px). Agora as colunas têm larguras explícitas (`Data 9 · Veículo 14 · Título 49 · Link 28` %), o que torna a tabela `table-fixed` e habilita o `overflow-wrap: anywhere` do CSS: a URL quebra dentro da célula e o **Título** — o texto mais longo — recebe a maior fatia. Decisão persistida no script (`buildNewsSection`) e na skill (`relatorio-cidade`), com o mesmo racional. Sem mudança de contrato de dados nem de conteúdo: só layout, verificável reconstruindo o HTML e medindo as colunas.
