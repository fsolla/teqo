**C191 — Barra de variação (delta) no gráfico de Instagram (2026-09-19):** a peça por tipo de equipe de saúde precisava mostrar base e extensão (2020 → 2026) numa barra por categoria, e o template só tinha barra/coluna/linha/âncora/duas séries; pedido da comunicação em sessão direta (worktree `work/20`, sem claim). (1) **Design**: sexta variante no `docs/plans/graficos-dados-instagram-ui-design.html` (CENA 02C), aprovada por sign-off humano em 2026-09-19 com o tier degradado registrado (quota do designer frontier indisponível): barra zero-based com base 0→inicial em `#a8a29e` e extensão inicial→final em `#d6d3d1` (tick de 3 px `#78716c` no degrau), dois valores em gutter tabular ("2020" e "jul/2026") e sem valência — `#c51414` fica fora do plot; 7 categorias inclusive no story (sem top-N, que apagaria a manchete "quatro dos sete"). (2) **Contrato**: `chartType: 'delta'` com `rows: [{ label, initial, final }]` + `startLabel`/`endLabel`; tabela de 3 colunas lida por `--type=delta` (xlsx/csv/tsv/md/txt) com o cabeçalho nomeando os dois períodos; fail-closed para retração (sugere a linha de 2 séries), valor negativo, final ≤ 0, mais de 7 categorias, medida ausente e `--highlight` (o par de tons é fixo). (3) **Renderer**: `deltaBody` portado classe-a-classe do design (`.delta-grade`, `.delta-head`, `.delta-row`, `.delta-label`, `.delta-track`, `.delta-base`, `.delta-ext`, `.delta-value`, `.delta-period`), com os ritmos de feed/square/story e o kicker "Variação no período"; sigla em negrito antes do "·". (4) **Docs**: contrato do `chart-spec.json`, exemplo de CLI e guardrail da variante no `SKILL.md`. (5) **Testes**: unit do parse (tabela com dois períodos, medida ausente flagrada), do validate (retração, highlight, teto de 7, story sem corte, medidas inválidas) e do render (gutter, ordem por final, flat sem `delta-ext`, proporções 92,1%/7,9%, sem vermelho no plot, story com as 7). Entrega final: `docs/research/graficos-instagram/equipes-saude-vdc-2020-2026-feed.png` (7 tipos do CNES sem a EAP; "Quatro dos sete tipos de equipe de saúde seguem sem ampliação desde 2020").
