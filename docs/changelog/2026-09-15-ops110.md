**OPS110 — runs longos de agente sobrevivem ao terminal: attach/detach por `opencode serve` compartilhado (2026-09-15):** o launch do terminal (`worktree next|plan|new|fix`) deixou de abrir um TUI local dono da sessão — `opencodeLaunchDirective` (`scripts/lib/worktree.mjs`) agora emite `launch node scripts/agent-session.mjs start --purpose=<P> --dir=<D> --model=<M> [--issue=<N>] [--argument="<bag>"]` (o mapa purpose→skill mudou de casa para `scripts/lib/agent-session.mjs`, `purposeInvocation`; a mecânica do `.agents/shell/worktree.sh` ficou intocada — 1 linha `launch` tokenizada por xargs). O novo `scripts/agent-session.mjs` (`serve|start|attach|stop|list [--json]`, `pnpm agent:session`) sobe/reaproveita o `opencode serve` **compartilhado em loopback `127.0.0.1:4199`** (`TEQO_AGENT_SERVER_HOST/PORT`; bind fora do loopback exige `OPENCODE_SERVER_PASSWORD`, fail-closed; nunca `--mdns`), cria a sessão endereçável (`POST /session?directory=…`), dispara o **driver destacado** `opencode run --attach … --auto --command <skill> -- <args>` (é o `--auto` dele que auto-aprova permissões enquanto o run vive) e **anexa** o TUI `opencode attach -s <sessionID>`. Fechar o terminal/TUI desconecta — o run continua (verificado ao vivo na 1.18.31: SIGKILL no cliente e `tmux kill-session` não abortam; `POST /session/{id}/abort` interrompe e o driver sai); encerrar é ato explícito (`agent:session stop` = abort + fim do driver + `stoppedAt`). Sessão/endereçamento ficam em `~/.local/state/teqo/agent-sessions/<slug>.json` (`TEQO_AGENT_SESSION_DIR`/`XDG_STATE_HOME`; campos `version, code, issue, purpose, branch, dir, sessionID, url, model, driverPid, logPath, startedAt, stoppedAt`) e `list --json` entrega `{version, server, sessions:[…status]}` — o contrato que o painel OPS109 consome. `start` reusa o run existente quando o driver vive ou a sessão está `busy` (nunca dois runs no mesmo branch; `--new` força) e `worktree kill` encerra a sessão antes de destruir o worktree (estado/lock removidos — sem sessão órfã). Unit novos: `tests/unit/agentSession.unit.spec.ts` (paths/overrides, fail-closed de bind, argv do driver/attach com separador `--`, mapa purpose→comando, parse/serialize, payload do `list --json`; RED→verde) e `tests/unit/worktree.unit.spec.ts` atualizado (diretiva nova). Docs: seção "Sessões persistentes" em `docs/AGENT-OPS.md`, bullet em `AGENTS-infra.md`, `AGENTS.md` e `.opencode/commands/worktree.md`. Sem migration/UI/access/Consent/e2e (smoke ao vivo em dir de scratch: detach com run vivo, reattach com replay, `stop` interrompendo, `list` `working→idle→stopped`).
