**Audit Pass 6 — P6-M909 (#909) — desarme determinístico do auto-merge da /testing-audit:** miss colhida no Pass 6 (o desarme dependia do agente lembrar a ordem; a 1ª noite mergeou sozinha — OPS96). Novo `scripts/testing-audit-disarm.mjs` (plain Node, shape de `github-pr.mjs`): `--head/--title/--body-file [--draft-on-failure]` cria Ready → desarma → verifica (atômico) e `--pr N` cobre o rearm pós-`synchronize`; exit 0 só com `autoMergeRequest === null && !isDraft`, JSON de status impresso para o relatório, `--draft-on-failure` converte a draft (veto estrutural) e sai 1, e `GITHUB_TOKEN` ausente morre antes de qualquer chamada (fail-closed por construção). Dono estendido: `github-api.mjs` ganha `getPullRequestAutoMergeStatus` (GraphQL: `autoMergeRequest`/`isDraft`/`mergeable`/`mergeStateStatus` + node `id`) e `convertPullRequestToDraft` (mutation), reusando `ensureAutoMergeDisabled`/`createPullRequest`; pins novos em `githubApi.unit.spec.ts` (status, draft, idempotência read-first). Fiação classe 3: Fase 4/5 da skill /testing-audit reescrita para um único comando no passo de criação (a sequência manual `gh pr merge --disable-auto` saiu do caminho canônico) + pin `testingAuditDisarmConventions.unit.spec.ts` que falha o build se a skill parar de referenciar o script; ledger em `docs/GUARDRAILS.md`.
