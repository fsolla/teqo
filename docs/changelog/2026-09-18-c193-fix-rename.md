**C193 (fix) — restaura `main` após rename concorrente com o C192 (2026-09-18):** o rebase do merge do C193 renomeou `canReadSpeechCatalog` → `canReadCommunicationCatalog`, mas o C192 (mergeado minutos antes) adicionou novas referências ao nome antigo em `src/utilities/speech/speechPageData.ts` (import + gate do modo tema). O merge rebase juntou as duas mudanças sem conflito textual e o `verify` do deploy pegou a quebra (`TS2305`); a branch do PR estava baseada antes do C192, então o CI do PR não via o arquivo. Fix mecânico: as 2 referências passam ao nome novo. `pnpm gate:fast` verde (364 arquivos / 3867 testes) + int `speechAcervo`/`reel` 22 verdes.
