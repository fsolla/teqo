**S21 — Jingles de Jorge Solla no site público: ouvir e baixar (2026-09-20):** o mandato ganha uma casa oficial para os jingles — a página pública `/jingles` (tema `campaign-site`, portada classe-a-classe do artefato aprovado `docs/plans/jingles-site-publico-ui-design.html`) lista os jingles publicados em grid de 3 colunas no desktop e coluna única no celular (capa quadrada com gradiente, play amarelo de 56px com sombra 3D, título, estado "Pronto para tocar"/"Em reprodução", trilha de progresso e Tempo/Duração reais, "Baixar MP3" com alvo de 44px e o nome do arquivo), com header próprio (marca completa + selo "Jingles oficiais"), hero "A trilha da nossa caminhada" e o rodapé eleitoral compartilhado. O player é um único client component (`JinglePlayer`): cada card tem seu `<audio preload="none">` — abrir a página **não** baixa áudio, comprovado no e2e —, tocar um pausa os demais (com o catch do `play()` superado preso ao próprio card) e o download usa `href`+`download` da mídia same-origin com nome legível derivado do slug (`jorge-solla-1313-<slug>.<ext>`; `WAV` fica fora do site). Conteúdo gerenciável no CMS: collection nova `jingle` (título, slug único auto-slugificado com validação de não-vazio, capa e áudio pela `media` existente — sem collection paralela —, `order` opcional e `published` como kill switch), escrita por `canManagePublishedContent` e leitura anônima fail-closed por um `publishedOrPanelAccess` novo no dono do access (`access/shared.ts`), que passa a servir também o `ShareLink`. Com zero publicados a página responde 200 com estado honesto (`noindex,nofollow`, ícone de nota cortada e "Voltar ao início"), o selo do header some e o link "Jingles" do rodapé desaparece — `hasPublishedJingles` deriva da mesma lista renderizável e alimenta home, cards e a própria página via prop (o rodapé segue um leaf síncrono; leitura cacheada pela tag `jingles`). Leitura em `unstable_cache` depth 0 com as mídias resolvidas por `getCachedDocumentById`: trocar o **arquivo** de uma capa/áudio no admin atualiza o card sem editar o jingle (revalidação por `revalidateJinglesListing` nos hooks `afterChange`/`afterDelete`), e o segmento estático novo fica reservado contra colisão com share links (`SHARE_LINK_RESERVED_SLUGS`, drift test fail-closed). Migration aditiva `add_jingle` (FKs para `media`). Testes: 15 unit novos (helper/mapper + RTL do player cobrindo play/pausa, exclusividade, rejeição do `play()` e relógio/progresso), 9 int (slug, capa/áudio obrigatórios, access fail-closed para anônimo e `campaignUser` em create/update/delete × editor, ordenação `order`+título, resolução e troca de mídia, revalidação da tag) e e2e `frontendJingles` novo (lista + badge + link do rodapé, zero request de áudio antes do play, um por vez, `suggestedFilename` do download, kill switch 200 vazio ↔ indexável) — pinado no manifesto da área e no conjunto curado OPS86 (a migration torna o PR high-risk), com fixture MP3 mínima commitada em `tests/fixtures/`. Verificação: `pnpm gate:fast` verde (4208 unit), int 892 verdes, e2e local selecionado 41 verdes (família `frontend` + `frontendJingles` + `campaignSpeechCut`), `pnpm build` com `/` e `/jingles` estáticas, e crítica final do `designer` **CERTIFICADO** contra o app renderizado (390/1280, tocando e vazio).
