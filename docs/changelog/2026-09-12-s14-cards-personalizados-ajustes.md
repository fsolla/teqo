**S14 — cards personalizados: ajustes de fidelidade e experiência (2026-09-12):** refinamento do funil entregue no S13, sem fluxo novo. A home passa a abrir o MESMO compositor ali mesmo: a seção `Faça parte / Mostre que você está com Solla` renderiza a ilha `CardsStudio` (modal no desktop, drawer no mobile), os três tiles viraram botões e o CTA `Criar meu card` saiu; nada navega e fechar devolve à seção (foco de volta ao tile). A seção subiu para imediatamente antes de `Receba as novidades da campanha` (depois de `Nossa história`), mantendo a captura de contato como último bloco de conversão. O nome no card gerado agora alinha à esquerda com a linha `#SOU` (borda x=213 no canvas de 1080) em uma e duas linhas, com o fit de tamanho/quebra intocado: `NAME_CARD_SLOT.centerX` virou `leftX` e o desenho do nome foi extraído de `renderNameCard` para um `drawCardName` puro (`textAlign='left'`), reusado pelo tile do modelo de nome — que deixou de ser um placeholder HTML em Exo2 e passou a desenhar `SEU NOME` com o mesmo pipeline real (fit + `drawCardName`) num canvas transparente sobre a base otimizada, então o tile não pode divergir do resultado. No compositor, o vão interno de 32px do dialog sumiu (`p-0 sm:p-0` no call site; o `sm:p-8` base sobrevivia ao twMerge) preservando borda fina e raio, e o texto de ajuda `Arraste para posicionar e use os controles para aproximar ou ajustar.` ficou legível nos dois shells via `--muted-foreground: var(--campaign-muted)` no tema `campaign-site` (~6:1 sobre branco; antes herdava o near-white do `:root`). Limpeza: a variante `link` do `CardModelGallery` e o helper `cardModelHref` saíram (sem call site). `/cards` e o deep-link `?model=` seguem intactos; sem migration, sem Payload/Consent, sem PII em rede. Unit de `cardModels`/`cardRender` atualizado (+ `drawCardName`) e e2e do spec `frontend` reescrito: ordem da home, tiles-botão, compositor abrindo sem navegar, alinhamento à esquerda conferido por pixel na faixa do nome (card e tile), além dos fluxos canônicos de `/cards`, `?model=` e download PNG. Plano: `docs/plans/cards-personalizados-ajustes-impl.md`.
