**S19 — Cartão de compartilhamento: imagem no origem do deploy (2026-09-19):** o `resolveShareLinkOgImageUrl` passa a absolutizar a mídia (`/api/media/file/…`, o proxy servido pelo próprio app) contra `resolveDeploymentOrigin` — novo em `src/utilities/seo.ts`, com `NEXT_PUBLIC_SITE_URL` na frente (o build de produção o inlina, mesmo carrier do `isStagingSite`) e fallback no `siteUrl` do global `metadata` —, nunca contra o domínio canônico do global. Motivo: o global aponta para `https://jorgesolla.com.br`, hoje servido pelo WordPress antigo, que responde 404 no caminho da mídia; o crawler do WhatsApp lia título/descrição do HTML de `jorgesolla1313.com.br/plenaria-cultura` mas não encontrava a imagem, e o cartão saía só com texto (caso real da Plenária da Cultura de 19/09). Canonical e `og:url` seguem a cadeia global-first documentada em `docs/plans/build-resiliente-metadata-ausente.md`; só a URL do asset muda. Cobertura: unit do novo resolver (env vence, fallback, vazio) e int pinando o env sobre um `URL` de global estrangeiro. Sem migration, Consent ou access.
