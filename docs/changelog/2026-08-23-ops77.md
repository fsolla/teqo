**OPS77 — flip pós-merge falhava: workflow setava só `GH_TOKEN`, script lia `GITHUB_TOKEN` (2026-08-23):** débito registrado no fechamento da E1 (#765, PR #782): o flip `issue-done-on-main-merge.yml` → `scripts/issue-transition-on-merge.mjs` RODAVA e FALHAVA com `Sem token para a API do GitHub — defina GITHUB_TOKEN` em todo merge em que o evento `closed` disparava (run 32651174993 do PR #782; mesmo padrão no merge da C142) — a Issue fechava com labels `in-progress` mentindo e o flip precisava de aplicação manual via API. Causa raiz: `scripts/lib/github-api.mjs` lê `process.env.GITHUB_TOKEN` (linha 80), e o workflow setava apenas `GH_TOKEN: ${{ github.token }}` (commit 97135544, OPS74 — env do `gh` no recovery manual). Fix (caminho A do item): `GITHUB_TOKEN: ${{ github.token }}` no env do job dos dois flips pós-merge (`issue-done-on-main-merge.yml`, `plan-issue-ready-on-main-merge.yml`) — commits `b00c69c9` (issue-done) e `ab420f9b` (ambos); `GH_TOKEN` mantido para o `gh`. Caminho B rejeitado: não muda a lib compartilhada `github-api.mjs` — call sites locais seguem exigindo PAT explícito. Sem migration/schema/UI. Validação ao vivo (critério do item): runs pós-fix verdes; no merge do OPS86 (run 32761803843) o flip executou `[issue-transition] #832: done + in-prod + fechada` sem recovery manual. Item fechado com evidência + changelog (PR de fechamento, `Closes #783`).
