**OPS111 — Auto-unblock volta a despachar no homeserver (2026-09-15):** o launcher do OPS106 morria no setup (`pnpm/action-setup@v6` sob o Node 18 default do runner — `TypeError paths[0] undefined`, runs 34994058878/34987287001/34999635390) antes de despachar qualquer agente. `actions/setup-node@v5` (Node 24, `package-manager-cache: false`) agora roda PRIMEIRO em `.github/workflows/auto-unblock.yml`, com comentário anti-regressão; `pnpm install --frozen-lockfile` e o launcher seguem intocados. Pin novo em `tests/unit/autoUnblockWorkflow.unit.spec.ts` asserindo a ordem setup-node < pnpm/action-setup < install. `deploy.yml` e `scripts/auto-unblock*` intocados (sem auto-retry/approve). Prova canônica fica como observação pós-merge: próximo `verify` vermelho real deve gerar run Auto-unblock `success` com token Issue ou comentário single-flight.
