**C179 — Helper único da URL do thumbnail do YouTube (2026-09-17):** o literal `https://i.ytimg.com/vi/<id>/hqdefault.jpg` estava repetido em 4 módulos (`SpeechDetailPlayer.tsx`, `SpeechCutPlayer.tsx`, `SpeechCutResultCard.tsx` e `src/app/(frontend)/corte/[id]/page.tsx`), o mesmo conhecimento (host + formato + sufixo) copiado 4× com risco de drift silencioso. O plano de intenção manda extrair o helper em `src/lib/speechShare.ts`, mas `src/lib/speechVod.ts` já é o dono do par id→cover: o C175 (#1085, `2026-09-16 12:36`) criou ali `youtubeThumbnailUrl` e o C182 (#1104) compôs `speechCoverUrl(youtubeUrl)` (parse + cover) por cima. Criar uma segunda implementação seria o twin que a própria Issue quer eliminar, então o delivery reusa os donos existentes ("edit the owner, don't twin"): os três consumidores que têm o video id passam a chamar `youtubeThumbnailUrl`, e a página do corte — que só tem a URL crua — passa a chamar `speechCoverUrl`, o mesmo helper do view model da listagem e da rota de poster. Nenhuma mudança de código em `speechVod.ts` (helper e unit do C175 ficam como estão). Sem UI nova, migration, Consent, URL pública ou escrita — a crítica visual do `designer` não se aplica (port mecânico de literal, non-trigger do §Design). Pins intocados: os testes que assertam a URL final (`speechVod.unit.spec.ts`, `speechViewModels.unit.spec.ts`, `speechAcervo.int.spec.ts`, e2e de acervo/corte) passam verbatim. `pnpm gate:fast` verde (334 arquivos / 3476 testes).
