# OPS97 — Skill /react-audit com ciclo fechado plan→implement de anti-patterns React/Next.js

Nova skill `.agents/skills/react-audit/` (SKILL.md + catálogo fixo `anti-patterns.md` com 9 famílias
React/Next conferidas contra next 15.4.11 / react ^19.2.4 + `context7-setup.md`): sweep read-only →
triage per-fix (3 selos: file:line, família, fonte oficial; cap ≤6) → implementação com gate completo
bare e um commit por fix → PR único Ready **sem auto-merge** cuja descrição é o relatório consolidado,
estabilizado até checks green ∧ mergeable ∧ auto_merge null — a skill para sem mergear (humano revisa).
Mecânica anti-auto-merge: PR nasce uma única vez já estabilizado e o desarme verificado em laço
(`githubApi.ensureAutoMergeDisabled`, novo helper GraphQL `disablePullRequestAutoMerge` +
re-poll em `scripts/lib/github-api.mjs`, com `auto_merge` normalizado em `getPullRequest`) é
obrigatório após a abertura E após cada push — o safety net `agent-pr-ready-automerge.yml`
re-arma a cada synchronize, e o endpoint REST DELETE responde 404 mesmo armado-via-GraphQL
(corrida e 404 capturados ao vivo no dogfood #905).
