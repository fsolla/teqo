**C144 — Formulários de escrita com options write-scoped (municípios só do escopo de edição) (2026-08-24):** `loadWritableMunicipalityOptions` irmã de `loadMunicipalityOptions` em `src/utilities/campaignRelationOptions.ts` resolve o eixo Edição via `getWritableMunicipalityIds` (C141): irrestrito/edita-tudo delega ao loader read; somente_leitura → `[]` sem fetch; carteira → `id: { in }` ordenado por nome. Call sites de escrita trocados (atividade create com split dos filtros read em `atividades/page.tsx`, atividade edit, demanda nova, organização nova+edita, liderança nova+ficha, apoiador novo via `loadSupporterCreatePageData`); giro composer ganha `writeScope` em `loadVisitPlannerRegions`/`loadVisitCandidates` (escopa o fetch, dossier `loadMunicipalityVisitEligibility` segue read); células chip de lideranças e dobradinhas passam a `addableIds` do write scope com guardas (dobradinhas mantém a lente de leitura da carteira para visão-carteira). Sem migration, sem mudança de enforcement. Int pins novos em `campaignAdvisorPermission.int.spec.ts` (matriz do loader, giro scoped vs dossier read, page-data de supporter). `pessoas` fica como follow-up C145 (#878). Plano: `docs/plans/escopo-formularios-escrita-perfil-impl.md`.
