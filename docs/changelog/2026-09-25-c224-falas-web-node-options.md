**C224 — ingestão web de falas funciona no comando documentado (2026-09-25):** o `NODE_OPTIONS` do wrapper (`pnpm falas-web:import` → `--no-deprecation --import=tsx/esm --import=./scripts/seed-loader.mjs`) não é mais herdado pelo processo filho do yt-dlp: o dono do spawn (`defaultRunner` em `src/utilities/media/ytdlp.ts`) passa ao `execFile` um env = `process.env` sem `NODE_OPTIONS`, preservando `PATH`/`HOME`/`YTDLP_PATH`/proxies. Antes, o runtime JS que o yt-dlp invoca para resolver o desafio do YouTube carregava o loader do app e o desafio morria com o erro enganoso `ERROR: [youtube] <id>: Requested format is not available` (estágio `acquisition`), queimando a rodada do operador. Fix no dono — `resolveYtDlp`, `readYtDlpMetadata` e `downloadWithYtDlp` ficam saneados de uma vez, e nenhum script chamador ganhou tratamento de ambiente. Cobertura: unit novo em `tests/unit/ytdlp.unit.spec.ts` com `vi.mock('node:child_process')` (precedente `dbStart.unit.spec.ts`) que exercita o `defaultRunner` real, planta o `NODE_OPTIONS` do wrapper via `vi.stubEnv` e assere `options.env.NODE_OPTIONS` ausente + passthrough de `PATH` (teste não-vacuoso); RED confirmado antes do fix. Evidência de mundo real no mesmo host (A/B/C): **A** wrapper + runtime `node` puro → `Requested format is not available`; **B** mesma config sem `NODE_OPTIONS` → metadados OK; **C** dono corrigido sob o `NODE_OPTIONS` do wrapper → `PLENÁRIO - Sessão Deliberativa - 13/03/2018 - 14:00` OK. O lote completo do comando documentado não foi reexecutado aqui por falta de `DEEPINFRA_API_KEY`/`DEEPSEEK_API_KEY` no `.env.local` do worktree (limitação de ambiente, não do fix). Sem migration, sem UI (Impeccable A), sem mudança de contrato do relatório/estágios/exit codes e sem tocar o caminho Câmara. Gates: `gate:fast` (eslint + `tsc --noEmit` + 4686 unit), `format:check`, `knip` e `check:cycles` verdes; simplify sem achados e débitos triados sem registro novo.
