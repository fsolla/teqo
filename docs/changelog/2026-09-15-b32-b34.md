**B32/B34 — Raiz dos testes de lideranças que quebraram o verify: fixture e2e apagava lideranças vivas (2026-09-15):** test-only, zero mudança de produção. **Causa raiz encontrada com logs do Postgres (`log_statement=all`) e reprodução local:** `CampaignE2EOwnership.discoverOwnedRows` (`tests/e2e/fixtures/campaignE2EFixtures.ts`) montava a descoberta de lideranças com um `or` dinâmico; quando o teste não cria `campaignUser` nem `contact` (jornada read-only), o cleanup rodava com `or: []` — e no Payload 3.82.0 um `or` vazio é **descartado** pelo Drizzle (`parseParams`), a query roda **sem WHERE** e "possui" TODAS as lideranças do banco compartilhado, deletando linhas que outros workers usavam. Forense: a liderança do B32 (id 27) foi criada às 21:37:51.875 e deletada **57 ms depois** por outra sessão em cleanup; o B32 abriu "Nenhuma liderança encontrada" e o POST do status devolveu 400 com `leadership` inexistente; o B34 idem (chip sumido/`NotFound`), além do `campaignMunicipalities:659`/B176 e da classe "flaky sob 4 workers". O fixture int (`campaignFixtures.ts` `discoverDependents`) já guardava `leadershipConditions.length > 0`; o e2e não. Fix: guardar o vazio espelhando o irmão + pin do semantic em `tests/int/campaignFixtureOwnership.int.spec.ts` (o `or: []` retorna todas as linhas) + spec endurecido com os helpers do OPS83 (`waitForStreamSettled`, poll focado do chip) e `expandCollapsedChips()` para o colapso por conteúdo da célula (o clamp de 3 linhas esconde TODOS os chips atrás de "Ver mais…" quando dois nomes largos não cabem — débito de UX registrado em #1042), e `expectPostResponse` ganhou opt-in `throwOnNonOk` no owner para o próximo 400 falhar com o corpo da rota em vez de 60 s cegos. Prova: antes do fix o full local (CI=1, E2E_PROD=1, 4 workers) reprovava B32/B34/B176 com linha sumida; depois, **B32/B34/B176 verdes** (restam só fragilidades locais de agenda/contatos, fora do escopo); gate:fast (lint + typecheck + unit 3268) e gate:push verdes. Plano: `docs/plans/b32-b34-liderancas-estabilidade-verify-impl.md`. Débito UX: Issue #1042.
