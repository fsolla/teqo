**S23 — Card "Time de você" no celular: drawer até o topo e nada sobre a imagem (2026-09-20):** o passo de ajuste fino da foto no celular obrigava o visitante a rolar para cima e para baixo no meio do gesto — o drawer parava em `92dvh` (faixa livre acima) e o badge "Arraste para ajustar" ficava sobreposto à prévia. O shell mobile passa a usar `h-dvh max-h-dvh` + `pt-[max(0px,env(safe-area-inset-top))]` **no ponto de uso** (`CardsStudio`), com a primitiva `Drawer` e seus 16 consumidores intactos (precedente C107); o dialog desktop mantém a geometria `512px/92dvh`. No `CardComposer` a IIFE de descrição vira duas fontes: a frase dos modelos de foto (`photoHeaderDescription`) continua no header, e o aviso do time (`teamBodyNotice`) vira o **primeiro item visível do corpo rolável** como `DrawerDescription`/`DialogDescription` do shell (uma única Description por estado; `aria-describedby` preservado pelo registro por contexto) com uma cauda de rolagem (`pb-14` só no drawer com aviso) para o aviso sair de cena por completo no fim do scroll — cena 02 do design. O badge é removido (a prévia fica limpa; gesto, zoom e setas seguem). Sem migration/Consent/URL/PII; nada além de apresentação. Testes: `frontend` e2e ajustado (asserção do badge → aviso visível + badge ausente; poll de geometria do drawer mobile `y=0`/`844`) 11/11 verde; `pnpm gate:fast` verde (4210 unit); craft 390/1280 com crítica final do `designer` **certificada** (`openai/gpt-5.6-sol`) após um ajuste (cauda da cena 02); nenhum unit novo (sem lógica pura nova).
