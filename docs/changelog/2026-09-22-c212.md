**C212 — Viabilidade da varredura automática do Instagram e da extração de mídia por link (2026-09-22):** investigação entregue como decisão documentada — nenhum arquivo de `src/` muda. Os achados (docs oficiais datadas em 2026-09-22 + repo verificado no commit `6d721ec7` + probes públicos sem credencial) ficam anexados à própria intenção (`docs/plans/central-conteudos-varredura-instagram.md` §Achados), respondendo às quatro perguntas do gate. **Q1:** a Graph API já integrada (Instagram Login, `graph.instagram.com`) pede os campos certos e **descarta o `media_url`** de vídeo/Reel no parse (`instagramFeed.ts:94-103,146-180`) — o arquivo existe no caminho oficial, falta guardá-lo; a edge `/{ig-user-id}/media` devolve até as 10K mídias mais recentes, não cobre stories e documenta paginação time-based (`since`/`until`), com cursor (`paging.cursors`) como paginação geral da Graph API; rate limit é a fórmula BUC `4800 × impressões` com webhooks recomendados (número real da conta: a confirmar na implementação); token long-lived de 60 dias renovável via `refresh_access_token` com ≥24 h de idade, devolvendo `expires_in` (o repo hoje só renova após falha e não persiste validade); a cadência de board (5 min) não serve para acervo — diária + "Sincronizar agora" cabe nos limites. **Q2:** conteúdo próprio extrai pela Graph API; terceiro só por oEmbed/embed — a doc proíbe "consuming, manipulating, extracting, or persisting the metadata and content", e o probe sem token devolveu 403 `Provide valid app ID` ao pedir metadados, então a peça de terceiro circula pelo link e nada é persistido/baixado; a YouTube Data API não tem URL de arquivo (o playback oficial é o embed) e o oEmbed público confirma título/thumbnail/iframe sem chave. **Q3:** tabela oficial × scraping × link manual — scraping **proibido** (Meta Automated Data Collection Terms exige permissão escrita; YouTube Developer Policies vedam scraping e download/armazenamento de audiovisual), recomendação é API oficial, com o que se perde declarado (stories, terceiros, >10K). **Q4:** mecanismo, limites, fora de escopo e o item de implementação **descrito, não criado** (irmão do C211). O refinamento de terceiro que o C211 precisa aplicar virou follow-up rastreado (#1263, `depends: C211`), já que a Issue do C211 está `in-progress` e o plano dela não pode ser editado. Sem migration, sem Consent/access, sem UI (Impeccable A), sem credencial em log, feed da home (S3) intocado.
