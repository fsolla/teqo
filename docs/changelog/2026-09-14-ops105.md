**OPS105 — Visão nativa: remove o workaround Qwen (`design-vision`/`design-vision-fidelidade`) (2026-09-14):** o modelo padrão da máquina/sessão (`deepseek/deepseek-flash`, DeepSeek V4.1 Flash, OPS101) enxerga imagens — o desvio por subagentes pinados em `deepinfra/Qwen/Qwen3-VL-30B-A3B-Instruct` virou custo morto (uma chamada de Task e um modelo extra para algo que a sessão faz direto). Saem `.opencode/agent/design-vision.md` e `.opencode/agent/design-vision-fidelidade.md`; a persona `designer-campanha-solla` (bloco 5 do fluxo) passa a ler screenshots/referências direto com a tool Read e mantém o checklist de crítica visual estruturada (o que está na tela, hierarquia/CTA dominante, contraste, espaçamento, mobile 375px, acessibilidade, ajustes numerados; a referência é o alvo, nunca criticada), com saída explícita se a sessão rodar modelo text-only (ex.: `deepseek-v4-pro`): parar e pedir troca via `/models` para `deepseek/deepseek-flash`, nunca descrever o que não viu; `docs/campanha/prompt-montagem-home.md` idem na validação da home. Fora do repo (máquina, não versionado): os dois modelos Qwen3-VL saíram do `~/.config/opencode/opencode.jsonc` (o provider DeepInfra segue servindo o catálogo). Sem app, sem schema, sem migration, sem Consent. Verificação: `opencode debug config` exit 0 (deepinfra sem `models` custom), zero refs a `design-vision`/Qwen em `.opencode/`, `prettier --check` nos arquivos tocados. Docs históricos (`docs/plans/*`, `docs/changelog/*` e o HISTORY congelado) citam os subagentes como registro — intocados.
