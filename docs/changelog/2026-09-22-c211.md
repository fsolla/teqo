**Central de Conteúdos interna — envio em lote, link, catalogação e publicação (C211, 2026-09-22):** a assessoria de comunicação ganhou `/campanha/comunicacao/conteudos` (sub-item "Conteúdos" na nav da vertical): envia N arquivos de uma vez (1 request raw-body por arquivo, fan-out ≤2 no cliente, até 50 por lote, 4 GiB cada) ou cola um link do Instagram/YouTube, e cada peça sai com estado próprio ("Processando" | "Pronto" | "Falhou" + "Reprocessar"), transcrição/texto e catalogação automática revisável (título, descrição, tipo, temas, cidade, região, instituição, data, duração). Duas collections novas — `contentPiece` (enums pt-BR, `slug` gerado no 1º "Publicar" e imutável, `sourceUrl` única, `searchText` com índice trigram) e `contentMedia` (upload privado, registrado no S3) — com migration `20260923_032714_add_content_piece`; gate único `canReadCommunicationCatalog` (advisor/leader negados; delete só pelo admin). O pipeline reusa o que já tinha dono: job `after()` + reaper preguiçoso + retry condicional do C199, `deepInfraTranscribeSegments` e os helpers de ffmpeg/chunk, o classificador de facetas do acervo e o `institutionCatalog`; a extração de link é só pela Graph API da conta própria (casa o permalink canônico; YouTube/terceiro = peça-link, sem download de terceiro). `curatedFields` impede o job de sobrescrever edição humana; o kill switch preserva arquivo, slug e `publishedAt`, e um `afterChange` na collection busta o tag `contentPieces` (a costura de cache com o S27). `instagramFeed` passou a expor `mediaUrl` (campo que o `MEDIA_FIELDS` já pedia). UI portada classe-a-classe do design aprovado (`central-conteudos-ingestao-ui-design.html`): lista desktop + cards mobile, diálogo de lote com progresso por arquivo, diálogo de link com o aviso "Sem scraping", ficha editável e Publicar/Despublicar. Testes: 2 unit specs novos (vocabulário/link/slug/lista + catalogação com deps injetadas), 1 int spec (access, upload, job, falha/retry, reaper, texto, publish/slug, curadoria, link/duplicado, anexo, lista) e 7 e2e na vertical de comunicação. Sem Consent novo (não há PII de eleitor) e sem invasão do S27/C212/C213. Endurecimento de dono compartilhado: `revalidateTag` passou a ser chamado por um wrapper que engole só o invariante "static generation store missing" (write de Local API fora de request — fixtures e2e/scripts/seeds não têm cache para bustar), preservando o erro real dentro de request. Follow-up de engenharia registrado em #1267 (`docs/plans/escala-dry-pos-c211.md`).
