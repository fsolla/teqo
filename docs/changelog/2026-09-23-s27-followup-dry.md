**Dono único do OG image de fallback do frontend público (S27-FOLLOWUP-DRY, 2026-09-23):** o fallback de imagem OG (imagem própria da página → imagem do global `metadata`, populando o id quando numérico → `null`) deixou de estar copiado em 6–7 páginas públicas e passou a viver num dono único server-only, `src/utilities/ogImageReads.ts` (`resolveOgImage(configured) → { url, media }`): a fonte própria (URL relativa, URL absoluta externa como o thumbnail do YouTube, ou `Media`/id cru de leitura depth-0) vence quando tem URL utilizável; senão o global resolve o id em `media`; a URL final é absoluta contra o origin de deploy (`resolveDeploymentOrigin`), nunca contra o domínio canônico do global — o proxy `/api/media/file/…` só existe no deployment e URL relativa o crawler do WhatsApp não resolve. O resolver reusa `resolveSiteMetadata`/`resolveDeploymentOrigin`/`toAbsoluteUrl` (o `seo.ts` segue puro, sem I/O) e devolve a `Media` escolhida junto da URL para os call sites que emitem width/height/alt/mimeType (artigo, petição ×2, layout). Migraram os 8 consumidores — os 2 do S27 (`/conteudos` e `/conteudos/<slug>`), `jingles`, `corte/[id]`, `abaixo-assinado/[id]` (metadata + JSON-LD), `[type]/[category]/[slug]` (o helper local morreu) e o `layout.tsx` — e `resolveShareLinkOgImageUrl` passou a delegar, sem twin; `artigos/page.tsx` ficou fora (fonte é `home.image`). Deltas intencionais: `corte`/`abaixo-assinado`/artigo absolutizam no deployment origin em vez do `siteUrl` canônico quando os dois divergem, e o `layout.tsx` deixa de emitir OG relativo; canonical e JSON-LD `url` seguem no canônico. Sem migration, sem Consent/access e sem mudança de superfície visual (Impeccable A). Verificação: unit novo do resolver (8 casos — sem imagem → `null`, própria relativa no origin de deploy, absoluta sem origin, `Media` própria, id cru, fallback objeto/id, `Media` sem url cai no global, relativa sem origin → `null`), int do shareLink verde sem tocar nas asserções (23 casos), e2e `frontendConteudos`/`frontendJingles`/`campaignSpeechCut`/`frontendShareLink`/`frontend` verdes (com asserts novos de `og:image` em jingles e corte, via `tests/helpers/metaContent.ts` extraído do spec do share-link), `gate:fast` (lint/typecheck/4421 unit), `pnpm test:int` (918) e `knip`/`check:cycles`/`format:check` limpos; o dono novo entrou no `E2E_AFFECTED_MANIFEST` nas 4 entradas de OG públicas.
