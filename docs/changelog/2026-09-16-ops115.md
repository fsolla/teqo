**OPS115 — work-issue/agent-work-issue: designer em qualquer toque de design + crítica visual fail-closed (2026-09-16):** qualquer toque de design nas duas skills de execução passa pelo agente `designer` e todo diff que muda estrutura visual só é certificado com crítica contra o app renderizado (screenshots 390/1280 + estados). `execution-pipeline.md` (dono da mecânica compartilhada) ganha a seção `## Design` com os 4 triggers (a superfície/estado novo que o design do plano não cobre — o designer estende o artefato ANTES de implementar; b — o aprovado não pode ser seguido como está; c — crítica final do diff; d — ícones/ilustrações), os non-triggers que seguem com o implementador (fiação de dados, hooks/rotas/queries, copy, port mecânico, bug que restaura o aprovado, reuso de tokens; default é não despachar) e o fechamento fail-closed: sem tier primário não há certificação — `work-issue` para antes do `pnpm push` e aguarda sign-off humano (só então o PR nasce Ready com `Design tier: DEGRADED`), enquanto o fluxo autônomo (`agent-work-issue` e `work-issue --auto`) **não abre PR**: comenta a Issue + flip `blocked` (ladder/`DEGRADED` citados de `ui-design-html.md`, fonte única, sem duplicar slugs). As duas skills declaram só o delta (sub-agente Designer + checklist + ponteiros de fechamento); `engineering-standards.mdc` ganha o bullet de ownership "Visual structure belongs to the `designer`" vizinho de "Edit the owner, don't twin"; `engineering-brief.md` e `docs/AGENT-OPS.md` ganham o ponteiro. Diff doc-only (`.agents/` + docs), sem `src/`/migration/schema; `pnpm gate:fast` verde e os literais pinados do `--auto` (`tests/unit/skillsAutoFlag.unit.spec.ts`) preservados.
