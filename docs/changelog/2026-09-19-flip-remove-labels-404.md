**FIX — flip pós-merge abortava com 404 ao remover label ausente; idempotência de `removeLabels` restaurada e pinada com 2 testes (2026-09-19):** o job `issue-done-on-main-merge` falhava `GitHub API DELETE .../labels/in-progress → 404 {"message":"Label does not exist"}` quando a Issue citada não tinha o label (keyword-close, re-run do dispatch de recuperação ou Issue que nunca entrou em `in-progress`), abortando o lote no primeiro item — evidenciado no merge do PR #1202 (#1176 fechada sem `done`/`in-prod`; #1177/#1184 não processadas) e antes em #1155/#1169 (runs 35416333242/35416350778). Causa-raiz: o `ignoreNotFound` de `scripts/lib/github-api.mjs` existia desde `4486c05b` (idempotência, OPS46-S1) e foi revertido por `9467e0f2` (C140 — commit de 70+ arquivos levou o blob antigo de `github-api.mjs`, artefato de rebase); sem teste, o revert passou ~4 semanas silencioso. Fix: restauração exata (`request` tolera 404 com `ignoreNotFound`; `removeLabels` o usa) + 2 pins em `tests/unit/githubApi.unit.spec.ts` (404 tolerado; não-404 ainda lança) — RED pré-fix, GREEN pós (31/31). Sem migration/schema/Consent; sem deploy de aplicação. Post-mortem: `docs/postmortems/2026-09-19-flip-remove-labels-404.md`.
