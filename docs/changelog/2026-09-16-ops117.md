**OPS117 — Guard de escrita dos agentes de design além dos file tools (bash) (2026-09-16):** fecha o resíduo declarado no OPS113 — os agentes `designer` e `designer-degraded` eram fail-closed nos file tools (`edit`/`write`/`patch`), mas a escrita via **bash** (redirecionamento, `sed -i`, `tee`) escapava do gate. Ambos os frontmatters ganham o mesmo bloco `permission.bash`: `'*': ask` + deny dos vetores de escrita (`*>*`/`>>`, editores in-place `sed`, mutadores `cp/mv/rm/mkdir/touch/truncate/dd/install/ln/chmod/chown/tee`, shells/interpretadores `bash/sh/zsh/python/node/bun/deno/perl/ruby`, `find -exec/-delete`, `xargs`, `git checkout/restore/apply`) e, **por último** (`last-match-wins`), o allow do artefato (`* > docs/plans/*-ui-design*`, `prettier --write docs/plans/*-ui-design*`). Evidência do mecanismo: o tool bash do opencode 1.18.31 resolve `permission.bash` pelo source tree-sitter do comando e, em redirecionamento, casa o `redirected_statement` inteiro **incluindo o caminho de destino**; smoke em subprocesso `opencode run --auto --agent designer` provou `echo x > src/…` negado (arquivo ausente), artefato permitido, `sed -i`/`tee`/`printf >`/`bash -c`/`python3 -c` negados e `cat` (leitura) não bloqueada. Novo `tests/unit/opencodeAgents.unit.spec.ts` (anti-drift: os dois frontmatters têm o bloco `bash` idêntico, o allow do artefato é a última regra e o `edit` fail-closed do OPS113 segue intacto). 1 nota na doutrina `.agents/skills/plan-issue/ui-design-html.md` e 1 frase em `docs/AGENT-OPS.md`. Resíduo honesto declarado: não é sandbox de SO — sob `--auto` um vetor exótico fora da deny-list ainda poderia passar; sem migration, sem schema e sem `src/`. Verificação: `opencode debug agent` (ruleset resolvido) + smoke subprocesso, `pnpm gate:fast`; Closes #1063.
