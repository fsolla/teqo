**S39 — Home: seção divulgando a Central de Conteúdos (2026-09-24):** a home pública ganha, entre os cards personalizáveis e a newsletter, a seção que mostra até 3 peças publicadas da Central (`/conteudos`, S27) e leva para lá pelo CTA `Ver todas as peças`; a ordem é **município do visitante → região → mais recentes** e sem peça publicada a seção inteira não é renderizada (fail-closed pela mesma leitura cacheada/tag `contentPieces` do rodapé — despublicar reflete sem deploy). O servidor renderiza a seleção **recente** já no HTML estático (a seção nasce completa e útil sem JS/conexão ruim) e a ilha client (`ContentPieceHomeBoard`) só **troca em silêncio** para o território do visitante: GPS automático apenas quando a permissão já está `granted` (sem dialog surpresa, sem auto-prompt — o `sessionStorage` do B14 não é usado) e, fora disso, só pela affordance explícita `Usar minha localização`; a posição é casada no browser contra as malhas commitadas (zona → município → território; Salvador resolve a ZE exata pelo nome do catálogo) e **nada é persistido, logado ou cookiado** (nem a localização, nem o IP). O caminho por IP foi **confirmado inviável** nesta entrega (não há fonte de geo-IP no repo; `headers()` dinamizaria a home ISR) — sem GPS acessível a seleção degrada para recentes, exatamente a degradação prevista no literal da intenção; fix grosso (`accuracyM > COARSE_ACCURACY_M`) mantém só a região, nunca "Do seu município" com confiança falsa. Peças: projeção lean nova (`src/lib/contentPieceHomeSelection.ts`, sem `searchText`/`description` no HTML), seleção/ordenação pura com dedup e anotação de procedência (`Do seu município`/`Da sua região`/`Mais recente`), resolução ponto→visitante pura reusando `findContainingFeature`/loaders lazy, card leve (`ContentPieceHomeCard`) com `ContentPieceMedia` (mídia só no toque, `preload="none"`) e o título/meta extraídos para `ContentPieceCardParts` (dono único com o card do catálogo); cards personalizáveis (itens sintéticos) nunca entram como amostra (a garantia é a origem do pool); demais seções, rodapé, selo do S27, `/conteudos` e o `SiteHeader` intocados; sem migration/Consent/access, sem rastreio novo. Gate: `designer` trigger (b) aprovou o estado inicial sem skeleton (cena 04 do artefato atualizada; `Design tier: openai/gpt-5.6-sol`) — crítica final (c) registrada no PR. Verificação: `pnpm gate:fast` verde (lint + typecheck + 4513 unit, incluindo os novos specs da seleção/resolução e do board), e2e novo no `frontendConteudos` (seção na home + kill switch + ordem `cards < content-pieces < newsletter` + overflow 390 ≤ 1 + amostra do município com geolocalização mockada), e2e da home (`frontend`) inteiro verde com a seção publicada e o spec B14 (`campaignNearestMunicipality`) verde com o `interiorPointOf` agora compartilhado em `tests/helpers/featureBounds.ts`.
