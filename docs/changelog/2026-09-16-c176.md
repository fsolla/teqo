feat(C176): página pública do corte — revisão de design (superfície neutra, vídeo herói, WhatsApp-first no mobile)

Redesenha `/corte/<id>` preservando o contrato C167 (rota, `noindex`, metadata). O "fundo colorido" era o gradiente do `body` herdado de `:root` — o segmento não tinha `data-theme` nem layout; um novo `src/app/(frontend)/corte/layout.tsx` espelha `[type]/layout.tsx` (`data-theme="editorial"` + `h-dvh overflow-y-auto bg-background`), consertando de uma vez a page e o `not-found` e provendo o scroll interno (sticky do `SiteHeader`). A page porta o gate aprovado (`docs/plans/corte-pagina-publica-design-ui-design.html`): vídeo herói (full-bleed no mobile, arredondado no desktop; `object-cover` faz o poster 4:3 preencher o quadro 16:9), título alinhado à esquerda, meta `tipo · data · duração` (relógio `02:14`, literal do gate), descrição com medida de prosa controlada e contraste AA, kit de compartilhamento responsivo — WhatsApp primário full-width acima da descrição no mobile (alvo 48 px) e kit inline de 3 botões no desktop (44 px) —, ações secundárias em grade de 2 colunas no mobile, link da sessão no YouTube inline na meta (desktop) e avulso (mobile), e rodapé com crédito CC BY + nota de não listagem. Estado indisponível redesenhado (ícone, copy e botão "Voltar ao início") e centralizado. `SpeechCutShareActions.tsx` extraiu `SpeechCutWhatsAppButton`/`SpeechCutDownloadButton` (o dono do URL/rótulo do WhatsApp segue `buildSpeechCutShare`) e ganhou `controlClassName` opcional — acervo/biblioteca continuam idênticos. Guard e2e novo: o HTML servido carrega `data-theme="editorial"` (regressão do fundo tintado). `campaignSpeechCut` (5) e `campaignSpeechAcervo` (11) verdes locais; `pnpm gate:fast` verde. Sem migration/schema/access/Consent.

Closes #1086
