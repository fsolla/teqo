**OPS127 — launch do worktree nasce na variante `max` do modelo selecionado (2026-09-18):** todo lançamento (`worktree next|plan|new|fix`, incluindo `fix --headless`/auto-unblock e o preset sem flag) passa a pedir a variante `max` do modelo escolhido, sem flag nova no terminal. A variante viaja no **body do `POST /session`** (`buildCreateSessionBody` → `Session.model.variant`, herdada pelo TUI anexado) e no argv do `opencode run` do driver destacado (`driverArgs`) e do headless (`opencodeHeadlessArgs`); o `opencode attach` e a diretiva `launch` continuam **sem** `--variant` (o yargs do TUI rejeita o flag — veto OPS95 preservado) e a config global da máquina fica intocada. Reabre o veto OPS95 apenas para o caminho da sessão: o fato novo é que o opencode 1.18.31 aceita `variant` dentro de `model` no `POST /session` e persiste. Dono único do literal é `MODEL_VARIANT` em `scripts/lib/agent-session.mjs`; o cardápio/preset de modelos (`WORKTREE_MODEL_MAP`, `OPENCODE_PRESET_MODEL`) e o mapa de flags não mudam — `--go` segue `opencode-go/deepseek-v4.1-flash` (OPS112). Sem migration, sem schema, sem UI. Verificação: `pnpm test:unit` (pins de `agentSession.unit.spec.ts` e `worktree.unit.spec.ts` atualizados) e `pnpm gate:fast`.
