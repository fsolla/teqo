**C196 — skill `/reels-tutoriais`: captura + composição do vídeo (2026-09-18):** a skill `.agents/skills/reels-tutoriais/SKILL.md` (+ command fino `.opencode/commands/reels-tutoriais.md`) gera um Reel tutorial vertical de uma funcionalidade do site dirigindo o **site real** em viewport de celular e compondo com ffmpeg. Pipeline determinístico em `scripts/build-reel.mjs` (`pnpm reels:build <slug>`) + módulos puros em `scripts/lib/reel*.mjs`: o shot list versionado (`scripts/reels/shot-lists/cards.json`) é a fonte única da verdade; a captura usa o Chromium do `@playwright/test` com viewport 360×640 + `--force-device-scale-factor=3` (frames nativos 1080×1920) e um screencast CDP próprio que grava JPEGs com timestamp de parede + encode offline (o `recordVideo` do Playwright grava CFR 25 e derrapa o relógio — o zoom/cursor derivam do log de cliques e ficariam dessincronizados); o cursor sintético e o halo do alvo são DOM-injected (headless não desenha cursor) e um ticker invisível garante frames durante as pausas. A composição faz um clipe H.264 por cena com `zoompan` (easing do clique) + legendas queimadas em PNG transparente (port classe-a-classe do hi-fi `docs/plans/reels-tutoriais-ui-design.html`) e concatena; o pacote sai em `data/reels/<slug>/` (`reel.mp4` sem áudio 1080×1920/30fps, `capa.png` que sobrevive ao recorte 1080×1350 e `metadata.json` com o hash do shot list). FFmpeg resolve `FFMPEG_PATH` → PATH → `@ffmpeg-installer/ffmpeg` (nova devDependency registry-only; `ffmpeg-static` foi rejeitado porque baixa de GitHub releases, que o homeserver não alcança) com probe fail-closed de libx264 + filtros. Ajustes passam só pelo shot list; o gate aprova/regenera e **nada é publicado em rede social** (biblioteca privada é o C195). Sem schema/migration; o DB e o site público não são tocados. Verificação: `pnpm test:unit` (26 testes novos em `reelShotList`/`reelTimeline`/`reelFfmpeg`/`reelsTutoriaisSkill` + pin do command) e `pnpm gate:fast`.

**C196 — identidade oficial do kit 1313 (2026-09-19):** o kit da campanha (manual + PNGs oficiais) entrou no repo: ativos em `public/campaign-kit/` (marca positiva/negativa completa, lockups do nome, número e estrela) + manual em `docs/campaign-kit/manual-campanha-jorge-solla-1313.pdf` + README com paleta (`#e4102f`/`#184e92`/`#ffeb00`/`#009647`) e regras de uso. As cenas gráficas do reel passaram a usar a paleta e as marcas oficiais (positiva em fundo claro, negativa em fundo vermelho/escuro; slogan só dentro das marcas completas; estrela a 100% sobre disco branco), mantendo a paleta do site nas cenas de captura; a skill e o agente `designer-campanha-solla` referenciam o kit.
