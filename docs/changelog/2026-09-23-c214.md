**C214 — Briefing C210 replanejado: 4 folhas 100% recorte (2026-09-23):** o Briefing de capacitação da família de dossiês (cidade/instituição/tema) foi replanejado a partir da crítica de produto: as 4 folhas fixas deixaram de ter capa/objetivo/trajetória e recomendações genéricas (roteiro de 5 passos, justificativa científica, "o que evitar", "o que conferir", "se você não souber" e "limites/defeso") e passaram a ser **todas do recorte** — `defesas` (princípios/crenças + posições com fonte), `essencial` (fatos-âncora com fase + o pedido literal e uma linha de plano), `qa` e `qa-2` (perguntas prováveis, metade do documento, 1/2 e 2/2). O contrato do `briefing.json` mudou: `script.steps`/`avoid`/`checklist` saíram, entrou `plan` (1 linha), `defenses` virou a folha de abertura e o Q&A foi ampliado (8–10 no alvo); o shed determinístico agora é `qa → defesas`, com essencial/pedido nunca cortados. O dono `scripts/lib/briefingContent.mjs` ganhou **âncora item-level**: como o id de checklist se repete entre itens e o ledger resolvia pelo primeiro, o âncora passou a aceitar `sourceUrl` (copiado do item do research) para resolver o par exato `factId` + `sourceUrl` — par errado falha fechado (o Q&A não repassava o `sourceUrl`, corrigido junto). O design hi-fi `docs/plans/briefing-capacitacao-solla-ui-design.html` foi refeito pelo designer (4 cenas novas, classes `.principles-copy`/`.request-block`/`.request-plan`; removidas `.lede`/`.script-list`/`.avoid-list`/`.check-list`/`.two-column`/`.limits`/`.sheet-compact`), a skill e o agente autor foram atualizados e os specs `briefingContent`/`briefingRender`/`briefingSkill` reescritos. O briefing da UFBA foi re-autorado (10 defesas, 8 fatos-âncora, 12 perguntas, âncoras item-level auditadas) e sai em 4 páginas sem cortes. Sem `src/`, sem schema/migration/rota; `pnpm gate:fast` verde (lint, typecheck, 389 arquivos / 4310 testes). Issue: #1284.
