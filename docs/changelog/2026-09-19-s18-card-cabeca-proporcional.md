**S18 — Card "Time de você": cabeça do visitante proporcional à dos candidatos (2026-09-19):** o auto-enquadramento do card time passa a mirar a proporção da cabeça: MediaPipe `FaceDetector` + `blaze_face_full_range.tflite` (Apache-2.0, 1,0MB lazy e same-origin, commitado com NOTICE; `minDetectionConfidence: 0.5`, maior rosto vence) mede o rosto no canvas do recorte — a mesma régua do lado dos candidatos, cuja referência é a mediana literal `TEAM_CARD_FACE_REFERENCE_SIZE = 103` medida uma vez sobre `public/cards/team-card-base.png` (5 caixas, SHA do modelo no comentário). A math pura nova `frameCardPhotoOnFace` escala a face à referência (±20%) e mantém o rosto sempre dentro da janela (com a referência 103px a face desenhada nunca passa de ~118px), com fallback silencioso ao enquadramento S15 (`frameCardPhotoOnBbox`) em qualquer falha/ausência de rosto — o único erro recuperável continua sendo o do recorte. **Decisão de execução (humano, 2026-09-19):** o piso de cobertura do S13 (`zoom ≥ 1`) deixava selfies fechadas em 2,0–2,6× a cabeça dos candidatos (craft: 207px/270px vs. 103px); o enquadramento do time pode descer até `CARD_TEAM_PHOTO_MIN_ZOOM = 0.2`, o que mantém a face alcançada em ≤ +15% da referência por construção (a foto menor fica _dentro_ da janela — contain/cover por eixo no `clampCardPhotoTransform`, default dos 3 modelos S13/S14 intacto) e deixa o ajuste fino do visitante como escape; a arte-mestre aparece ao redor, como na Cena 1 do design. Detector com catch local sem log; e2e determinístico pelo stub estendido (`'noface'` → fallback; rosto → slider < 0.5) e unit novo da math/pins. Integra o S17 (harmonizar cores) e o S16 (banner azul dinâmico), mergeados no mesmo pipeline: as features convivem (a variante harmonizada e o banner dinâmico seguem intactos) e o e2e do S17 teve o ponto de amostra deslocado para dentro da foto desenhada pelo enquadramento novo. Sem migration, sem Consent/access, sem copy/controle novos; 100% no aparelho. Verificação: unit dos specs puros (4083), `pnpm gate:fast`, e2e do `frontend` (32 verdes), craft com engine real (retrato real: 96/86/98px vs. 103px, fallback sem alerta). Design artefato `DEGRADED` (frontier sem quota no plan-issue) com crítica final Tier 1 certificada (`openai/gpt-5.6-sol`): sign-off humano registrado no PR #1210.
