**C168 — Acervo: biblioteca de cortes (origem, download, compartilhar, editar título/descrição) (2026-09-15):** a vertical Comunicação ganha a **biblioteca do que já foi cortado**, para o corte não morrer no instante em que o link é enviado. Nova rota **`/campanha/comunicacao/acervo/cortes`** lista os cortes mais recentes primeiro, cada item com status (publicado/despublicado/processando/falhou), data de criação, a **fala de origem** (tipo/fase/data com link para o acervo) e a duração, com busca por título/descrição (`like` ILIKE em `title`/`description`, sem migração — o volume não paga `searchText`/índice; gatilho: ~5–10 mil cortes ou busca acento-insensível) e a paginação padrão da casa pelo mesmo esqueleto do acervo de falas (`CampaignPageShell`/`CampaignListPendingBoundary`/`Footer`/`EmptyState`, omnibox degenerado `searchOnlyListOmnibox`). O detalhe **`.../cortes/[id]`** toca o **mesmo MP4** do público num `<video>` simples, mostra o discurso de origem (degradando quando a fala foi excluída — FK `SET NULL`), edita **título/descrição** inline e tem o **kill switch** de publicar/despublicar com a copy aprovada no `AlertDialog` ("Despublicar este corte?" → "O link público deixa de funcionar para quem já recebeu. Você pode publicar de novo quando quiser."); despublicado, o link `/corte/<id>` para de responder e **republicar restaura o MESMO link** (id imutável, `publishedAt` = última publicação, bust de cache pelo `afterChange` do C167). No detalhe também se baixa o MP4, copia o link e compartilha no WhatsApp (`SpeechCutShareActions`, o link é o da página pública); para corte despublicado sobra só o download (o link está morto). Acesso intocado no domínio: `communicator`/`coordinator`/`candidate` leem/editam, `advisor`/`leader` negados fail-closed (gate `speechCatalog` na página e repetido em cada action); o texto editável é o do CORTE, nunca o da fala (`canReadSpeech`, não `canUpdateSpeech`) nem o vídeo. Mutação em JSON routes (`campaignJsonMutationRoute`) — `.../cortes/[id]/texto` e `.../cortes/[id]/publicacao` — sobre `updateSpeechCutTextForActor`/`setSpeechCutPublishedForActor`; publicar é recusado enquanto não houver arquivo (`SPEECH_CUT_PUBLISH_NOT_READY_MESSAGE`). Sem collection/migration nova, sem deletar/métricas/kits/versionamento, sem editor de vídeo. Testes: unit do contrato de URL (`speechCutListUrl`), int da lista/busca/ordem, origem (presente e degradada), edição refletida, matriz que nega advisor/leader e o ciclo do kill switch com mídia real (fake ffmpeg) provando que o anônimo deixa de ver o corte e que o link volta idêntico, e e2e browserless estendendo `campaignSpeechCut` (lista/detalhe 200 para communicator, edição e toggle por HTTP, 404 público após despublicar, e `advisor`/`leader` redirecionados na página e negados na rota de edição).
