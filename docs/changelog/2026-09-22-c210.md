**C210 — Briefing de capacitação Solla 1313: até 4 páginas para quem vai pedir voto no recorte (2026-09-22):** a peça que faltava entre o dossiê e quem pede o voto passa a ser o **3º entregável** da família `dossie-solla-{cidade,instituicao,tema}` — dossiê + boletim + **briefing de capacitação** na mesma invocação — e também roda sozinho (`/briefing-capacitacao-solla cidade:Miguel Calmon`, `instituicao:UFBA`, `tema:Educacao`, lote por vírgula). Um recorte = **um PDF A4 de até 4 páginas + companion `.md`**, portado classe-a-classe do design aprovado (`docs/plans/briefing-capacitacao-solla-ui-design.html`): folha 1 essencial com fato-âncora e fonte; folha 2 "o que Solla defende" (C209 quando existir; sem registro = linha de lacuna) + roteiro do pedido com o literal **"vote 1313"**, plano de voto e compromisso nomeado; folha 3 perguntas prováveis × melhores respostas dos dois lados (régua reconhecer → fato verificável → pedido); folha 4 o que evitar + o que conferir + limites/defeso, com o rótulo **"Insumo interno de capacitação — não publicar"** em todas as folhas. Ele **deriva do dossiê, sem segunda pesquisa factual**: o autor (subagente `.opencode/agent/briefing-capacitacao-solla.md`) escreve um `<slug>.briefing.json` e o build (`scripts/build-dossie-solla-briefing.mjs`, offline) resolve cada `factId` contra o ledger `bulletinFacts` de `buildDossierReport` — âncora sem fonte **falha fechado**, lacuna vira `gapReason`, chave de cenário/estimativa/staff-only é proibida por deny-list, e a redação é auditada citação por citação pelo orquestrador. O teto de 4 páginas é rígido e provado por três guardas novas em `scripts/lib/buildPdf.mjs`: `assertPageCount` (contagem de `[data-page]` ≤ `maxPages`), fit por folha e `emitHtmlSinglePdf` (emit aditivo de documento único, com `emitHtmlPairPdf` e os 3 builders publicados intocados); no estouro, `trimBriefing` (`scripts/lib/briefingContent.mjs`) corta por prioridade declarada — `qa` até o mínimo de 4 com os dois lados → `defesas` → `conferir` → `evitar` até 3 —, declara o resto na folha ("e mais N no briefing completo (.md)") e o `.md` (`scripts/lib/briefingRender.mjs`) carrega tudo; essencial, roteiro e identificação nunca são cortados, e abaixo do mínimo o build morre apontando a lista a encurtar. Sem schema/migration/DB/rota; artefatos gitignored (`data/dossie-solla-*/`, `docs/research/dossie-solla-*/`); sem CTA público e sem cenário eleitoral (o briefing não promete efeito). As 3 SKILLs irmãs e os 3 commands ganharam o ponteiro do 3º entregável (mesmo build, nunca um segundo pipeline). Verificação: `pnpm gate:fast` verde (389 arquivos / 4284 unit, lint 0 warnings, `tsc --noEmit`), knip limpo (`SCRIPTS_SPEC_PINNED` atualizado com os 2 libs pinados por spec), build real de Miguel Calmon em **PDF de 4 páginas** com o rótulo em todas (conferido por `pdfinfo`/`pdftotext`), recorte tema (`--unit=theme`, Educação) também em 4 páginas, e a prova do corte: fixture de estresse com 12 perguntas sai em 4 páginas com "e mais 6 perguntas" na folha e as 12 no `.md`. Crítica final do `designer` (tier primário `openai/gpt-5.6-sol`): **Certificado** — paridade estrutural e visual confirmada contra o hi-fi aprovado, com 3 ajustes não bloqueantes aplicados (kicker com o nome do recorte nas folhas 2–4, negrito de varredura no checklist, respiro/ênfase na nota do shed).
